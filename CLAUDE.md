# DaliGo — Guía del proyecto y bitácora

> **Documento vivo.** Recopila las *formas correctas de hacer las cosas* en DaliGo y una
> **bitácora de errores y soluciones** que crece con el tiempo. Mientras más lo alimentemos,
> más fácil se vuelve trabajar en el proyecto y más rápido entran los nuevos colaboradores.

---

## ⭐ La regla de oro (obligatoria para todos)

**Antes de trabajar:** lee este documento.

**Cuando tengas una dificultad o un error** —de deploy, build, creación de herramientas/features,
infraestructura, base de datos o **cualquier tema de la app web y relacionados**— **y lo resuelvas:**

1. Agrega una entrada **al inicio** de la [Bitácora](#bitácora-de-errores-y-soluciones) usando la plantilla.
2. Hazlo **ANTES de cerrar / commitear** la tarea. No lo dejes "para después".
3. Sé concreto: el **síntoma** (mensaje de error textual si lo hay), la **causa raíz** y la
   **solución que SÍ funcionó** (comando / archivo / commit).

Esto aplica por igual a **colaboradores humanos** y a **asistentes de IA** (Claude Code lee este
archivo automáticamente al iniciar cada sesión en este repo).

**Además, toda sesión de trabajo:** arranca y termina con `docs/PROTOCOLO-SESION.md` (retomar en
10 minutos §1; checklist de cierre §3). El estado del proyecto vive en `docs/RUTA-MAESTRA.md` —
**un paso completado sin marcar ahí (en el mismo push) es trabajo que no existe.**

---

## Cómo trabajar en DaliGo (formas correctas)

### Recetario de prompts (consúltalo ANTES de improvisar uno propio)
Ante cualquier prompt por redactar —delegaciones, planes, auditorías, fichas, cierres—, consulta
PRIMERO `docs/delegacion/RECETARIO-PROMPTS.md` según el momento del flujo: orientarse (§1),
planificar (§2), construir (§3), pre-merge (§4), incidentes, datos, dirigir y cierre. Las fichas
traen los gotchas de este proyecto ya horneados; un prompt improvisado los pierde. Atajos:
skills `/arranque`, `/cierre` y `/pre-merge` (delgadas — el texto canónico vive en el recetario).

### Stack y convenciones
- **Laravel 12** + **Blade**, **PHP 8.3.31** (fijado en `composer.json` → `config.platform.php`; local = producción).
- **Tailwind CSS v4** vía `@tailwindcss/vite` (sintaxis `@import 'tailwindcss'`, **no** `@tailwind`).
  No existen `tailwind.config.js`/`postcss.config.js` — la config vive en [resources/css/app.css](resources/css/app.css).
- **Color de marca centralizado** en `resources/css/app.css` → bloque `@theme` (`--color-brand-*`, naranjo `#EA580C`).
  **No hardcodear** naranjos en las vistas; usar las utilidades `brand-*`.
- **UI en español**, tema claro y sobrio, **motion sutil** (`dg-enter` / `dg-shake`, respeta `prefers-reduced-motion`).
- **Permisos** con `spatie/laravel-permission`; el seeder `RolesAndPermissionsSeeder` es **idempotente** (se puede correr siempre).

### Reglas de diseño (UI)
Tema **claro y sobrio**: naranjo de marca + blanco + neutros, **sin degradados**. **Reutiliza los componentes Blade** de `resources/views/components/`; no recrees botones/inputs/iconos inline.

**Colores — paleta ESTRICTA de 4** (siempre utilidades, nunca hex hardcodeado): **naranjo de marca (`brand-*`), negro/gris (`neutral-*`) y blanco**. **Prohibido** verde/ámbar/azul u otros matices; el **rojo (`red-*`) se usa SOLO para lo destructivo/negativo** (eliminar, errores de validación, `*` obligatorio, estado *devuelto*). El significado se transmite con **relleno, peso, tono y posición**, no con más colores:
- Marca: `brand-600` (#ea580c primario), `brand-700` (hover/énfasis fuerte), `brand-50`/`brand-100`/`brand-200`/`brand-300` (fondos, bordes y tintes suaves). Definidos en [resources/css/app.css](resources/css/app.css) (`@theme`); para cambiar el tono, edítalo **solo ahí**.
- Neutros: escala `neutral-*` (texto `neutral-900/700/600/500/400`, fondos `neutral-50/100`, bordes `neutral-200/300`, sólidos `neutral-800/900`).
- **Cómo mapear intención sin colores extra:** *producido / métrica-foco / “lo bueno”* → `brand` (naranjo, nuestra producción); *secundario / merma / de-enfatizado* → `neutral` atenuado (`neutral-500`, menor peso); *requiere acción / atención* → `brand`; *en reposo / final / “todo al día”* → `neutral`. Estados por **relleno**: badge *Enviado* = `brand-600` sólido (acción), *Aprobado* = `neutral-800` sólido (final), *Borrador* = `neutral` suave, *Devuelto* = `red` suave.
- Destructivo/error (única excepción de color): `red-600`/`red-500`/`red-50`.
- **Excepción sancionada (D-013):** los **squircles de ícono de las cards de accesos del Inicio** pueden usar la paleta pastel curada de 8 (`$paleta` en `resources/views/dashboard.blade.php` — celeste/verde/ámbar/violeta/turquesa/índigo + naranjo/gris) **solo ahí y solo si el usuario lo personaliza** (default sobrio brand/neutral, preferencia por perfil en `users.dashboard_colores`). El resto de la app sigue con la paleta de 4; el rojo NO es elegible.

**Iconos** — Heroicons estilo **outline**, como componentes Blade en `resources/views/components/icon/`, usados con `<x-icon.nombre />` (hoy: `pencil`, `trash`, `plus`):
- Formato exacto del SVG: `fill="none"`, `viewBox="0 0 24 24"`, `stroke-width="1.5"`, `stroke="currentColor"`, `aria-hidden="true"`. **Tamaño por defecto `h-5 w-5`, sobrescribible de verdad** (ver abajo): usa `class="h-4 w-4"` (idioma dentro de botones) **o** el prop `size="h-4 w-4"` estilo `x-avatar` — ambos ganan sobre el default, hacia abajo y hacia arriba.
- **Por qué necesita un mecanismo (gotcha):** `$attributes->merge(['class' => 'h-5 w-5'])` **CONCATENA**, así que un `class="h-4 w-4"` producía `h-5 w-5 h-4 w-4` y en el CSS compilado `.h-5` va después de `.h-4` → ganaba `h-5` y el override quedaba **inerte**. La solución vive en el componente: `@props(['size' => 'h-5 w-5'])` + una guarda `@php` que **descarta el default si el llamador ya puso un alto (`h-*`) en `class`**, de modo que nunca se concatenan dos tamaños (ver `pencil.blade.php`, la plantilla). No hace falta tocar los llamadores. Candado: `tests/Feature/IconoTamanoTest.php`. Detalle en la bitácora [2026-07-24].
- El color se **hereda** con `currentColor`: no pongas color en el SVG; contrólalo con `text-*` en el contenedor.
- Icono accionable (botón/enlace): agrega `title="..."` y un `<span class="sr-only">Acción</span>` para accesibilidad.
- Icono nuevo: copia uno existente (ej. `pencil.blade.php`, que ya trae el `@props`/`@php` del tamaño), pega el `path` de Heroicons *outline 24* y conserva el mismo formato.

**Botones** — usa los componentes, no los reconstruyas: `<x-primary-button>` (`bg-brand-600 hover:bg-brand-700`), `<x-secondary-button>` (borde `neutral-300`, fondo blanco), `<x-danger-button>` (`bg-red-600 hover:bg-red-500`). Todos: `rounded-lg`, `text-sm font-semibold`, `shadow-sm`, `transition duration-150`, foco con anillo de marca y `active:scale-[0.98]`. Icon-button de fila: `rounded-lg p-2 text-neutral-400 hover:bg-neutral-100 hover:text-neutral-700` (destructivo: `hover:bg-red-50 hover:text-red-600`).

**Formularios** — `<x-text-input>`, `<x-input-label>`, `<x-input-error>`, `<x-input-hint>`. Inputs: `rounded-lg border-neutral-300`, foco `focus:border-brand-500 focus:ring-2 focus:ring-brand-500/30`.

**Radios:** `rounded-lg` (botones/inputs/icon-buttons), `rounded-2xl` (tarjetas), `rounded-full` (badges/pills y avatares).

**Tipografía:** fuente **Instrument Sans** (en `@theme`). Títulos `text-xl font-semibold text-neutral-900`; encabezados de sección `text-xs font-medium uppercase tracking-wide text-neutral-500`; cuerpo `text-sm`; badges/ayudas `text-xs`.

**Tarjetas y listas:** contenedor `rounded-2xl border border-neutral-200 bg-white shadow-sm`; cabecera `border-b border-neutral-100 px-6 py-3`; filas con `divide-y divide-neutral-100`, cada una `flex items-center gap-4 px-6 py-4 hover:bg-neutral-50`. Avatares `h-10 w-10 rounded-full bg-neutral-100`. Para columnas alineadas a la derecha (badge + acciones) dar **ancho fijo** a esas columnas (ver commit `0c3f4fa`).

**Badges / pills:** `inline-flex rounded-full bg-brand-50 px-2.5 py-0.5 text-xs font-medium text-brand-700 ring-1 ring-inset ring-brand-100`.

**Filas de métricas** (listas con varias cifras por fila: producido/merma/tasas, 1ª/2ª/malos/dañadas, Δ): usar `<x-produccion.metrica>` — etiqueta + valor **pegados** (legible) dentro de un **cell de ancho fijo** (`w-*` = ancho del cell completo) con `tabular-nums`; el ancho fijo alinea las columnas entre filas y el sobrante queda como separación *entre* métricas. El `w` debe cubrir "etiqueta + valor máximo" (ej. Producido/Merma `w-28`, 1ª/2ª `w-16`, Malos `w-24`, Dañadas `w-28`). La **merma** se muestra `N (N%)` (unidades y, entre paréntesis, el % sobre el total) — nunca `N · N%` (confuso). Grupos de 4+ métricas con `flex-wrap` para no desbordar en móvil.

**Motion** (sutil, rápido, con propósito — **sin rebotes ni loops**): `transition duration-150` en estados/hover; `active:scale-[0.98]` al presionar; `dg-enter` para materializar contenedores al cargar; `dg-shake` para feedback de error; **siempre** respetar `prefers-reduced-motion` (ya cubierto en `app.css`).

**Dos canales de mensaje, no los mezcles.** `session('status')` = resultado de una acción propia (éxito, 419); lo pinta cada vista con `<x-status-alert>`. **`session('aviso')` está RESERVADA** al handler de 403/404 de `bootstrap/app.php` (D-014) y la pinta `<x-aviso>` **ya incluido en los layouts** — no la escribas desde un controlador ni agregues el componente a una vista: es lo que le explica al usuario por qué terminó en el Inicio. Para negar el acceso a una pantalla, gatea la RUTA (`middleware('permission:…')`) y oculta el enlace con `@can`; el handler se encarga del resto. Y recuerda: el 2º argumento de `abort(403, '…')` **es texto de cara al usuario** (se le muestra tal cual) — jamás datos de otro usuario.

**Errores: "señalar el control", no solo narrar.** Ante una acción bloqueada por una precondición, en vez de dejar solo un texto rojo lejano, **lleva la mirada al control exacto que falta**. Primitivo reutilizable: `window.dgDestacar(el, {ring})` y el magic Alpine `$destacar(el)` (en [resources/js/app.js](resources/js/app.js)) → hace `scrollIntoView` al elemento y reinicia sobre él la animación de feedback: `ring:true` usa la clase `.dg-destacado` (sacude **y** dibuja un anillo rojo breve, para un control concreto), `ring:false` reusa `.dg-shake` (solo sacude, para el texto de error). Respeta `prefers-reduced-motion` (scroll sin animar). Dos usos: (1) **client-side** en forms con estado Alpine (ej. el soplador en `mi-reporte.blade.php`): interceptar el `submit`, y si falta una precondición abrir su `<x-collapsible>` (`paneles.*`) + `$destacar($refs.grupoX)` **sin recargar** (el servidor valida igual como respaldo); (2) **global**: al cargar una página con errores de validación del servidor, un handler en `app.js` lleva y sacude el **primer** `[data-error-message]` visible (marca que emite `<x-input-error>`). Nota: `dg-shake` y `dg-destacado` no pueden ser dos clases separadas sobre el mismo elemento (ambas setean la shorthand `animation` y una pisaría a la otra) → `.dg-destacado` corre las dos animaciones en un solo `animation`.

**Pantallas de operario (módulo Mi producción y similares):** el operario usa el celular en planta, con apuro y a veces con guantes. Reglas: (1) mínima cantidad de elementos en pantalla — sin banda de cabecera de página; título + fecha compactos en una línea dentro del contenido; (2) todo lo autocompletable viene precargado (fecha, asignación, datos del usuario) — el operario **solo ingresa cantidades**; (3) las cantidades se ingresan **tocando**, no tipeando: usar `<x-stepper-input>` (botones −1/+1/+10, objetivos táctiles ≥48px); el input central queda como atajo para números grandes; (4) botones de acción a ancho completo y `h-12` en móvil; (5) los campos condicionales (ej. motivo de diferencia) solo aparecen cuando aplican, con sugerencias tocables (`datalist`) para minimizar tipeo; (6) los apartados con muchas opciones (máquina, tipo, motivos, observaciones) van en `<x-collapsible>` **colapsados por defecto**, con un resumen del valor elegido y chevron, para que la pantalla no crezca de más (las cantidades sí quedan siempre visibles).

**Ancho de página: DOS anchos y el ancho NO se declara en la vista (doctrina del dueño, 2026-07-25).** Antes había **7 anchos distintos** elegidos a dedo vista por vista (`xl`…`7xl`) y, como la banda del título estaba fija en `max-w-7xl` dentro del layout, **47 de las 69 pantallas con cabecera tenían el título desalineado del contenido** — hasta **352px** de desfase. Nadie lo reportó porque es un defecto **de escritorio**: a menos de 768px de viewport el desfase es cero, y solo llega a su máximo desde ~1544px.
- **Se declara UNA vez, en el layout:** `<x-app-layout ancho="listado|formulario">`. Los tokens los valida `App\View\Components\AppLayout::ANCHOS` y **un token desconocido revienta** (no cae en silencio al default: un `ancho="lista"` se ve idéntico al correcto en pantalla).
  - **`listado`** (1280 / `max-w-7xl`, **default**): listas y tablas de registros, informes, el tablero.
  - **`formulario`** (768 / `max-w-3xl`): formularios y fichas de **un** registro en una columna. También las pantallas de una sola tarea (operario, bandeja de aprobación, notificaciones) — en el celular el tope no llega a aplicarse, así que solo cambia su render en escritorio.
- **Prohibido** que una vista escriba su propio `mx-auto max-w-* px-*`. El layout emite los **tres** contenedores de página (banda del título, `<x-aviso>` y cuerpo) desde la **misma variable `$maxW`**, así que desalinearlos deja de ser posible por construcción — mismo criterio que `MenuPrincipal` para el menú. La vista solo trae su envoltorio de **padding vertical**.
- Las **clases literales** viven en `layouts/app.blade.php`, **no** en `AppLayout.php`: Tailwind v4 solo escanea `resources/**`, así que un `max-w-*` dentro de `app/` se purgaría del bundle y la página perdería el tope.
- **El tope de 1280px se queda**: en un monitor ancho deja ~500px vacíos a cada lado y eso es deliberado (una línea muy larga se lee peor). No se ensancha para «aprovechar la pantalla».
- Sí es correcto un `max-w-*` **interno** dentro de una vista (una maqueta, una tarjeta angosta): lo que está prohibido es el contenedor **de página**. Los candados están en `AnchoDePaginaTest` y distinguen ambos casos por el `px-` (los de página lo llevan).

**Marco horizontal: DOS capas como máximo en móvil (pedido del dueño, 2026-07-28).** El hermano del ancho de página, pero al revés: no sobra pantalla, sobra **padding**. Cada tarjeta anidada cobraba el suyo y se sumaban. Medido a 375px en el formulario del QR por cantidad: el campo «Equipo» tenía **217px de ancho útil sobre 375** — la cadena era `12 (input) + 12 (tarjeta de máquina) + 16 (tarjeta de sección) + 24 (tarjeta blanca) + 24 (marco de página)` = **158px, el 42% de la pantalla**, en gran parte invisible: una tarjeta blanca dentro de otra tarjeta blanca no muestra su borde pero sí cobra su padding.
- **El marco de página se declara UNA vez, en el layout** (`px-3 sm:px-6` en `guest`, `px-4 sm:px-6 lg:px-8` en `app`), y **la vista no lo repite** — misma regla que el ancho.
- **Un grupo de campos dentro de una pantalla va en `<x-seccion titulo="…">`, no en un `<div class="rounded-2xl border … p-4">` a mano.** El componente **no dibuja marco en móvil** (es solo un grupo: `space-y-4`) y recupera la tarjeta desde `sm:`. Así el aire de escritorio se conserva sin cobrárselo al celular.
- **Sí conserva su borde** la tarjeta que es una **unidad real** y no un adorno: cada máquina del acordeón, una fila de listado, un panel flotante. La prueba para decidir: si al quitarle el borde se pierde información (dónde empieza y termina *esta* máquina), el borde se queda; si solo se pierde decoración, se va en móvil.
- Resultado de aplicar la regla al mismo campo medido: **217px → 295px (+36%)**, con la cadena en `12+10+16+12 = 50px`. Escritorio **idéntico** (verificado: borde 1px, fondo blanco, `padding:16px`, radio 16px a 1024px).
- **Ninguna tarjeta cobra en móvil el padding pensado para escritorio.** El barrido del 28-07 tocó **92 tarjetas en 64 vistas**: todas declaraban un padding único (`p-4` a `p-8`) que el celular pagaba igual que el monitor — el patrón más repetido era `p-6 sm:p-8`, o sea **24px por lado en un teléfono**. Escala adoptada: **32/24 → 16 · 20 → 16 · 16 → 12**, con el valor grande siempre detrás de `sm:`. En las pantallas internas el marco de página ya aporta 16px, así que la tarjeta no necesita repetirlo.
- En `app-layout` la tarjeta **conserva** su borde y su fondo blanco incluso en móvil: ahí la página es `bg-neutral-50`, así que el marco se ve y separa. Lo que se recorta es el padding, no el marco. Quitarlo solo corresponde cuando es **blanco sobre blanco** (el caso del QR).
- Los **correos** quedan fuera de la regla a propósito: van con tablas y padding fijo porque los clientes de correo no entienden media queries.
- Candados en `MarcoHorizontalTest` (4): el marco de página se declara solo en el layout, las vistas del QR no vuelven a la tarjeta a mano, `<x-seccion>` mantiene todo su marco detrás de `sm:` (mutado: al quitarle los prefijos, falla), y **ninguna vista declara un padding de tarjeta sin su variante `sm:`** — este último recorre las 64 vistas y nombra archivo:línea de cada infractor, porque es un defecto que revisando en pantalla grande no se ve.

**«Volver»: UN solo botón, siempre igual (doctrina del dueño, 2026-07-24).** Antes convivían **13 familias** distintas de volver (79 controles): la misma flecha a la izquierda del título en 5 vistas y escrita a mano a la **derecha** en 26, una X rotulada "Cancelar" arriba, un "Cancelar" de texto al pie del formulario, y enlaces tenues escondidos **después de la paginación**. Reglas, sin excepciones de estilo:
- **Forma:** `<x-volver>` y nada más. Flecha + el literal **"Volver"**; el destino va en el `title` (`titulo`/`backTitle`), **nunca** en el texto visible. `min-h-12` (48px táctil). No tiene props de diseño: no admite variantes.
- **Posición:** **arriba a la izquierda, pegado al título** — vía `:back` del `<x-page-header>`. La derecha (slot `action`) es de las acciones (Guardar/Editar/Crear): salir no va pegado a confirmar. En las pantallas de operario, que omiten la banda de cabecera, el componente va suelto en el cuerpo **antes** del título.
- **Cuándo va:** solo en pantallas que **cuelgan de un listado**. Un listado que es **ítem del menú NO lleva Volver** (se llega por la sidebar; no tiene pantalla padre) — lo vigila `VolverTest::test_ningun_item_del_menu_lleva_volver`, derivado de `MenuPrincipal`. Dos excepciones documentadas: los **listados huérfanos** (Máquinas, Tipos de botellón, Conductores — aún fuera del menú, P-NAV-06) conservan el suyo porque es su única salida; y un **formulario** que además es ítem del menú lo conserva (`lote/create`, con destino calculado por permiso).
- **Formularios: una sola salida.** El Volver reemplazó a la X y al "Cancelar". `form-actions` y `form-footer` ya **no** aceptan `cancel`/`back`.
- **Comportamiento:** el `href` al padre es el destino **garantizado**. Un handler delegado en `app.js` (`a[data-dg-volver]`) usa `history.back()` **solo** si el `pathname` del `referrer` coincide con el del `href`, para devolver scroll y estado del listado (el mes abierto de la agenda). Si llegaste desde otra parte, navega al padre igual. Prohibido volver a poner `onclick="…history.back()…"` en una vista.
- **No es "volver":** las flechas de **paginación/carrusel** (mes anterior, scroll de solicitudes), los **reset de filtro** ("← Todos los años", "Volver a los últimos 45 días") y los **cerradores de overlay** (X del lightbox, "Cerrar menú"). Usan la misma flecha y **no se tocan**. Las públicas del QR (`publico/taller/*`, sin menú ni sesión) tienen su propio botón grande "Volver al inicio" y quedan **fuera** de esta doctrina.
- El candado `VolverTest::test_no_quedan_formas_viejas_de_volver` barre los fuentes Blade y falla si reaparece cualquier idioma viejo.

**Paneles flotantes: la posición se MIDE, no se declara (doctrina del dueño, 2026-07-26).** Un panel anclado con `start-0`/`end-0` apuesta a que de ese lado sobra espacio, y la apuesta se hace al escribir la vista — antes de saber dónde va a caer el disparador y en qué ancho de pantalla se va a mirar. Cuando falla, el panel se sale y su contenido es **inalcanzable** (la campanita perdía 72px por la izquierda; los globos ⓘ se recortaban en móvil, bitácora [2026-07-01]).
- **El mecanismo es `x-dg-anclar`** (directiva Alpine en `resources/js/app.js`), y lo traen puestos `<x-dropdown>` y `<x-info-tip>`. Al abrirse, el panel **mide y decide**: nace hacia el lado con más espacio libre; si aun así se sale, se corre hacia adentro; si no cabe a lo alto, voltea sobre el disparador o scrollea por dentro. Se abstiene si el panel es `position: fixed` (ya está anclado al viewport).
- **Prohibido** armar a mano un panel `absolute` con **ancho fijo** + un borde horizontal a dedo. Usá los dos componentes. Sí son correctos los paneles que **no eligen lado**: `w-full` (autocompletados, heredan el ancho del input) e `inset-x-0` (swatches del Inicio).
- **`align`/`direction` de `<x-dropdown>` son PREFERENCIA**, no corrección: fijan de qué lado nace y desde qué esquina crece la animación. Que estén "mal" ya no puede romper nada. `<x-info-tip>` **no tiene** `align`: se le quitó junto con sus 14 usos escritos a mano, porque es exactamente el knob que no se puede acertar.
- **El ancho del menú es un token validado** (`w-48` · `w-56` · `w-80` en el `@php` del componente) y **uno desconocido revienta**. Antes un `width="56"` emitía la clase inválida `56` y el panel se quedaba **sin ancho, en silencio**.
- **Al mover un panel en JS, escribí `left`/`top`, nunca `translate`/`transform`:** Tailwind v4 compila `scale-95` y `translate-y-1` a las propiedades independientes `scale`/`translate`, que el `x-transition` de estos componentes usa; un estilo inline las apaga. Y si fijás `top`, **descontá el margen** (`mt-2`): ya venía dentro de `offsetTop` y el navegador lo vuelve a sumar.
- **Ojo con el apilamiento:** un panel que se corre cruza sobre `<main>`. La sidebar es `sticky`, y `sticky` crea contexto de apilamiento **aunque el z-index sea `auto`** — por eso lleva `z-40` también en `lg:`. Candados en `PanelAncladoTest` (8, mutados).

### Responsive (obligatorio en cada cambio)
La app es **mobile-first**. Toda pantalla y componente debe verse bien y **sin scroll horizontal** a **375px (móvil), 768px (tablet) y ≥1024px (desktop)**.
- Las clases base son para móvil; se escala con prefijos `sm:` / `md:` / `lg:`, nunca al revés.
- **Prohibido** un ancho fijo (`w-28`, `w-64`, …) sin fallback responsive; en móvil usar `w-full` o reflow.
- Filas/tablas con varias columnas deben **reflowar o apilar** en móvil (usa `<x-list-row>`), no desbordar.
- **Cada cambio se entrega ya responsive y verificado** a esos 3 anchos.
- Checklist antes de cerrar: ¿se ve bien a 375/768/1024? ¿sin scroll horizontal? ¿los botones/enlaces son cómodos al tocar en móvil?

### Reutilizar antes de crear (DRY)
**Antes de escribir UI nueva**, revisa el catálogo de abajo y **reutiliza**. Si un patrón se repite (o se va a repetir), **extrae un componente** en `resources/views/components/` en vez de copiar markup. **Prohibido** inline-stylear a mano botones, inputs, selects, checkboxes, badges, íconos o filas que ya tienen componente. Los elementos de uso constante SIEMPRE se componentizan.

#### Catálogo de componentes (`<x-...>`)
- **Layout / página:** `app-layout` (**prop `ancho="listado|formulario"`** — pone el ancho de la banda del título, del aviso y del cuerpo de una sola vez; ver «Ancho de página» en Reglas de diseño), `guest-layout`, `page-header` (props `title`, `subtitle`, **`back`** = URL de la pantalla padre y **`backTitle`** = destino para el tooltip; + slot `action` para las acciones de la derecha), `list-card` (tarjeta + cabecera + `<ul>`), `list-row` (fila **responsive** con slots `leading`/`meta`/`actions`), `avatar`, `modal`, **`dropdown`** / `dropdown-link` (menú anclado; **la posición la mide `x-dg-anclar`** — ver «Paneles flotantes» en Reglas de diseño; props `width` = token validado `w-48|w-56|w-80` (uno desconocido **revienta**), `align`/`direction` = solo preferencia; cierra con click afuera y con **Escape**), `collapsible` (sección colapsable con cabecera tocable + chevron que rota; props `label`/`model`; `model` es una propiedad **booleana del `x-data` del contenedor** —ej. `paneles.maquina`— así se auto-colapsa al elegir y se abre ante errores; slot por defecto = cuerpo, slot `summary` = resumen mostrado solo colapsado).
- **Navegación (sidebar V4 «menú Talana»):** el menú es DATOS en `app/Support/MenuPrincipal.php` — fuente única: módulos/ítems con `label`, `route`, patrones `activo` (routeIs), `permiso` spatie (`|` = canAny), `icon` y badge declarativo (`badge` + `badge_title`); candados en `MenuPrincipalTest` (route/permiso/ícono/patrón existen). Render: `layout.sidebar` (UN solo `<aside>`: fijo 264px desde `lg:` 1024px; drawer izquierdo 300px con hamburguesa bajo eso — `menuAbierto` vive en el `x-data` del shell `layouts/app.blade.php`) + `layout.topbar` (título del módulo activo + campanita + avatar; la campana móvil va SIEMPRE visible en la barra). Acordeón por módulo = `sidebar-group` (details/summary nativo, chevron `group-open:rotate-90`; el módulo activo llega `open` del servidor) e ítems = `sidebar-item` (`aria-current="page"` contiguo al href, para tests). **Agregar un ítem al menú = UNA línea de datos en MenuPrincipal** (la visibilidad del módulo se deriva sola de sus ítems). **Doctrina de badges (dueño, 24-07): un badge del menú es una ACCIÓN pendiente del usuario anclada al ítem exacto donde se resuelve** (la categoría cerrada muestra la suma y al abrirla se ve el ítem); los estados/inventarios (ej. equipos activos del taller) viven en las pantallas y el Inicio, nunca como badge del menú.
- **Botones / enlaces:** `primary-button`, `secondary-button`, `danger-button`, `button-link` (enlace con estilo primario), `icon-button` (`:href`, `variant=default|danger|primary|secondary|brand`, `size=sm|md|lg` — `lg` = 48px táctil, `label` → `sr-only`), `secondary-link` (enlace tenue de texto: "ver además", filtros, limpiar — **NO** para volver, eso es `volver`), **`volver`** (el ÚNICO botón de volver; props `href` + `titulo` — ver «Volver» en Reglas de diseño).
- **Form controls:** `input-label`, `text-input`, `select`, `textarea`, `checkbox`, `radio`, `checkbox-item` (checkbox + label en tarjeta; slot `note`), `input-error`, `input-hint`, `form-footer` (contenedor del submit al pie, alineado a la derecha; **sin** "Cancelar"), `form-actions` (el submit como ícono para el slot `action` del page-header; props `form`/`submitLabel`, **sin** cancelar/volver), `stepper-input` (cantidad para operarios: botones grandes −1/+1/+10 sin tipear; requiere `x-data` en el contenedor; props `name`/`label`/`hint`/`:value`/`:steps`), `chip-radio` (radio táctil estilo chip ≥48px para operarios; props `name`/`value`/`label`/`:checked`; los atributos extra como `x-model` van al input), `reason-chips` (selector de motivo táctil construido **sobre `chip-radio`**: grilla de chips desde un array de opciones, con chip "Otro" opcional que revela un texto libre; props `name`/`:options`/`:selected`/`label`/`:allowOther`/`:cols`; el "Otro" viaja como centinela `__otro__` y el texto en `{name}_otro`).
- **Feedback:** `badge` (`variant=brand|neutral|danger`), `status-alert` (`:status`), **`aviso`** (mini-notificación de BLOQUEO; se renderiza **ya** una sola vez en `layouts/app` y `layouts/guest`, **no la pongas en tus vistas**), `errors.shell` (shell **standalone** de las 10 páginas de `resources/views/errors/`: DOCTYPE + CSS inline + logo; props `titulo`, slot. NO usa layout, `@vite` ni `Auth` a propósito — ver la bitácora [2026-07-24]), `auth-session-status`, `stat-card` (indicador clickeable del Inicio: `label`/`valor`/`href`/`alerta`), `produccion.estado-badge` (`:estado` del reporte), `produccion.metrica` (etiqueta + valor de **ancho fijo** `text-right tabular-nums` para columnas alineadas entre filas; props `label`/`w`/`tone`).
- **Íconos:** `icon.pencil`, `icon.trash`, `icon.plus`, `icon.chevron-down`, `icon.chevron-right`, `icon.information-circle`, `icon.home`, `icon.shopping-cart`, `icon.building-office-2`, `icon.user`, `icon.bars-3` (agrega nuevos como Heroicons outline; ver Reglas de diseño).
- **Ayuda contextual:** `info-tip` (ⓘ que revela un globo con el slot como explicación; abre al **tocar** en móvil y al **hover** en desktop —`matchMedia('(hover:hover)')`—, cierra con click-afuera/Esc; globo viewport-safe; **sin props** — el lado lo elige `x-dg-anclar` midiendo, ver «Paneles flotantes» en Reglas de diseño). Reutilizable en cualquier tarjeta; en esquina va dentro de un `<span class="absolute right-2 top-2">` sobre un contenedor `relative` (si la tarjeta es un `<a>`, la ⓘ va como **hermano** superpuesto, no anidada).
- **Inicio:** `dashboard.acceso` (card de acceso del zócalo del dashboard: squircle de ícono + label; props `:item`/`:paleta`; el color del squircle es personalizable por usuario vía el modo «Personalizar» — `dgTiles` en `app.js`, endpoint `dashboard.colores.update`, D-013).

Si algo no existe y se va a usar seguido, créalo siguiendo el formato de los componentes existentes.

### Entorno local
- Todo junto: `composer dev` (levanta `serve` + `queue` + `pail` logs + `vite` a la vez).
- O por separado: `php artisan serve` y `npm run dev`.
- BD local de desarrollo: **SQLite**.

### Frontend / assets ⚠️ (gotcha importante)
- Tras tocar **Blade, CSS o JS** → corre **`npm run build`** y **commitea `public/build/` junto con el cambio**.
- `public/build` **está versionado** (no está en `.gitignore`) y el **servidor no tiene Node**.
  Si no recompilas y commiteas, en producción el CSS/JS queda **viejo**. Además Tailwind v4 **purga**
  las clases no usadas, así que cualquier **clase nueva** necesita el rebuild para existir en el bundle.

### Tests
- `composer test` (o `php artisan test`). Framework: **PHPUnit** (no Pest). BD de test: SQLite en memoria.
- Tests en `tests/Feature/` y `tests/Unit/`.

### Deploy
- **`git push origin main` = despliegue automático.** GitHub Actions (`.github/workflows/deploy.yml`)
  entra por **SSH** al servidor y corre **`deploy.sh`** en `/home4/impdali/daligo`.
- `deploy.sh` ya hace, en orden: descartar `public/.htaccess` → `git pull --ff-only` →
  `composer install --no-dev` → `migrate --force` → `db:seed --force` (DatabaseSeeder: roles/permisos + sucursales) →
  `storage:link` → `config:cache` + `route:cache` + `view:cache` → `permission:cache-reset`.
  **No corras seeds/cachés a mano en producción**; ya están cubiertos.
- Mirar el avance en la pestaña **Actions** del repo `DaliGo-2013/DaliGo`. Staging: **staging.impdali.cl**.
- Servidor: HostGator compartido (cPanel + LiteSpeed), PHP **ea-php83**, BD **MySQL 5.7** (`impdali_daligo`), **sin Node**.
- **Cron del scheduler** (one-time en cPanel/SSH, no lo crea el deploy): `*/15 * * * * /opt/cpanel/ea-php83/root/usr/bin/php /home4/impdali/daligo/artisan schedule:run >> /dev/null 2>&1` (+ una línea gemela `*/15` de `queue:work --stop-when-empty --max-time=840`). **NO es `* * * * *`**: HostGator reescribe los crons <15 min (I-01, ver bitácora [2026-07-07]) → grilla `*/15` que dispara :00/:15/:30/:45, y TODA tarea de `routes/console.php` debe caer EXACTO en esos minutos (`hourly()`/`hourlyAt(15|30|45)`) o no correrá jamás. Latencia de cola/notificaciones ≤15 min, aceptada.

---

## Cómo registrar un error en la bitácora

Copia esta plantilla y pégala **al inicio** de la sección Bitácora (las entradas más recientes van arriba):

```markdown
### [AAAA-MM-DD] Título corto del problema
- **Síntoma:** qué se vio (mensaje de error textual si aplica).
- **Causa:** la causa raíz.
- **Solución:** qué se hizo (comando / archivo / commit) que SÍ funcionó.
- **Evitar a futuro:** (opcional) cómo no repetirlo.
```

---

## Bitácora de errores y soluciones

> Las entradas más recientes van arriba. Sembrada con los problemas ya resueltos del proyecto.

### [2026-08-10] Commitear por hunks con OTRA sesión editando el mismo archivo: la suite que corriste no probó lo que estás commiteando
- **Síntoma:** dos sesiones de Claude Code trabajando a la vez en `C:\Users\marco\DaliGo` (árbol compartido, sin worktrees). Al ir a commitear el simulador, `git status` mostraba **23 archivos modificados que no eran míos** — incluido `admin/carga/index.blade.php`, el mismo archivo que yo estaba tocando, y `CLAUDE.md` con la entrada de bitácora de la otra sesión. Un `git add` de ese archivo se habría llevado su trabajo a medio hacer.
- **Causa raíz de lo peligroso, que no es el `add`:** después de stagear **solo mis hunks**, lo que quedó en el índice **no existía en ningún lado** — ni en HEAD, ni en el árbol de trabajo. Todas las verificaciones (suite verde, navegador) habían corrido sobre el árbol, que es HEAD + lo mío + lo de ellos. Es decir: **iba a commitear código que nunca ejecuté.** Y hay una trampa concreta esperando ahí: al descartar hunks se pueden dejar etiquetas Blade desbalanceadas si un hunk agrega un `<div>` y otro, descartado, cierra el suyo.
- **Solución (el flujo completo, reutilizable):** (1) `git add` **por pathspec**, nunca `-A`; (2) para un archivo con hunks mezclados, `git diff -U3 -- archivo > full.patch`, filtrar los hunks propios con un script (numerándolos por `^@@`) y `git apply --cached mio.patch`; (3) para un archivo donde el hunk propio y el ajeno son **adyacentes** (dos entradas nuevas de bitácora seguidas), construir el blob a mano: `git show HEAD:archivo` + insertar lo propio → `git hash-object -w` → `git update-index --cacheinfo 100644,<blob>,archivo`; (4) **y después verificar lo staged en un worktree aparte**: `git worktree add --detach $TMP/verify HEAD`, `git diff --cached > p.patch`, `git apply p.patch` ahí adentro, **`cp -r` de `vendor`** (está en .gitignore; 13 s en SSD) y correr la suite completa — así se prueba el commit exacto y no el árbol.
- **NO junctionar `vendor` en el worktree (`mklink /J`): rompe el aislamiento en silencio y te hace acusar a otro.** PHP resuelve `__DIR__` **al realpath**, así que `$vendorDir = dirname(__DIR__)` dentro del junction devuelve la carpeta ORIGINAL y `$baseDir` termina siendo el árbol principal: el autoloader carga `App\*` y `Database\Seeders\*` **desde el árbol viejo** mientras los tests, las vistas y la config salen del worktree. Un Frankenstein que corre y da números creíbles. Acá dio **32 fallos** que atribuí a otra sesión —«pushearon M11 roto»— y eran míos: con `cp -r` de vendor, los mismos tests dan **176 verdes**. Diagnóstico en una línea: `php -r 'require "vendor/autoload.php"; echo (new ReflectionClass("App\\Models\\Maquina"))->getFileName();'` — si imprime la ruta del árbol principal, todo lo que midas es mentira. Y el contraste barato que lo destapa: **si CI está verde y vos en rojo, sospechá del entorno antes que del código ajeno.**
- **Y el `public/build`:** NO se commitea cuando hay trabajo ajeno sin commitear. El rebuild toma de Tailwind lo que hay en el árbol, así que mi CSS **borraba** `lg:col-span-2` y `lg:col-span-4` (que ellos estaban migrando a `xl:`) y sumaba un `gap-x-5` que era de ellos. La comprobación que reemplaza al build: `tr '}' '\n' < css | sort` en el bundle commiteado y en el nuevo, y `comm` — si lo único que AGREGA es de otro, tu cambio no necesita clases nuevas y el bundle de HEAD alcanza. (Si sí las necesita, hay que sacarlas del markup o esperar a que el otro commitee: acá se cambió un `col-span-2` por reacomodar el campo en la grilla que ya existía.)
- **Evitar a futuro:** antes de tocar nada, `git status` y `git log --oneline -3`; si aparece trabajo ajeno, **no usar `git stash`** (ya costó perder el archivo de otra sesión el 10-08) y asumir desde el arranque que el commit va por hunks y se verifica en worktree. Lo barato es abrir el propio `git worktree` al empezar.

### [2026-08-10] `validate()` NO conserva el orden de un array de líneas: una línea a la que le falta una clave se adelanta, y la lista de resultados desalineada reventaba el visor
- **Síntoma:** dos defectos que salieron del mismo lugar mientras se escribía un candado. (1) En el simulador de carga, con la primera línea "a medida" y la segunda del catálogo, la carga se calculaba **al revés del orden escrito** — con «Como armé la lista» eso decide qué producto va al FONDO del camión, y además cambia la letra y el color de cada bloque en el lienzo. (2) Con una línea vacía en el medio, la pantalla devolvía **500: `Undefined array key` en `SimuladorCargaController::escena()`**.
- **Causa:** `$request->validate()` arma el array devuelto **recorriendo REGLA por regla**, no dato por dato. Con `lineas.*.tipo` como primera regla del grupo, la primera línea que se crea en el resultado es la primera que TIENE `tipo` — una línea sin esa clave (un bulto a medida no manda `tipo`) queda detrás aunque se haya enviado antes. `array_values()` después reindexa por ese orden y lo deja fijo. El 500 es un primo hermano: la lista de resultados se armaba con `$filas[] =` (reindexada 0..n-1) mientras los bloques del motor viajan con el índice ORIGINAL de la línea; una línea descartada por no tener producto corría todas las de abajo un lugar, y `escena()` —que resuelve el nombre de cada bloque con `$mixta['lineas'][$b['linea']]`— pedía una clave que ya no existía.
- **Solución:** `ksort($datos['lineas'])` justo después de validar (las claves SON los índices del formulario, así que ordenar por clave devuelve el orden que armó el usuario) y `$filas[$i] =` en vez de `$filas[] =`, para que la lista quede indexada por línea. Con eso los cuatro lugares que responden «¿de quién es este bloque?» —el nombre en el lienzo, la letra, el color y el Excel— quedan alineados **por construcción** y no por coincidencia. Candados mutados: `test_las_lineas_se_calculan_en_el_orden_en_que_se_enviaron` y `test_una_linea_descartada_no_le_corre_la_letra_a_las_de_abajo`.
- **Evitar a futuro:** (1) **nunca confiar en el orden de un array anidado que salió de `validate()`** — si el orden significa algo (prioridad, secuencia, posición física), reordenar explícitamente por la clave; (2) el primer candado que escribí NO reproducía el bug (las dos líneas traían `tipo` y solo cambiaba `estiba`, que es una regla posterior): para mutar esto hay que **omitir la PRIMERA clave del grupo de reglas**, no una cualquiera; (3) cuando dos estructuras se referencian por índice (acá lista de resultados ↔ bloques del motor), **indexar la lista con la clave del emisor** en vez de reindexarla — el `[]` es cómodo y es exactamente lo que las desalinea en cuanto un elemento se descarta.

### [2026-08-10] Comillas dobles dentro de un `x-data` inline matan TODO el Alpine de la pantalla (y el pane oculto no dispara rAF)
- **Síntoma:** (cazado en el gate E2E de P-M11-20, antes de llegar a prod) en mi-reporte NINGÚN control Alpine respondía: colapsables muertos, chips sin estado, cola offline sin contador; consola llena de `ReferenceError: paneles is not defined` en cascada sobre TODOS los bindings hijos del componente.
- **Causa:** un comentario JS dentro del objeto `x-data="{...}"` contenía `"HH:MM"` — las **comillas dobles cortan el atributo HTML** (el atributo se delimita con `"`), el resto del objeto queda fuera y Alpine recibe un x-data truncado → el componente entero no evalúa y cada binding descendiente revienta con ReferenceError. Blade no escapa el markup literal del template. Segundo escollo del mismo gate: verificando en el Browser pane, `x-show` + `x-transition` parecía roto también en código VIEJO de producción — era el **pane con `document.hidden=true`**: Chrome pausa `requestAnimationFrame` en tabs ocultos y las transiciones de Alpine (que usan rAF) nunca arrancan; el estado (`:class`, `aria-expanded`) SÍ reaccionaba. Diagnóstico diferencial: una sonda `x-show` SIN transition funcionaba; con transition, no.
- **Solución:** quitar las comillas dobles del comentario (commit `1aaff9d`, con nota OJO dentro del propio x-data); para la verificación visual en pane oculto, forzar `style.display=''` y medir geometría (no necesita rAF) y validar la interactividad por los efectos que no dependen de rAF (`:class`/`aria-expanded`/`x-text`).
- **Evitar a futuro:** (1) dentro de un `x-data`/`x-on` inline JAMÁS comillas dobles — ni en comentarios ni en strings (usar simples); el síntoma-firma es la cascada de `ReferenceError: <prop> is not defined` en todos los bindings del componente. (2) En el Browser pane, si `x-show`+`x-transition` no muestra pero `:class` sí reacciona, revisar `document.visibilityState` ANTES de diagnosticar el código: rAF pausado = transición que nunca corre. (3) El mismo gate cazó que toda guarda de precondición de la TANDA necesita su gemela en la PARADA: una parada sin máquina se encolaba offline y el drenado la perdía en silencio (422 permanente; la cola de tandas no tiene UI de rechazadas).

### [2026-08-07] La pantalla prometía una mano de obra que el guardado bajaba a $0: se sembraba Alpine con la COLUMNA cuando el servidor la DERIVA
- **Síntoma:** en «Cotización» de servicio técnico, una orden cuyo trabajo no tiene tiempo estándar mostraba la mano de obra **vieja** —con la nota ámbar «el trabajo … no tiene tiempo estándar» al lado, o incluso «1,5 h × **—**»— y un «Costo total a pagar» que la incluía; al **Guardar**, la mano de obra pasaba a **$0** en silencio y el total caía. Nadie lo reportó: el número mostrado era *plausible* porque es el que estaba en la base.
- **Causa:** **dos fuentes para el mismo número.** La vista sembraba el componente Alpine con la **columna guardada** (`manoObra: {{ old('mano_obra', $orden->mano_obra ?? 0) }}`) mientras `guardarCotizacion()`/`guardarReparacion()` la **recalculan siempre** desde el catálogo (`manoObraDe()` = `horas × valor hora`, y **0** si `TiempoReparacion::horasDe()` da null o si el SKU de la hora no tiene precio en la lista oficial). Mientras las dos coinciden no se nota; divergen en cuanto jefatura **desactiva** un trabajo del catálogo (`horasDe()` filtra por `activo`) o el SKU de la hora pierde su precio — o sea **sin datos legados: basta un `activo = false`**. El defecto existía desde que la mano de obra pasó a ser fija por catálogo (`cd2a9d7`), pero se volvió cobrable cuando «Enviar cotización» empezó a guardar antes de mandar.
- **Solución:** el controlador pasa a la vista `manoObraVigente` (= `manoObraDe($orden->trabajo_realizado)`, **lo que va a quedar al guardar**) y la vista siembra Alpine con eso, no con la columna. Ninguna pantalla vuelve a prometer un total que su propio Guardar va a bajar. Además la nota ahora distingue los **dos** huecos (sin tiempo estándar / sin precio de la hora) en vez de dejar un «× —» junto a un monto que ya no se sostiene.
- **El candado que faltaba no es de pantalla, es de salida:** que la vista sea honesta no evita que se mande al cliente una cotización **sin** mano de obra (cobro de menos silencioso). `faltaManoObra()` bloquea el **ENVÍO** —y solo el envío: Guardar sigue libre para que el técnico ponga precios de repuestos mientras jefatura carga el tiempo que falta— con un motivo por hueco, espejado en la lista `$faltas` de la vista.
- **Gotcha dentro del gotcha:** el candado se pone sobre **«el catálogo no PUEDE calcularla»**, NO sobre «el monto es $0». `TiempoReparacion` valida `horas` con `min:0`, así que **0 h es una decisión legítima de jefatura** («este trabajo no se cobra») y esa cotización sí se envía. Probado con mutación: la versión que bloquea por monto pasa los dos tests de bloqueo y **rompe** `test_cero_horas_fijadas_por_jefatura_si_se_puede_enviar` — esa asimetría es lo que justifica ese test.
- **Evitar a futuro:** (1) si el servidor **deriva** un campo al guardar, la pantalla se siembra con **la derivación**, nunca con la columna — la columna es el resultado de la *última* vez que se derivó, y su valor pasa a ser una afirmación falsa en cuanto cambia una de las entradas del cálculo; (2) un `old('campo', …)` sobre un input que **ya no existe** (aquí se quitó al volverse solo-lectura) es la pista de que la siembra quedó huérfana de su formulario; (3) cuando la fuente de un cálculo es un catálogo con `activo`, «la fila existe» y «la fila cuenta» son cosas distintas: todo consumidor de `horasDe()`-style hereda el caso null aunque su BD esté bien sembrada; (4) para un candado que dispara con un valor de borde, mutar hacia **la implementación tentadora y equivocada** (aquí: comparar el monto) es lo que demuestra que el test fija la regla y no solo el resultado.

### [2026-08-07] El simulador prometía 25% menos de lo que entra, y la culpa era de 4 cm: en una rejilla exacta, «casi bien» no existe
- **Síntoma:** el dueño marcó el hueco que quedaba arriba de la carga del HD35 y pidió llenarlo, con su número de terreno: *«acostados… 480 en total camión completo»*. El cálculo daba **360**. La sospecha natural era el motor (¿le falta apilar? ¿mezclar estibas? ¿anidar en tresbolillo?) y las tres habrían sido features grandes.
- **Causa: el ANCHO del catálogo, 200 cm en vez de 204.** Una bolsa acostada ocupa 51 cm de ancho: con 200 entran **3** a lo ancho (153, sobran 47) y con 204 entran **4** (204 justos). **Cuatro centímetros valían 120 botellones**, porque hacen entrar una columna entera. En un motor de rejilla exacta el error no es proporcional al de la medida: es escalonado, y un dato «casi bien» cae del lado de abajo del escalón sin avisar.
- **Solución — el número se DERIVÓ de sus cupos, no de una ficha.** Fuerza bruta sobre todas las cajas enteras que reproducen **a la vez** sus dos números de terreno (420 de pie y 480 acostado): el ancho queda acotado a **204–207**, y largo y alto del catálogo caen **dentro** de su rango — o sea que el único dato fuera de lugar era el ancho. Se tomó el extremo bajo (204): todo el rango da los mismos cupos de botellones y es el que menos promete para los demás productos. Es el mismo método con el que se habían verificado los otros dos camiones.
- **Lo que volvió creíble la corrección:** un ancho elegido para arreglar UN número habría roto el otro. Con 204 salen los dos, y el candado los fija juntos — mutado en los dos sentidos: con 200 se pone rojo el acostado (360 ≠ 480), con 208 se pone rojo el de pie (480 ≠ 420, entraría una octava columna parada). Ese «se rompe por los dos lados» es lo que distingue una medida de un ajuste a medida.
- **La ficha de fábrica NO servía, y parecía servir.** El dueño pasó la hoja del HD35: 1,76 de ancho, 2,20 de alto, 3,4–3,7 «carrozables». **Ninguno es el interior del furgón**: el 1,76 es el ancho de la CABINA (y el furgón carrozado la desborda por los costados — lo dice la propia doc del visor, moldeada sobre sus fotos), el 2,20 es alto de cabina y los 3,4–3,7 son largo de bastidor. Un furgón es carrozado aparte: su interior no está en la ficha del chasis.
- **Evitar a futuro:** (1) ante un cupo que no cuadra con el terreno, **sospechar primero de las MEDIDAS y recién después del algoritmo** — es más barato de comprobar y, en este proyecto, fue la causa; (2) para validar una medida dudosa, **buscar el rango de valores compatible con TODOS los números de referencia** en vez de ajustar contra uno solo: si un solo dato de referencia se puede satisfacer de muchas formas, dos lo acotan a un rango angosto y tres lo clavan; (3) en cualquier motor de rejilla entera, una medida con 2% de error puede dar 25% de diferencia en el resultado — las medidas de caja se miden, no se estiman; (4) para un vehículo **carrozado**, la ficha del fabricante describe el chasis y la cabina, nunca la caja.

### [2026-08-04] Refactor sin red: cómo se toca el código que genera el Excel de la gerencia sin poder «mirar si se ve bien»
- **Síntoma:** no hubo error — hubo una **deuda que nadie se atrevía a pagar**. El esqueleto OOXML (`contentTypes`, los dos `.rels`, `workbook`, `filaCeldas`, `letra` y el armado del ZIP) estaba **copiado igual** en `CartaGanttExcel` y `FlotaExcel`, y la cabecera de la segunda lo anotaba explícitamente: *«no se hizo ahora para no meter un refactor en la clase que genera el Excel que la gerencia usa en reuniones»*. El miedo era razonable: **un .xlsx no se puede revisar a ojo**, y su modo de falla es binario — o abre, o Excel dice «formato no válido» y no da una pista más.
- **Causa del bloqueo (no del bug): los candados que había NO alcanzan para autorizar un refactor.** Los 25 tests verifican zip válido, partes mínimas, XML bien formado en TODAS las partes, content-type, nombre y contenido… y aun así **no ven** el defecto que rompe estos archivos: Excel exige las celdas de una fila **en orden de columna y sin refs repetidas**, y si no se cumple rechaza el archivo entero **con el XML perfectamente bien formado**. Un candado de parseo es ciego a eso por construcción.
- **Solución — la red se arma ANTES de tocar nada, y son tres capas:**
  1. **Diff de la SALIDA, no del código.** Un script captura las 13 partes XML de los dos archivos y las hashea; se corre antes del refactor y después, y se comparan con `diff -r`. Resultado: **idénticas byte a byte**. Es la única evidencia que convierte «creo que no cambié nada» en un hecho. **Ojo: no se comparan los bytes del .zip** — `ZipArchive` estampa el mtime en cada entrada, así que dos corridas del *mismo* código nunca dan el mismo binario; hay que comparar las partes descomprimidas. Los datos de prueba se arman **en memoria y con valores fijos** (`new Vehiculo([...])` sin tocar la BD de desarrollo): con una factory random el diff no significa nada.
  2. **Abrir con Excel de verdad** (COM desde PowerShell, Excel está en la máquina de desarrollo). Señales que se miran: `$wb.Saved` en `False` tras el `Open` = Excel modificó el libro al abrirlo, o sea **lo reparó**; más logs `error*.xml` nuevos; más contenido (hojas, rótulos, que la fecha sea `Double` con formato `dd-mm-yyyy` y no texto).
  3. **El control que vuelve creíble el «OK»** — y sin el cual la capa 2 es decorativa: se fabricó un .xlsx **roto a propósito** reordenando las celdas de una fila, y se comprobó que **Excel se niega a abrirlo** (`No se puede obtener la propiedad Open`) mientras `simplexml_load_string` lo sigue dando por bueno. Recién ahí el verde del archivo bueno dice algo.
- **Y el refactor cambió el radio de explosión, así que pide candados nuevos.** El `ksort` de `filaCeldas` ahora es UNO solo para los dos Excel: borrarlo rompía dos archivos a la vez y **los 25 tests seguían verdes**. Se agregaron `tests/Unit/Excel/{FilasXlsxTest,EscritorXlsxTest}.php` (15) y se **mutaron los dos en los dos sentidos**: sin `ksort` → rojo el nuevo, **verdes los 25 viejos**; con el mapa de `rId` roto → rojo el nuevo, verdes los 25. Esa asimetría *es* la justificación de los candados nuevos.
- **Detalle que se conservó a propósito:** `styles.xml` se queda con **`rId2` siempre**, así que las hojas van `rId1, rId3, rId4…`. Se ve raro, es lo que ya emitían las dos clases, y mantenerlo es lo que permitió el diff byte a byte. Está documentado en `EscritorXlsx::rIdHoja` para que nadie lo «arregle» sin querer.
- **Evitar a futuro:** (1) ante un refactor de un generador de binarios, **la red no es el test suite: es el diff de la salida** — captura, refactor, captura, `diff`; (2) para que ese diff valga, la entrada tiene que ser **determinista** (valores fijos, nada de faker) y hay que comparar **las partes**, no el contenedor con timestamps; (3) cuando un refactor **junta** código duplicado, el candado que antes protegía un caller ahora protege dos — es el momento exacto de escribirle un test propio a lo compartido, y de probar con una mutación que los candados viejos **no** lo cubrían.

### [2026-08-04] Un vencimiento a 31 días entraba en la franja de «30 días»: restar la hora de Chile menos una columna `date` en UTC se come un día
- **Síntoma:** `VehiculoTest::test_por_vencer_es_treinta_dias_y_no_uno_mas` en rojo — un documento que vence en **31** días resolvía a `por_vencer` en vez de `al_dia` (`Failed asserting that two strings are identical: -'al_dia' +'por_vencer'`). El de 30 días estaba bien, así que la franja se veía «corrida en uno» sin ninguna suma de más en el código.
- **Causa:** se restaban **dos instantes de zonas distintas**. `FechaNegocio::ahora()` devuelve `Carbon::now('America/Santiago')` (−04) y una columna con cast `date` se hidrata en la timezone de la app (**UTC**): `startOfDay()` en Chile es 04:00 UTC, así que la diferencia contra una medianoche UTC daba `30.83` días y el `(int)` **trunca hacia cero** → 30. El error es de un día completo y solo se nota en el borde exacto de la franja; con un umbral menos redondo pasa inadvertido.
- **Solución:** comparar **fechas, no instantes**: `Carbon::parse(FechaNegocio::hoy())` contra `Carbon::parse($vence->toDateString())`. Las dos nacen de un `'Y-m-d'` en la misma zona, así que la diferencia es un entero de días exacto sea cual sea la zona de origen (`app/Models/Vehiculo.php::documentos()`).
- **Evitar a futuro:** (1) `FechaNegocio::*` da el **día de negocio**, y mezclarlo con un `datetime`/`date` de la base es comparar peras con manzanas — para «cuántos días faltan» pasar las dos puntas por `toDateString()` primero; (2) el `(int)` sobre un `diffInDays` de Carbon 3 (que devuelve **float**) trunca, no redondea: en un borde `<=` eso es un día entero de diferencia; (3) un candado de umbral tiene que probar **los dos lados del borde** (30 y 31) — con solo el caso interior este defecto pasaba verde.

### [2026-08-04] Un cast `date` escribe `'Y-m-d H:i:s'`: el `firstOrCreate` no encontraba la fila que él mismo había insertado y reventaba contra su unique
- **Síntoma:** `SQLSTATE[23000]: Integrity constraint violation: 19 UNIQUE constraint failed: vehiculo_avisos.vehiculo_id, vehiculo_avisos.documento, vehiculo_avisos.hito, vehiculo_avisos.vence` al correr **dos veces** el comando de avisos. Un `firstOrCreate` no puede violar su propio unique: eso significa que el `SELECT` no ve lo que el `INSERT` escribió.
- **Causa:** la clave de búsqueda llevaba la fecha como **string** `'2026-08-09'`, pero el atributo tiene cast `date`, y al guardar Eloquent pasa por `fromDateTime()` → serializa con `getDateFormat()`, o sea **`'2026-08-09 00:00:00'`**. El `where` con el string corto no matchea nunca (comparación de texto en SQLite), así que la segunda corrida vuelve a intentar el `INSERT`.
- **Solución:** pasar un **Carbon a medianoche** en la clave (`$fecha->copy()->startOfDay()`): la lectura la bindea con el mismo formato con el que la escritura la guardó. Aplica igual a `firstOrCreate`, `updateOrCreate` y `firstOrNew`.
- **Evitar a futuro:** en la clave de un `firstOrCreate`/`updateOrCreate` **nunca** poner un string para un atributo con cast de fecha (ni `date`, ni `datetime`, ni `immutable_*`): o se pasa un objeto de fecha, o se declara el formato del cast (`date:Y-m-d`) **y** se guarda igual. Síntoma-guía: si un `firstOrCreate` viola su unique, el sospechoso es la **serialización** de una de sus claves, no la concurrencia.

### [2026-07-31] `now()->subMonth()` un día 31 DESBORDA al mismo mes: main rojo solo los días 29/30/31 (el reloj congelado de la suite fija la HORA, no la FECHA)
- **Síntoma:** el push del 31-07 (P-PLAN-01) dejó Tests de main en FAILURE con 2 rojos ajenos al diff: `DashboardTest::test_tarjetas_de_taller_agrupan_estados…` (`Failed asserting that 2 is identical to 1` en «Entregadas (mes)») y `ServicioTecnicoManagementTest::test_index_periodo_por_fecha_de_retiro` (veía «Retiro Mes Pasado»). Reproducible en local ese mismo día con el código de main SIN el diff nuevo; la víspera la misma suite había dado 1256 verdes.
- **Causa:** ambos fixtures creaban su registro «del mes pasado» con `now()->subMonth()->toDateString()`. Carbon con overflow por defecto: 31-jul − 1 mes = «31 de junio» → **desborda al 1 de julio, el MISMO mes** → el fixture contaba en la tarjeta/filtro del mes actual. El `travelTo(now()->setTime(12,0))` de `tests/TestCase` congela la HORA (mata el flaky de medianoche, [2026-07-20]) pero la FECHA sigue siendo la real → la suite entera es sensible al calendario: esto solo revienta los días 29/30/31 tras un mes más corto. Es el primo por-fecha del flaky-por-reloj de [2026-07-13].
- **Solución:** en los 2 fixtures con assert de borde de mes, ancla estable `now()->startOfMonth()->subDay()` (último día del mes ANTERIOR — nunca desborda, se auto-documenta). Los otros ~14 `subMonth(s)` de los tests (todos `garantia_doc_fecha`) NO se tocaron: solo necesitan «una fecha pasada» y el desborde de ±3 días no cruza ningún borde de aserción. El único `subMonth` de producción (`AgendaTrabajoController:48`) es seguro: opera sobre `Carbon::create($anio,$mes,1)` (día 1 jamás desborda). Verificado en el día de repro natural (31-jul): 2/2 verdes y los 2 archivos completos 102 verdes. Fix test-only en territorio ST/dashboard bajo la orden de deploy del dueño (excepción puntual, precedente I-06 / hotfix PwaTest [2026-07-13]).
- **Candado agregado después (misma fecha, sesión aparte que reprodujo el rojo por su cuenta):** el fix de los fixtures deja los dos tests verdes **el 31 de julio**, pero ninguno de los dos prueba el borde del mes los otros 364 días — siguen mirando un solo día, el de la corrida. `DashboardTest::test_entregadas_del_mes_no_depende_del_dia_de_la_corrida` recorre con `travelTo` 8 días peligrosos (los 31 de mar/may/jul/oct/dic, dos días 1 y el 29-feb bisiesto) y en cada uno crea 4 órdenes ancladas a los **cuatro bordes**: primer y último día del mes DENTRO, último del anterior y primero del siguiente FUERA → siempre 2. Mutado en los dos extremos del rango de `DashboardController` (correr `endOfMonth()` un mes → rojo; `startOfMonth()` un mes → rojo), y con la primera mutación puesta **el test de arriba seguía verde**: el candado cubre terreno que el fix no cubría. Los dos rangos de producción quedaron auditados y correctos (`FechaNegocio::ahora()->startOfMonth()..endOfMonth()` en el dashboard, `Carbon::create($anio,$mes,1)..endOfMonth()` en `ServicioTecnicoController::filtros`): el defecto era 100% de los fixtures. De paso quedó descartado el otro sospechoso, `test_cada_orden_recibe_un_codigo_unico_impredecible` — no es flaky (15 corridas en procesos frescos, 3 de su clase, la suite entera: verde; su `codigo` sale de `Str::random(8)` y no toca fechas): era el rojo del test **vecino** (línea 991 vs 1000, nombre truncado en la salida de PHPUnit) atribuido al de al lado.
- **Evitar a futuro:** (1) en un fixture cuyo assert depende de un **borde de mes/año**, jamás `subMonth()/addMonth()` relativo a hoy — usar anclas que no desbordan (`startOfMonth()->subDay()`, `subMonthNoOverflow()`); (2) si main amanece rojo con fallas «de conteo por mes» un día 29/30/31, sospechar de este gotcha ANTES que del diff que lo destapó — reproducir local con el código viejo separa entorno de defecto; (3) el reloj congelado de la suite NO protege contra la fecha del calendario: un test sensible al día del mes debe viajar con `travelTo()` propio a una fecha fija.

### [2026-07-30] El bundle se alimentaba de la DOCUMENTACIÓN, de un test y de un worktree viejo — se cortó con `source(none)` (y el fix anterior nunca llegó a commitearse)
- **Síntoma:** un `npm run build` tras tocar solo Blades (sin clases nuevas) cambió el hash del CSS. El diff no mostraba clases agregadas: mostraba **14 reglas `file:*` que se caían** (las de un `<input type="file">` crudo que hoy es `<x-archivo-input>`). O sea, el bundle **commiteado** tenía CSS que ninguna plantilla puede usar, y el build "nuevo" era el correcto.
- **Causa:** la **autodetección de fuentes** de Tailwind v4 barre el proyecto entero, así que cualquier archivo con una palabra que parezca una utilidad genera CSS. Entraban clases de cuatro sitios: (1) **la propia bitácora y `docs/`** — la versión anterior de esta entrada nombraba dos utilidades al explicarlas y **por eso las generaba**: documentar el defecto lo causaba; (2) **el regex de un test** (`MarcoHorizontalTest` busca un patrón de padding y ese patrón entraba como clase); (3) **`.claude/worktrees/`** — worktrees de sesiones viejas con **copias antiguas de los mismos Blades**; git los excluye de `git status` (los tiene registrados) pero existen en el filesystem y el escáner no razona en términos de git; (4) **el bundle ANTERIOR**, que se auto-alimenta si no se borra `public/build` antes de compilar (el escáner saca candidatos del texto del CSS: de una declaración `border-…` sale la utilidad de visibilidad homónima). Misma familia que [2026-07-24] con `storage/framework/views`: **el barrido dependía de directorios transitorios, así que el bundle no era reproducible**.
- **Solución:** `@import 'tailwindcss' source(none)` en `resources/css/app.css` → se barren EXACTAMENTE los globs declarados (`resources/**/*.blade.php`, `resources/**/*.js`, vistas de paginación) y nada más. 57.4 KB contra 60.8: **29 reglas que ninguna plantilla usa**. De paso se fue el `@source` de las vistas compiladas (redundante: compilar un Blade no inventa clases), y con eso el gotcha del 24-07 queda cerrado por construcción, no por acordarse de `view:clear`. **Verificado en el peor caso:** con las 297 vistas compiladas pobladas (`view:cache`) el bundle sale igual.
- **Cómo se comprobó que no se cayó nada EN USO** — la mitad que importa, porque un build que "adelgaza" el CSS también puede estar rompiendo una pantalla, y hay que poder distinguirlo: en vez de revisar clase por clase (ahí se cuela el verde-engañoso), se hace al revés — se extraen los **688 tokens de clase reales** de los `class="…"` de los Blades/JS y se comprueba, por cada uno **que el bundle viejo generaba**, que el nuevo también. Resultado: **0 pérdidas**. Con los dos controles, que son lo que vuelve creíble ese 0: **positivo** — 474 de los 688 matchean en el bundle viejo, así que el escapado del selector funciona (sin este número, un escapado roto da "0 pérdidas" **por vacío**); **negativo** — quitándole a mano una regla al bundle nuevo, el chequeo la detecta.
- **Dos falsos resultados en el camino, los dos por substring** (misma lección que el `\brut\b` del 28-07): `grep "px-8"` matchea dentro de `lg:px-8` → falso "esta clase SÍ se usa"; y `grep 'file\\:mr-3'` entre comillas simples busca **dos** backslashes cuando el CSS tiene uno → falso "ya no está en el bundle". Para buscar una clase hay que exigirle **límites de token**, y para buscar en el CSS conviene `grep -F` o hacerlo en un script, no pelear con el escapado del shell.
- **Y el fix no llega si otra sesión compila en el mismo árbol:** el rebuild limpio iba en `c2fb826`, quedó **staged**, y entre el `git add` y el `git commit` otra sesión corrió `npm run build` en el mismo working tree y devolvió el bundle viejo → el commit salió sin `public/build` y su mensaje afirmaba un cambio que no viajó. **Después de commitear assets, confirmar con `git show --stat HEAD` que `public/build` está en el commit**; con varias sesiones sobre un repo, el índice no es un lugar seguro donde dejar algo esperando.

### [2026-07-30] Un candado estructural que en Windows NO aplicaba su lista de excepciones: rojo falso permanente en la suite local, verde en la CI
- **Síntoma:** la suite completa daba **1198 verdes y 1 rojo**, y el rojo (`ArchivoInputTest::test_ninguna_vista_usa_un_input_file_crudo`) acusaba de «input file crudo» a **los dos casos que su propio comentario dice excluir**: el componente `archivo-input.blade.php` —que ES el que lleva el input nativo— y `errors/429.blade.php`, que solo lo menciona en un comentario. En la CI el mismo test pasaba.
- **Causa:** la línea que calcula la ruta relativa era `str_replace(resource_path('views').'/', '', $archivo->getPathname())`. En **Windows** los dos caminos vienen con `\`, así que el `/` **concatenado a mano** no matchea nunca: `$ruta` se quedaba con la ruta **absoluta** (`C:\...\resources\views\components\archivo-input.blade.php`) y las dos guardas siguientes —`$ruta === 'components/archivo-input.blade.php'` y `str_starts_with($ruta, 'errors/')`— no podían dar `true` jamás. En Linux (la CI es `ubuntu-latest`) el separador sí es `/` y todo funcionaba, así que el defecto **solo muerde a quien desarrolla**, que en este proyecto es toda la flota.
- **Solución:** `str_replace('\\', '/', $archivo->getRelativePathname())` — `File::allFiles()` devuelve el `SplFileInfo` de Symfony, que ya trae la ruta relativa a la raíz del barrido; solo hay que normalizar el separador. De paso el mensaje de fallo pasó a nombrar `profile/edit.blade.php:41` en vez de una ruta absoluta.
- **Verificado por mutación en los dos sentidos**, que es lo que importa acá: con el fix el test pasa (3 verdes), y **metiendo un `<input type="file">` crudo de verdad en una vista real vuelve a ponerse rojo**. Sin esa segunda mitad, «arreglar» el test podría haber sido convertirlo en un no-op que nunca vuelve a fallar — que es peor que el rojo falso.
- **Evitar a futuro:** (1) para la ruta relativa de un archivo en un barrido, **nunca** concatenar el separador a mano (`resource_path(...).'/'`) — usar `getRelativePathname()` y normalizar `\`→`/`, o `DIRECTORY_SEPARATOR`; (2) **un rojo que acusa exactamente a lo que el test dice excluir no es un hallazgo: es el filtro del test que no está aplicando** — mirar el valor real de la variable de ruta antes de tocar el código acusado; (3) todo candado que se «arregla» se muta **en los dos sentidos** (pasa cuando debe pasar **y** falla cuando debe fallar), misma lección que el `\brut\b` del 28-07: un candado que no se muta no es un candado.

### [2026-07-29] Un panel escondido con `translate` sigue siendo TABULABLE (el drawer cerrado se recorría entero con el teclado), + dos trampas de medición
- **Síntoma:** ninguno visible, y por eso duró desde que existe el menú V4. En móvil, con el menú **cerrado**, un usuario de teclado o de lector de pantalla recorría los ~20 controles del menú —invisibles, fuera de la pantalla— **antes** de llegar al contenido de la página. Lo destapó el gate R-31 de P-NAV-05 mirando el shell, no una pantalla.
- **Causa:** el drawer se oculta con `max-lg:-translate-x-full`, que compila a `translate:-100%`. **Un `translate` saca de la VISTA pero no del orden de tabulación ni del árbol de accesibilidad** — para el navegador el elemento sigue siendo visible, solo está en otras coordenadas. Y este `<aside>` es el **primer nodo del shell**, así que se recorría todo antes que nada. No había ninguna mitigación: cero `inert`, cero `aria-hidden` sobre el `<aside>` (el único estaba en el scrim, que sí corresponde), y el JS no movía el foco.
- **Solución:** `max-lg:invisible` **pegada al translate y por la misma vía** del patrón anti-flash que el componente ya usaba (clase estática que oculta desde el primer paint; Alpine solo la RETIRA al abrir, y con el prefijo `max-lg:` el escritorio queda intacto). `visibility:hidden` sí saca del tab order. La transición pasa a `transition-[translate,visibility]`: con solo `translate` el drawer **desaparecería de golpe** al cerrar en vez de deslizarse, porque `visibility` se interpola de forma discreta y sin transición salta de inmediato.
- **Medido, no argumentado** (banco estático del patrón [2026-07-26], con el CSS compilado real y las clases verbatim del componente): a 375px, la versión vieja da `visibility: visible` con `left:-300` y sus controles **`focusable: true`**; con el fix, `visibility: hidden` y **`focusable: false`**. Los dos asides quedan en `left:-300`, así que la única variable es `visibility`. Drawer abierto y escritorio a 1280 (`sticky`, 264px): focuseables, sin regresión.
- **Trampa de medición 1 — no midas después de un `resize`:** al agregar `visibility` a la transición, un cambio de viewport deja la propiedad **en vuelo**, y como el Browser pane oculto **no corre `requestAnimationFrame`** (gotcha [2026-07-26]) la transición nunca termina: `getComputedStyle` devuelve el valor **congelado del estado anterior** y el fix parece no aplicar. Hay que medir en **carga fresca** al ancho que se quiere probar. Es la misma familia que el `x-transition` a medias, pero ahora muerde a una medición de CSS puro.
- **Trampa de medición 2 — el candado propio salió verde-engañoso:** el `aria-controls` se le puso a los **dos** toggles (la hamburguesa y la X del drawer), así que `assertSee('aria-controls="dg-menu-lateral"')` **pasaba en verde con el atributo quitado de la hamburguesa** — lo aportaba el otro. Lo cazó la mutación. Se corrigió **contando** (`substr_count` == 2) en vez de buscar la cadena.
- **Evitar a futuro:** (1) esconder algo con `translate`/`opacity`/`left:-9999px` **no lo saca del teclado ni del lector de pantalla** — para eso va `visibility:hidden`, `display:none` o `inert`; ante cualquier panel off-canvas, preguntarse qué pasa al tabular con el panel cerrado; (2) si agregás `visibility` a una transición, toda medición de estado se hace en carga fresca, nunca tras un resize; (3) cuando el mismo atributo lo emiten **dos** elementos de la página, el assert se cuenta, no se busca — y la mutación es la única forma de enterarse (doctrina [2026-07-20]).

### [2026-07-28] El RUT terminado en K, y un candado estructural que se equivocó porque `\brut\b` NO matchea dentro de `cliente_rut`
- **Síntoma:** ninguno en producción — y el valor de la entrada es justamente eso. Una rama de trabajo le puso `inputmode="numeric"` al campo del RUT en 5 pantallas (los 3 formularios públicos del QR incluidos) con un comentario razonable: «el teclado telefónico trae + * # pero NO el guión». El detalle que faltaba: el teclado **numérico** de iOS tampoco tiene la **K**, ni el guión, ni tecla para pasar a letras.
- **Causa:** `Cliente::dvRut()` devuelve **'K'** cuando el resto del cálculo es 10, y eso ocurre en **9 de cada 100 RUT** (medido sobre 1.000 consecutivos). El validador exige el DV correcto, así que ese 9% de clientes —escribiendo su propio RUT en su propio celular, de pie en el mostrador— no podía terminar el formulario. El defecto es **silencioso al probar**: el 91% de las pruebas a mano funcionan perfecto.
- **Solución:** producción quedó auditada punta a punta (validador con las 5 formas que la gente escribe, `normalizarRut`, columna `varchar(20)`, y la búsqueda que solo quita puntos y espacios y por eso conserva la K) y blindada con `RutConKTest`, cuyo cuarto test es **estructural**: prohíbe `inputmode="numeric|decimal"` y `type="number"` en cualquier campo de RUT. Si algún día hace falta mejorar ese teclado, lo que sí sirve es `inputmode="text"` con `autocapitalize="characters"`.
- **La lección del candado, que es lo que hay que recordar:** la primera versión del test **no atrapó la mutación** y pasó en verde con el bug puesto. La razón es de regex, no de lógica: **`\brut\b` no matchea dentro de `cliente_rut`**, porque el guión bajo cuenta como carácter de palabra y entre `_` y `r` no hay frontera. Y buscar `rut` como substring suelto habría dado falso positivo con el campo **`ruta`** del formulario de servicio técnico. La forma correcta fue mirar el **atributo** (`name`/`id`/`x-model` terminado en `rut"`). **Un candado que no se muta no es un candado: es un test que da tranquilidad falsa.**
- **Evitar a futuro:** (1) antes de elegir un `inputmode`, preguntarse qué caracteres admite el campo en su peor caso legítimo — el DV K, un guión, una letra en un código; (2) `\b` no sirve para aislar una palabra que viaja pegada con guión bajo, que es como se llaman casi todos los campos de este repo; (3) todo candado estructural se prueba inyectando el defecto que dice prevenir, y en más de un archivo.

### [2026-07-28] Tres minas del acordeón y del `confirm()`: un `required` escondido aborta el envío EN SILENCIO, el candado no puede ser un microtask, y un apóstrofo se come la confirmación
- **Síntoma:** los tres se destaparon probando en el simulador de iPhone, ninguno en la teoría. (1) Al plegar una máquina completada del ingreso por cantidad, el botón Enviar no hacía **nada**: sin mensaje, sin error visible. (2) Al blindar eso, con 3 máquinas vacías se abría la **máquina 3** en vez de la 1, o sea el último problema en vez del primero. (3) El gate R-31 sobre el propio lote encontró que la confirmación de «Marcar realizado» desaparecía con ciertos nombres de cliente.
- **Causa:** (1) `x-show` oculta con `display:none`, y un campo `required` **oculto no se puede enfocar**: el navegador aborta la validación con un `TypeError` en consola y no envía — es la falla más silenciosa posible, porque el usuario solo ve que el botón no responde. (2) El navegador dispara `invalid` en **TODOS** los campos inválidos de la pasada, en orden del documento, no solo en el primero; quedarse con el último gana el equivocado. (3) `{{ }}` = `e()`, que convierte el apóstrofo en `&#039;`; el parser de HTML lo devuelve a `'` **dentro del atributo**, así que `confirm('… D'Angelo?')` es un **SyntaxError**, el handler `onsubmit` muere y el formulario se envía sin preguntar. Y ese nombre lo escribe el cliente en el formulario público del QR, así que el apóstrofo llega solo.
- **Solución:** (1) escuchar `invalid` en **fase de captura** en el contenedor (el evento `invalid` **no burbujea**, pero la fase de captura sí alcanza al target) y abrir la tarjeta del campo que falló, enfocándolo en `$nextTick`. (2) Guardar el **primero** de la pasada y actuar una sola vez con `setTimeout(…, 0)` — **NO** `queueMicrotask`: al volver de cada listener la pila de JS queda vacía y el navegador **drena los microtasks**, así que un candado en microtask se limpia entre evento y evento y no filtra nada (probado). Una tarea nueva corre ya terminada toda la ráfaga. (3) `Illuminate\Support\Js::from()` en vez de `{{ }}` dentro de cualquier string JS: escapa el apóstrofo a `'`, y también `"` y `&`. Candado con un nombre con apóstrofo real, mutado.
- **Evitar a futuro:** un `x-show` que envuelva campos `required` es un envío roto en silencio — o el candado de `invalid.capture`, o los campos no se esconden; ante «el botón no hace nada» en un formulario, mirar la consola por el `TypeError` de foco antes que la lógica; y **ningún dato de usuario entra a un `confirm()`/string JS por `{{ }}`** (apellidos y razones sociales traen apóstrofos: "D'Angelo", "O'Higgins"), va por `Js::from`. Los `confirm()` del repo que interpolan folios o correos están a salvo por el tipo de dato, no por el mecanismo.

### [2026-07-30] El candado anti-no-op de las one-shot de plantillas no admitía CADENAS (segunda migración sobre la misma clave)
- **Síntoma:** al cambiar el texto de 7 plantillas en `ConfiguracionSeeder` (arreglos de la auditoría de la ruta de taller) y agregar su one-shot `2026_07_30_100000`, falló `OneShotPlantillasCandadoTest::test_la_one_shot_de_notificaciones_internas_calza_con_el_seeder` — señalando la one-shot **anterior** (`2026_07_22_180000`), que yo no había tocado.
- **Causa:** el candado (excelente, del dictado v29) verificaba que el `new` de **cada** one-shot fuera lo que siembra el seeder hoy. Ese invariante solo puede cumplirlo **la última** one-shot que toca una clave: cuando una plantilla se edita por segunda vez, la cadena es A→B (22-07) y B→C (30-07), y el seeder siembra C — así que el `new` de la primera (B) deja de coincidir por construcción, sin que nada esté roto. El candado modelaba «una one-shot por clave» y la segunda edición de una misma plantilla lo invalida.
- **Solución:** el invariante correcto tiene DOS mitades y el test ahora verifica las dos: **(1)** la cadena está *encadenada* — el `new` de una one-shot es el `old` de la siguiente que toca esa clave (si se rompe, la segunda no encuentra el texto que espera en una BD ya migrada → el no-op silencioso que el candado existe para cazar); **(2)** el `new` de la **última** one-shot de cada clave es lo que siembra el seeder. Las migraciones se listan en orden cronológico y se agrupan por clave antes de comparar. La de aprobaciones queda con su test aparte porque reemplaza el JSON completo (asunto + cuerpo) y hoy es la única que toca esas claves.
- **Evitar a futuro:** un candado que ata «el código de hoy» con «una migración histórica» tiene que modelar la **serie**, no el par. Regla al agregar una one-shot de plantillas: sumarla **al final** de la lista de `OneShotPlantillasCandadoTest` y verificar que su `old` sea EXACTAMENTE el `new` de la anterior (no el texto original del seeder de hace tres migraciones). Y no “arreglar” el fallo editando el `new` de la migración vieja: eso reescribe una migración ya ejecutada en producción y deja la cadena mintiendo sobre lo que entregó.

### [2026-07-29] Un test INTERMITENTE que fallaba 1 de cada 100 corridas: `assertDontSee('UF')` contra el token CSRF
- **Síntoma:** `VisitaIndustrialTest::test_el_cliente_no_ve_los_valores_uf_de_los_servicios` falló en CI (`Not to contain: UF`) sobre un commit que no tocaba nada de esa pantalla, y **pasaba en local**. La corrida de CI de una hora antes, con el mismo código de esa vista, había pasado.
- **Causa:** `assertDontSee('UF')` busca esas dos letras en **todo el HTML**, y la página pública trae el **token CSRF** — 40 caracteres al azar en base62, y aparece **dos veces** (el `<meta name="csrf-token">` y el `@csrf` del formulario). La probabilidad de que un token de 40 caracteres contenga la secuencia `UF` es **1,07%** (medido: 2.150 de 200.000). O sea que el test fallaba ~1 de cada 100 corridas de CI, sin relación con el commit que lo disparaba.
- **Por qué costó cazarlo:** las dos primeras hipótesis eran razonables y las dos eran falsas. (1) *"Es el hash del bundle de Vite"* — los nombres de `public/build/assets/*` cambian en cada `npm run build` y podrían contener `UF`; se descartó mirando el manifest. (2) *"Es la caché de vistas: el `{{-- comentario --}}` de la vista dice «el valor en UF es interno» y en local está compilado de antes"* — se descartó con `php artisan view:clear`, y además los comentarios Blade nunca llegan al HTML. **Y la medición que hice primero era inválida:** renderizar la página 200 veces dentro de un mismo test dio 0 coincidencias, porque ahí el token **es el mismo** — lo aleatorio es cada CORRIDA, no cada render. Lo que lo destapó fue listar todas las secuencias en mayúsculas del HTML (`preg_match_all('/[A-Z]{2,}/')`): aparecían `FC`, `GZ`, `OXO`, `ZMO`, **cada una exactamente dos veces** — la firma de un único valor repetido en dos lugares.
- **Solución:** assertar contra el **número** y no contra las letras. El servicio se crea con `valor_uf => 7.75` y se verifica `assertDontSee('7,75')` + `assertDontSee('7.75')`: un token base62 no puede contener un punto ni una coma, así que la colisión es imposible. Se conserva el `assertSee` del nombre, que es la otra mitad de la intención.
- **Evitar a futuro:** **un `assertDontSee` de 2-3 caracteres es una bomba de tiempo** si la página contiene cualquier cosa aleatoria (token CSRF, hash de assets, ids de Alpine, UUID). Regla: para negar la presencia de un dato, assertar el **valor concreto y distinguible** que tendría ese dato al renderizarse —con separadores, decimales o espacios—, no una sigla corta. Y cuando un test falla en CI y pasa en local, antes de culpar al entorno conviene preguntarse **qué cambia entre corridas y no entre renders**.

### [2026-07-28] Emisión de DTE: por qué el POST a Bsale NO lleva reintentos, y dos trampas de Http::fake
- **Síntoma:** ninguno todavía — se cazó al escribir `BsaleClient::post()` para M05·B3. El cliente ya tenía un helper `request()` compartido con `->retry(2, 500, throw: false)`, y reusarlo para el POST era lo natural (una línea menos).
- **Causa:** un reintento automático sobre `POST /v1/documents.json` **emite un segundo documento tributario**. La ventana es real: si Bsale procesa la solicitud y la respuesta se pierde de vuelta (timeout, corte de red del hosting compartido), el cliente no puede distinguir "no llegó" de "llegó y no me enteré" — y reintenta. El resultado son dos folios reales, con IVA declarado al SII, que **no se borran**: se corrigen con nota de crédito, con el cliente esperando en el mostrador. El `retry` de Guzzle es correcto para los GET del espejo (idempotentes por definición) y venenoso para el único verbo que crea documentos.
- **Solución:** `post()` NO comparte el `request()` de `get()` y no lleva `->retry()`. Solo reintenta el **429**, que significa *rechazado por exceso de velocidad* → no llegó a crear nada. La protección contra duplicados es de dos capas y ninguna es un reintento: `salesId` del lado de Bsale y el **índice único de `dte_emitidos.sales_id`** del lado nuestro, con la fila **reservada ANTES** de llamar al emisor (`EmisionDte::reservar()`). El orden importa: si se guardara después de emitir, dos clics rápidos mandarían dos POST antes de que ninguno alcanzara a escribir. El `catch (QueryException)` de esa función no es defensivo "por si acaso" — es el camino que recorre la segunda petición cuando choca con el índice, y el candado `EmisionDteTest::test_emitir_dos_veces_el_mismo_origen_no_duplica` verifica que el emisor recibe **una** llamada.
- **Dos trampas de test que costaron una corrida cada una:** (1) los patrones de `Http::fake(['*/x.json' => …])` comparan la **URL COMPLETA, con query string**: `*/document_types/caf.json` NO matchea `…/caf.json?codesii=39`, el fake se salta y el test **sale a la red de verdad** (falla con `cURL error 6: Could not resolve host`, que parece un problema de entorno y no del patrón) → hay que cerrar con `*`; (2) el tercer parámetro de `assertDatabaseCount` es la **conexión**, no un mensaje — pasarle un texto explicativo da `Database connection [Reusa la fila...] not configured`. Y de PHP: no se puede usar `...$extra` después de argumentos con nombre (`Cannot use argument unpacking after named arguments`) — en un helper de test, construir el objeto y sobrescribir las propiedades públicas después.
- **Evitar a futuro:** antes de reusar un helper HTTP compartido, preguntarse si el verbo nuevo **crea** algo. "Reintentar" y "crear" no se combinan sin una clave de idempotencia, y cuando lo creado es un documento tributario el costo de equivocarse no es un registro duplicado: es un trámite contable.

### [2026-07-28] El valor hora y el precio de los repuestos salían de una lista de precios ARBITRARIA (5 listas activas, criterio «la primera que aparezca»)
- **Síntoma:** ninguno visible — y eso es lo grave. Las cotizaciones de servicio técnico mostraban un precio plausible, pero no había forma de saber de qué lista salía. Se cazó revisando la pantalla *Comercial → Precios* mientras se definía qué lista usar para facturar: hay **15 listas espejadas de Bsale y 5 ACTIVAS con ~2.855 precios cada una** (COQUIMBO-1, CSTS, EXTERIOR 1, EXTERIOR 2, GENERAL).
- **Causa:** el criterio de precio estaba escrito **cuatro veces** (`ServicioTecnicoController::precioVentaProducto`, `::precioHoraServicio`, `::buscarRepuesto`, `TiempoReparacionController::valorHora`) y las cuatro decían lo mismo: `$producto->precios->first(fn ($x) => $x->lista?->activa) ?? $producto->precios->first()`. O sea **"la primera fila activa que devuelva la BD"**, sin `orderBy`. Con **una** lista activa eso era correcto y por eso nadie lo notó; al espejarse las 5 pasó a ser una lotería estable-pero-arbitraria → una reparación de Mirador podía cotizarse con precios de Coquimbo. El bug no lo introdujo un cambio de código: lo introdujo un cambio en **los datos**.
- **Solución:** fuente única + lista explícita. `config('daligo.lista_precios_ventas')` (= `GENERAL`, elegida por el dueño porque el origen de las otras no está claro) y un solo método `Producto::precioVentaConIva()` que reemplaza las 4 copias. **Si el producto no tiene precio en la lista oficial devuelve `null` a propósito** (quien atiende lo escribe a mano) en vez de caer a otra lista: mejor sin precio que con uno de origen incierto. La comparación es por **nombre** normalizado (mayúsculas/espacios), no por el flag `activa`, para que desactivar GENERAL en Bsale no haga saltar el precio a otra lista en silencio. Candados en `PrecioListaOficialTest` (8; el primero crea la lista de Coquimbo **antes** que GENERAL, así que falla con el criterio viejo). Suite 1033 verde.
- **Evitar a futuro:** (1) **`->first()` sobre una relación sin `orderBy` no es "el precio", es "un precio"** — si la respuesta correcta depende de *cuál* fila, hay que nombrarla, no confiar en el orden de la BD; (2) cuando una lógica de negocio está copiada en 4 lugares, el problema no es el duplicado sino que **nadie puede cambiar la regla**: los 4 tenían el mismo comentario ("prioriza una lista activa; si no, cualquiera") y ninguno decía por qué; (3) al hacer una regla más estricta, mové el **default de la factory** al caso normal (acá `PrecioFactory` ahora crea el precio en la lista oficial, con estado `enOtraLista()` para el caso contrario): los 4 tests que ya existían siguieron expresando su intención sin tocarlos, y el escenario del bug quedó disponible como estado con nombre.

### [2026-07-26] El panel de la campanita se salía 72px de la pantalla — REINCIDENCIA del hallazgo [2026-07-01], que arregló el componente y no el mecanismo
- **Síntoma:** (reportado por el dueño en staging) al tocar la campanita, el panel aparecía cortado por el borde izquierdo: "NOTIFICACIONES" se leía "CIONES", "Bandeja de aprobaciones" se leía "de aprobaciones". Pidió que el panel "se expanda en una dirección en la que lo pueda hacer completamente para el tamaño de la pantalla".
- **Causa:** aritmética, no recorte. El panel es `w-80` (320px) anclado `end-0` —borde derecho pegado a la campana— y la campana vive en una sidebar de **264px** con `px-4`: el borde derecho del botón cae en x≈248, así que el panel ocupaba **x ≈ −72 → 248**. No hay `overflow-hidden` en la cadena de ancestros ni `transform` que rompa el contenedor; simplemente no cabía del lado al que lo mandaron. **Lo grave es que ya estaba diagnosticado:** la entrada [2026-07-01] sobre los globos ⓘ concluye textualmente que *"el `align` es estático y no se puede acertar"*, porque el lado bueno depende de dónde caiga el disparador y del ancho de pantalla de quien mira. Ese arreglo reescribió `info-tip` a mano y **no tocó `x-dropdown`**, que quedó con la misma enfermedad y la manifestó en cuanto la campanita se mudó a la sidebar (pulido 5, `03f895a` — el commit dice "los defaults del partial ya son correctos arriba", que era la apuesta que falló).
- **Solución:** el mecanismo, no el componente. Directiva Alpine `x-dg-anclar` (`resources/js/app.js`) que al abrir **mide y coloca**: nace hacia el lado con más espacio libre → si aun así se sale, se corre hacia adentro → si no cabe a lo alto, voltea sobre el disparador o scrollea por dentro; se abstiene si el panel es `fixed`. La regla del lado no es una heurística arbitraria: **reproduce exactamente lo que las vistas ya elegían a dedo** (un ⓘ en la esquina de una tarjeta cae a la derecha del centro y abre hacia la izquierda; uno pegado a un título cae a la izquierda y abre hacia la derecha), y por eso se pudo borrar el prop `align` del ⓘ con sus 14 usos. Va como **directiva sobre el panel** y no como estado del componente, para servir igual al `x-dropdown` (click) y al `info-tip` (hover + single-open por evento de ventana) sin tocar la lógica de apertura de ninguno. De paso: tope estático `max-w-[calc(100vw-1rem)]` por si el JS no corrió, cierre con **Escape** (solo cerraba con click afuera), validación del prop `width`, y `z-40` también en `lg:` para la sidebar. Candados en `PanelAncladoTest` (8, los 6 escenarios **mutados** uno por uno). Suite 912 verdes.
- **Dos trampas técnicas que valen para el próximo panel:** (1) la corrección se escribe en `left`/`top`, **no** en `translate`: Tailwind v4 compila `scale-95` y `translate-y-1` a las propiedades **independientes** `scale`/`translate` (verificado en el bundle: `.scale-95{…;scale:var(--tw-scale-x) var(--tw-scale-y)}`), que ambos componentes usan en su `x-transition`, y un estilo inline las apaga —primo del gotcha [2026-07-22]—; (2) al fijar `top`, el navegador **vuelve a sumar** el `margin-top` del `mt-2`, que ya venía dentro de `offsetTop` → hay que descontarlo, o el panel baja 8px y su borde inferior queda pegado al filo de la pantalla (se cazó midiendo, no leyendo).
- **Verificación sin login:** la sesión local estaba expirada y no se ingresan contraseñas, así que se montó un **banco de pruebas estático** en `public/` (temporal, borrado antes del commit) que replica la geometría del shell y carga el **CSS/JS compilados reales** + el markup real de los componentes vía `Blade::render`, servido por el preview de artisan. Medido con `getBoundingClientRect()` a 375/768/1024/1280/1600/1920: los 4 paneles caben enteros en todos los anchos, sin scroll horizontal, y los dos ⓘ caen a lados **opuestos** solos. Es el patrón a repetir cuando haya que verificar UI y no haya sesión.
- **Evitar a futuro:** (1) cuando la bitácora diga que un idioma "no se puede acertar", el arreglo tiene que ir al **mecanismo compartido**, no al componente donde se notó — si no, el error espera al próximo componente que lo use (acá esperó 25 días); (2) en el banco de pruebas **no** apagar las transiciones globalmente (`transition: none !important`): el `x-show` del ⓘ, que declara `enter` sin `leave`, termina en `display:none` y el globo nunca aparece; (3) el Browser pane oculto **no corre `requestAnimationFrame`**, así que todo `x-transition` queda a medias y ningún panel llega a abrirse por click — para medir, forzar `display` y leer la caja, y descartar de entrada cualquier medición que dependa de rAF.

### [2026-07-24] El override de tamaño de los `<x-icon.*>` era INERTE en todo el repo (`$attributes->merge` concatena y gana el `.h-5` posterior en el CSS)
- **Síntoma:** cualquier `<x-icon.pencil class="h-4 w-4" />` (el idioma documentado para íconos dentro de botones) se renderizaba a **20px, no 16px**. El mismo mecanismo ya se había visto en el avatar de la topbar (medía 40px en vez de 32px, uno de los 13 hallazgos del pre-R-31 de `2557043`), pero la familia entera de los **31** componentes `resources/views/components/icon/*.blade.php` seguía con el patrón roto: `icon-button`, botones con `pencil`/`trash`, chevrons `h-4`, todos ignoraban el override hacia abajo. Los overrides hacia arriba (`h-6`/`h-8`) "funcionaban" pero **por accidente** del orden del CSS.
- **Causa:** el `<svg>` hacía `$attributes->merge(['class' => 'h-5 w-5'])`. `merge` **CONCATENA**: un llamador con `h-4 w-4` producía `class="h-5 w-5 h-4 w-4"`. Como `.h-4` y `.h-5` tienen la misma especificidad, decide el **orden en el CSS compilado**, no el orden del string ni el del llamador — y ahí `.h-5` va **después** de `.h-4` (verificado: `.h-4`@12598 < `.h-5`@12635 < `.h-6`@12672 < `.h-8`@12746 en `app-*.css`) → gana `.h-5`. Por eso el override hacia abajo era inerte y el de hacia arriba ganaba solo por venir después.
- **Solución:** el patrón del avatar (`2557043`) llevado a los íconos **en el componente**, como superset: `@props(['size' => 'h-5 w-5'])` (mismo prop que `x-avatar`) **más** una guarda `@php` que **descarta el default si el llamador ya puso un alto `h-*` en `class`** → nunca se concatenan dos tamaños (`$size = preg_match('/(?:^|\s)h-/', (string) $attributes->get('class')) ? '' : $size;`). Se eligió arreglar en el componente y **no migrar los ~39 llamadores** con tamaño (26× `h-4 w-4`, etc.) a `size="…"`: (1) esos llamadores viven en ~31 vistas, muchas en el territorio de mayor churn (ST/terreno) → la doctrina [2026-07-21] desaconseja tocarlos; (2) migrarlos dejaría el footgun vivo (cualquier `class="h-4"` futuro volvería a ser inerte). La guarda mantiene el idioma `class="h-4 w-4"` funcionando y elimina el footgun. Se conserva el literal `'h-5 w-5'` en el `@props` para la purga de Tailwind. Candado: `tests/Feature/IconoTamanoTest.php` (6 casos; **mutado** — con el merge viejo se pone rojo en el assert del override `h-4`). Suite **825 verdes**; render verificado con `Blade::render` (default→`h-5 w-5`, `class="h-4 w-4"`→`h-4 w-4`, `size`→reemplaza, `min-h-12` no dispara la guarda). Cero cambios de llamadores; cero cambios de bundle salvo el rebuild determinista.
- **Evitar a futuro:** (1) un `$attributes->merge` con una clase de **tamaño** (o cualquier utilidad de igual especificidad que exista dos veces) es un footgun: dos valores concatenados los decide el orden del CSS, no el llamador → mover el tamaño a un `@props(['size'…])` y/o descartar el default cuando el llamador ya lo trae; (2) al crear un ícono nuevo, copiar `pencil.blade.php` **con** su bloque `@props`/`@php` (no volver al `merge(['class' => 'h-5 w-5'])` pelado); (3) para verificar un override de utilidad que "no aplica", mirar el **orden en el CSS compilado**, no asumir que gana el del llamador.
- **Integrado el 2026-07-26** (la rama quedó 71 commits atrás antes de aterrizar): al mergear `main` apareció un **ícono nuevo**, `arrow-right-start-on-rectangle.blade.php` (llegó en `e3abbbb`, pie de la sidebar), con el `merge(['class' => 'h-5 w-5'])` pelado. Se le aplicó el mismo patrón y la familia quedó en **32** componentes. Es el punto (2) de «Evitar a futuro» ocurriendo en la práctica **antes** de que el arreglo alcanzara a aterrizar: mientras la rama esperaba, el repo siguió produciendo íconos con el footgun. Moraleja operativa: un arreglo transversal que toca una familia entera de componentes se integra rápido o se le va llenando el piso de casos nuevos.
### [2026-07-24] Las vistas de error del VENDOR ganan al comodín (y por eso un 500 salía en inglés), + `APP_DEBUG` de producción VERIFICADO por fin

- **Síntoma:** con el 403/404 ya resueltos, un 500 seguía mostrando «500 | SERVER ERROR» y un 429 «429 | TOO MANY REQUESTS» — **en inglés, sin logo y sin salida**. (Ojo: NO era la pantalla de Symfony, como se dijo primero; era la vista *minimal* del vendor de Laravel. La de Symfony aparece solo para los status que el vendor no trae, p. ej. 405.)
- **Causa:** `Handler::getHttpExceptionView()` prueba `errors::{status}` y **solo si no existe en NINGUNA parte** cae en `errors::{N}xx`. El vendor trae `errors/{401,402,403,404,419,429,500,503}.blade.php`, así que **para esos 8 el comodín nunca se usa**: hay que crear el archivo en la app para pisarlos. Un `errors/5xx.blade.php` por sí solo no cubre el 500 ni el 503.
- **Solución:** las 8 del vendor + los comodines `4xx`/`5xx` (que sí atrapan 405, 400, 410, 413, 502, 504…), todas sobre `components/errors/shell.blade.php`. Candado que se auto-actualiza en `ErroresServidorTest`: recorre las vistas numéricas del vendor y exige la nuestra, así un `composer update` que agregue una vista nueva pone el test rojo en vez de dejar una página en inglés.
- **Segundo hallazgo (el que decidía si todo esto servía):** nadie había verificado nunca `APP_DEBUG` en el servidor — se deducía. Importa porque con `debug=true` un 500 real **nunca** llega a la vista (`prepareResponse` corta antes) y además expone el stack trace. **Verificado: `APP_DEBUG=false` en staging**, con una sonda HTTP inocua que además sobrevive a este lote porque no pasa por las vistas:
  ```bash
  R=$(curl -sS -f -L -H 'Accept: application/json' https://staging.impdali.cl/_no-existe) || R=''
  case "$R" in *'"trace"'*) echo 'DEBUG ON - PELIGRO';; *'"message"'*) echo 'DEBUG OFF - ok';; *) echo 'INCONCLUSO: la respuesta no vino de Laravel';; esac
  ```
  El **control positivo** (`"message"` presente) no es opcional: sin él, un host caído, un `ErrorDocument` de LiteSpeed, un WAF o un redirect imprimen «DEBUG OFF» igual — falso negativo hacia el lado peligroso (gate R-31).
  (`convertExceptionToArray()` es el único punto donde `app.debug` cambia una respuesta **sin** depender de las vistas, y aplica también a los HttpException, que se provocan sin daño.)
- **Evitar a futuro:** (1) al crear una vista de error, comprobar si el vendor ya trae ese status — si lo trae, el comodín no te salva; (2) la página del 500 se muestra **cuando la app ya está rota**: prohibido `route()` (usa `url()`), `@auth`/`Auth::` (la sesión vive en MySQL, la causa más probable del 500), `@vite` y `$exception->getMessage()` (para un 500 ese mensaje **es** el `SQLSTATE` original) — hay un candado estructural que lee el Blade y lo verifica; (3) `APP_DEBUG` en el servidor se **comprueba**, no se deduce: la sonda de arriba es de solo lectura y no rompe nada.

### [2026-07-24] Manejo amable de 403/404: `back()` en un GET fallido es un BUCLE, la vista de error con layout revienta SOLO en prod, y un 403 que se vuelve 302 borra tandas del soplador

- **Síntoma:** (D-014) un 403/404 mostraba la pantalla genérica de Symfony, en inglés para los de spatie. Al implementar el manejo amable aparecieron tres trampas, ninguna evidente:
- **Causa:** (1) **bucle de `back()`**: `StartSession::storeCurrentUrl()` guarda `_previous.url` DESPUÉS de generar la respuesta, incluida la que dio 403 → sin `Referer` (URL tecleada, `start_url` de la PWA, link de correo/QR) el `back()` vuelve a la URL que acaba de fallar. (2) **vista de error con `x-app-layout`**: `components/layout/sidebar.blade.php` hace `Auth::user()->name` sin guardas y estas páginas las ve también un visitante sin login; peor, `Handler::renderHttpException()` envuelve el render en un `try/catch` que **en producción se come el throw** y cae en la pantalla de Symfony, mientras en local con `debug` la relanza ⇒ divergencia local/prod. (3) **la cola offline**: `resources/js/offline-queue.js` clasifica el 403 como rechazo **PERMANENTE**; si un 403 se volviera 302, el `fetch` seguiría el redirect, vería `resp.ok` del dashboard y **borraría la tanda sin haberla registrado** (el peor bug posible del cambio).
- **Solución:** un solo `render()` sobre `HttpException` con switch por `getStatusCode()` (cuando la excepción llega ahí, `prepareException()` ya convirtió TODO a `HttpException`: `AuthorizationException`, `ModelNotFoundException`, `abort()`, spatie y firma). Orden que importa: **primero** la guarda de máquina (`expectsJson() || ajax()`, + `Sec-Fetch-Mode`), después visitante, después anti-bucle (`routeIs('dashboard')`), y solo entonces el redirect **a `route('dashboard')`, jamás `back()`**, para GET. Vistas `errors/{403,404}.blade.php` en **HTML standalone** (shell propio con CSS inline, sin `@vite`, sin layout). Textos en `App\Support\AvisosError` — al usuario nunca le llega `$e->getMessage()` del framework.
- **Bloqueante que cazó el gate R-31 (4ª trampa, la más sutil):** una URI que no matchea **ninguna** ruta —el 404 más común— se resuelve **antes** del grupo `web`, así que `StartSession` no corre y **`@auth` es FALSE aunque el usuario tenga sesión**. La vista 404 condicionaba el botón «Ir al Inicio» a `@auth` ⇒ el usuario real quedaba en el callejón sin salida que el lote venía a eliminar. Y el test lo tapaba: `actingAs()` **sí** deja al usuario en el guard en memoria, así que el assert pasaba. Doble lección: (a) el botón de una página de error va **sin condición** (para un invitado, `/dashboard` lo lleva al login, que es mejor que nada); (b) `actingAs()` **no** equivale a "tener sesión" — para probar lo que ve producción en este caso hay que pedirlo como **invitado real**, y `actingAs` persiste en la instancia del test, así que el invitado va **primero**.
- **Evitar a futuro:** (1) en un handler de excepción, `back()` solo para no-GET; para GET, ruta fija. (2) Una vista de error **no** puede depender de `Auth::user()`, del layout ni del manifest de Vite: si falla, en prod vuelve la pantalla de Symfony y en local no te enteras. (3) Todo cambio que altere un status HTTP debe revisar los consumidores **de máquina** (`offline-queue.js`, buscadores con axios, endpoints de conteo, y los `<img src>` de rutas protegidas — llegan con `Sec-Fetch-Mode: no-cors`, no `navigate`): los candados viven en `ManejoErroresTest`. (4) Un test de navegación que asserte redirect describe la rama de `navigate`; si el endpoint real se consume como imagen/fetch, el test debe **mandar el header** o estará fijando un contrato que producción no cumple.

### [2026-07-24] La suite pasó de 128 MB y `php artisan test` moría antes del primer test (y la CI corre ese mismo comando)

- **Síntoma:** `php artisan test` abortaba con `Allowed memory size of 134217728 bytes exhausted ... in Filesystem.php line 123`, repetido y **sin ejecutar ni un test** (ni una línea `PASS`). Parecía un bug del código nuevo; `php -d memory_limit=512M vendor/bin/phpunit` corría perfecto (889 verdes, pico **130 MB**).
- **Causa:** el `memory_limit` del `php.ini` local es **128M** y la suite creció hasta rozarlo; el mensaje aparece en `Filesystem::get()` (leyendo un archivo cualquiera) porque la memoria ya estaba agotada, lo que apunta al lugar equivocado. Nada que ver con el cambio en curso: solo lo empujó al borde.
- **Solución:** fijar el límite **en el repo**, no en cada máquina: `<ini name="memory_limit" value="512M"/>` dentro del bloque `<php>` de `phpunit.xml`. Con eso `php artisan test` vuelve a correr (889 verdes). **Corrección del gate R-31:** esto NO "salva la CI" como se afirmó primero — en `ubuntu-latest` el SAPI cli suele traer `memory_limit=-1`, así que la línea le **pone un techo** (holgado: 512M contra un pico de 130M) en vez de levantarlo. Sirve para que el límite sea el mismo en todas las máquinas.
- **Evitar a futuro:** si `artisan test` muere sin ejecutar tests y el error habla de memoria en `Filesystem.php`, es el `memory_limit`, no el código: reproducir con `php -d memory_limit=512M vendor/bin/phpunit` para separar entorno de defecto. Y todo límite de ejecución de la suite se fija en `phpunit.xml` para que viaje con el repo. Ojo: subir el número es tapar el síntoma — antes medir el pico (`Memory:` del resumen de PHPUnit) para descartar una fuga real.
### [2026-07-24] `npm run build` con `storage/framework/views` poblado infla el CSS (+14KB de clases fantasma) — `view:clear` antes del build

- **Síntoma:** (cutover sidebar V4) el mismo `npm run build` que daba 56KB de CSS pasó a 70.9KB sin cambios de clases en los fuentes. El diff del bundle traía utilidades que ningún Blade usa.
- **Causa:** `app.css` incluye `@source '../../storage/framework/views/*.php'` — Tailwind escanea las **vistas compiladas**, que acumulan archivos de cada corrida de tests/serve local. Tokens del PHP compilado matchean como clases y entran al bundle. Es la cara inversa del gotcha [2026-06-15] (build que DROPEA clases por escanear un estado raro): el escaneo depende de un directorio transiente → el build deja de ser reproducible.
- **Solución:** `php artisan view:clear && npm run build` para el bundle que se commitea (quedó en 57.4KB, determinista). Verificado comparando `diff` de los CSS token por token.
- **Evitar a futuro:** todo build de producción que se vaya a commitear corre con las vistas compiladas limpias: `php artisan view:clear && npm run build`. Si el tamaño del CSS salta >2KB sin clases nuevas en los fuentes, sospechar de este gotcha antes que del código.

### [2026-07-24] Directiva Blade pegada a texto (`<details@if`) NO compila — `\B@` exige no-letra antes del `@`

- **Síntoma:** para dejar `<details open` contiguo (assert de forma estable) se escribió `<details@if ($abierto) open @endif ...>` → 29 tests rojos de golpe: el `@if` quedó LITERAL en el HTML de todas las páginas (la sidebar se renderiza en todas).
- **Causa:** el compilador de Blade matchea directivas con `\B@(\w+)` — entre una letra (`s` de `details`) y `@` hay word boundary, así que `\B` falla y la "directiva" no se reconoce. Pegado a comillas o `}}` sí funciona (`"@if`, `}}"@if`), pegado a texto no.
- **Solución:** para atributos booleanos contiguos usar echo ternario: `<details {{ $abierto ? 'open' : '' }} class="group">` (renderiza `<details open class` — contiguo y determinista). Ver `components/sidebar-group.blade.php`.
- **Evitar a futuro:** directiva Blade que deba quedar pegada a texto de markup → reescribir como `{{ expr ? '...' : '' }}`; reservar `@if` inline para posiciones tras comillas/espacios. Si un cambio "cosmético" de Blade rompe MUCHOS tests a la vez, buscar la directiva sin compilar en el HTML antes que revisar los tests.

### [2026-07-22] Panel Alpine abierto desde un disparador EXTERNO: el mismo click lo cierra vía `@click.outside`, y `x-transition` lo dejó pegado (+ el mapa `@js` volvió verde-engañosos los assertSee de clases)
- **Síntoma:** (P-M16V1-04, cazado en preview — los tests estaban verdes) en el modo «Personalizar» del dashboard, tocar una card abría su panel de swatches y se cerraba EN EL MISMO instante; tras quitar esa causa, el panel quedó SIEMPRE visible con estilos inline pegados (`opacity: 1; transform: scale(1)`) y el `display` de `x-show` nunca se aplicaba. Además, la suite completa reveló un assert imposible: `assertDontSee('bg-violet-100')` fallaba para un usuario sin violeta.
- **Causa:** tres capas. (1) El click que abre nace en el `<a>` de la card, FUERA del panel → burbujea al `@click.outside` del panel y lo cierra en el mismo evento (primo del gotcha info-tip [2026-07-01], que era con `@click.stop` de terceros). (2) `x-transition.opacity.duration.150ms` sobre ese `x-show` dejaba la transición a medio camino: estilos inline pegados y el toggle de `display` sin aplicar. (3) El `x-data` inyecta el mapa paleta completo vía `@js()` para el repintado optimista → TODAS las clases (`bg-violet-100`, `bg-emerald-100`, …) están SIEMPRE en el HTML de la página: cualquier `assertSee`/`assertDontSee` de una clase suelta pasa/falla por la razón equivocada.
- **Solución:** (1) en el handler del disparador: `$event.preventDefault(); $event.stopPropagation()` SOLO en modo edición — el "single-open" no depende de `click.outside` porque `abierto` es un estado único del `x-data` (abrir B cierra A solo); (2) quitar el `x-transition` (el `x-show` puro togglea `display` confiable; el motion del proyecto es sutil igual); (3) asertar la **forma contigua** que solo produce el squircle renderizado server-side (`'duration-150 bg-emerald-100'`, con `false`) + `viewData('accesos')` para los defaults. Ver `resources/views/components/dashboard/acceso.blade.php` y `tests/Feature/DashboardColoresTest.php` (`4b5a64f`).
- **Evitar a futuro:** (1) si un panel con `@click.outside` se abre desde un elemento EXTERNO a él, el click del disparador debe `stopPropagation` (o el panel debe ignorar ese origen) — el mismo evento que abre puede cerrar; (2) ante un `x-show` que "no funciona" con estilos inline `opacity/transform` pegados, sospechar del `x-transition` antes que del estado; (3) cuando una vista inyecta un **mapa de clases por `@js()`**, ninguna clase suelta sirve como assert — usar la forma contigua del markup o `viewData` (doctrina verde-engañoso [2026-07-20]).

### [2026-07-28] Un ítem de menú nuevo bajo un padre con patrón COMODÍN enciende dos filas a la vez (y ningún candado lo veía)
- **Síntoma:** al entrar el Kardex al menú (P-NAV-06), su página resaltaba **DOS** ítems de la sidebar: «Producción» (patrón `admin.produccion.*`) y «Kardex» (patrón exacto). Medido en el render real: `aria-current="page"` ×2, dos filas hermanas con clases idénticas (se lee como bug de estado) y un lector de pantalla anunciando dos «página actual».
- **Causa:** `layout.sidebar` resuelve cada ítem por separado con `request()->routeIs(...$item['activo'])` — **no hay arbitraje «gana el más específico»**, así que un comodín del padre se come la ruta del hijo y ambos dan `true`. La convención de la casa ya lo resolvía en otro lado (el ítem `listado` de ST usa la ruta EXACTA justamente para no comerse `lote`/`qr`/`informe`), pero no estaba escrita ni cubierta: `MenuPrincipalTest` solo valida la dirección directa (que un patrón matchee su PROPIA ruta), y el único candado de unicidad de la sidebar era el del acordeón (`substr_count('<details open') === 1`).
- **Solución:** enumerar los patrones del padre en vez del comodín (`admin.produccion.index`, `…dia`, `…maquina`, `…tipo`, `…sopladores`, `…soplador`, `…asignar*`, `…reporte.*`) **+ el candado que faltaba**: `SidebarTest::test_cada_ruta_del_menu_resalta_exactamente_un_item`, derivado de `MenuPrincipal`, que barre cada ítem sin parámetros y exige exactamente un `aria-current`. Falla en los DOS sentidos (2+ = comodín que se come a un hermano; 0 = ruta sin dueño) y cubre solo cada ítem futuro. Mutación verificada: con el comodín de vuelta, rojo exacto en `admin.produccion.movimientos` («2 is identical to 1»).
- **Evitar a futuro:** (1) al colgar un ítem cuya ruta cae DENTRO del patrón de otro ítem, angostar el patrón del padre — el comodín es cómodo hasta que un hijo entra al menú; (2) los ítems `cuenta.*` (dropdown del pie) no emiten `aria-current` por diseño: quedan excluidos del candado con su razón, y `items()` los incluye solo para los candados de route/permiso.

### [2026-07-28] Mutar un fix SIN COMMITEAR: el `git checkout --` de la restauración se lleva el trabajo
- **Síntoma:** tras verificar por mutación que un test discriminaba (fix en el Blade → mutación → rojo ✓), la restauración con `git checkout -- <archivo>` —la forma correcta según la bitácora [2026-07-20] para no corromper el encoding— dejó el archivo **sin el fix**: el grep del marcador devolvió 0.
- **Causa:** el fix estaba en el working tree **sin commitear**, así que `checkout --` restauró la versión del ÚLTIMO COMMIT, que no lo tenía. La mutación y el fix eran indistinguibles para git: ambos eran «cambios no commiteados».
- **Solución:** re-aplicar el fix con la herramienta de edición y, esta vez, **commitear ANTES de mutar**: así `git checkout --` restaura exactamente el fix y nada más.
- **Evitar a futuro:** el orden es **fix → test verde → COMMIT → mutar → rojo → `git checkout --`**. Y después de restaurar, verificar con un grep del marcador del fix que sigue ahí (barato y cierra el ciclo); no basta con que la suite vuelva a verde, porque un fix perdido puede dejar tests verdes por otras razones.

### [2026-07-21] Fix en territorio ajeno con churn activo: el fix viaja por el DUEÑO; la flota lo fija con tests que nacen verdes
- **Síntoma:** un fix de 1-2 líneas en la agenda (territorio de Marcos, 4 features en 24h) murió DOS veces: la rama editaba un Blade que Marcos borró/reescribió antes del merge — colisión perpetua.
- **Causa:** perseguir con ramas de la flota un archivo que otro dev está reescribiendo activamente; la rama siempre llega tarde al merge.
- **Solución:** coreografía canal-directo + tests-flota (decisión del dueño 21-07, cierre del PLAN-TIMEZONE): el dueño le pasa las líneas exactas al dev del territorio (las aplicó como `daf948d`), y la flota sube una rama **SOLO-tests** (cero archivos del territorio) **gated**: los tests deben NACER VERDES — verde = el fix llegó y queda blindado contra regresiones; rojo = confirmación de que aún no llega, la rama espera. Cero colisiones.
- **Evitar a futuro:** ante churn activo en el territorio del fix, no insistir con la 3ª rama: proponer las líneas exactas en el parte y pedir el canal directo; el valor de la flota queda en los tests de frontera (inmunes al churn por construcción).

### [2026-07-20] Render en hora chilena: macro `enChile()` SOLO para timestamps con hora — jamás sobre casts `date` (y los `diffForHumans` no se tocan)
- **Síntoma:** (P-TZ-02) los timestamps absolutos de historiales/auditoría/tandas mostraban la hora UTC del storage (+4h): el QA del 15-07 leyó «15:45» cuando en Chile eran las 11:45.
- **Causa:** el storage es UTC a propósito (opción C del PLAN-TIMEZONE) y ningún Blade convertía al mostrar — no existía helper central de render.
- **Solución:** `Carbon::macro('enChile')` en `AppServiceProvider::boot()` → `->copy()->tz(config('daligo.tz_negocio'))` (el `copy()` importa: Carbon es mutable y un `->tz()` directo correría también todo lo que se calcule después sobre ese instante). Aplicado a los 8 formatos absolutos inventariados en `PLAN-TIMEZONE.md` §1b + el payload de prueba de `NotificacionController`. Tests en `RenderEnChileTest` (conversión conocida, DST de verano, E2E con `assertSee('11:45')`+`assertDontSee('15:45')`, relativos comparados contra el valor computado — independiente del locale).
- **Evitar a futuro:** (1) todo formato absoluto **con hora** que vea el usuario lleva `->enChile()` antes del `format()`; (2) **JAMÁS** sobre casts `date` puros (`fecha`, `fecha_ingreso`…): su medianoche UTC retrocedería al día ANTERIOR al convertirla; (3) los `diffForHumans` no se tocan — un delta entre dos instantes no depende del tz de render; (4) los nombres de archivo (`Ymd_His` de los CSV) y el motor siguen en UTC.

### [2026-07-20] Editar archivos del repo con PowerShell 5.1 (`Get-Content | Set-Content`) corrompe el UTF-8 (mojibake en los Blades)
- **Síntoma:** tras una mutación de prueba hecha con `(Get-Content -Raw) -replace ... | Set-Content -Encoding utf8`, el Blade quedó con mojibake (`tÃ©cnico`, `Â· HOY`): todos los acentos doble-codificados.
- **Causa:** en Windows PowerShell 5.1, `Get-Content` **sin `-Encoding utf8`** lee el archivo UTF-8 como ANSI (Windows-1252); al re-escribir con `-Encoding utf8`, los caracteres mal interpretados se codifican de nuevo → doble encoding. El archivo original estaba bien; lo rompió el round-trip de lectura.
- **Solución:** `git checkout -- <archivo>` para restaurar el original y re-aplicar el cambio con la herramienta de edición (que preserva encoding). La mutación de prueba (doctrina verde-engañoso) igual cumplió su propósito: el test se puso rojo antes de restaurar.
- **Evitar a futuro:** no editar archivos del repo vía `Set-Content`/`Out-File`/`-replace` en PS 5.1 (haría falta `-Encoding utf8` en AMBOS extremos y aún así reordena finales de línea); las mutaciones temporales de la doctrina se revierten SIEMPRE con `git checkout --`, nunca des-mutando a mano.
- **Ampliación [2026-07-27] — el operador `>` es la misma trampa, y en `manifest.json` es FATAL:** resolviendo conflictos del lote NOTIF-1 se usó `git show origin/main:<archivo> > <archivo>` (la forma canónica «tomar el lado de main») y los 4 archivos quedaron **UTF-8 con BOM y CRLF** — `>` en PS 5.1 es `Out-File`, que antepone BOM. En un Blade el BOM se emite antes del HTML (rompe headers/redirects); en **`public/build/manifest.json` el BOM hace fallar el `json_decode` de Vite → la app queda sin CSS/JS en runtime**, y el `git status` no lo delata (solo avisa del CRLF). **La forma correcta de tomar el lado de main es `git checkout origin/main -- <archivo>`** (escribe git, no el shell) y luego re-aplicar los injertos con la herramienta de edición. Verificación barata: `file -b <archivo>` en Bash — debe decir `UTF-8 text` **sin** `(with BOM)`.

### [2026-07-20] El "hoy" operativo es el DÍA DE NEGOCIO chileno, no `now()->toDateString()` — y la suite necesita reloj determinista
- **Síntoma:** (P-TZ-01, destapado por el inventario del PLAN-TIMEZONE) `app.timezone` es UTC → desde las 20:00/21:00 de Chile, `now()->toDateString()` ya es MAÑANA: el soplador nocturno no veía sus producciones, la cola del jefe se vaciaba, el pulso marcaba ceros y la visita pública RECHAZABA el "hoy" del cliente (`after_or_equal:today`).
- **Causa:** todo "hoy" operativo se derivaba del reloj UTC del servidor. La familia es ancha: `toDateString()`, prefills `format('Y-m-d')`, `isToday()`, cabeceras `now()->translatedFormat`, la regla `after_or_equal:today` y fallbacks de scopes.
- **Solución:** helper central `App\Support\FechaNegocio` (`hoy()`/`esHoy()`/`ahora()`, tz en `config/daligo.php::tz_negocio`) en ~25 sitios; **`app.timezone` sigue UTC a propósito** (storage y motor intactos — flipearlo rompe los `diffForHumans` históricos y corre el backoff en el cutover; ver `docs/planes/PLAN-TIMEZONE.md`). Además `tests/TestCase` congela TODA la suite a **mediodía UTC** (misma fecha en ambas zonas): sin eso, una corrida de CI entre 00:00-04:00 UTC cruzaba el borde del día y los fixtures "de hoy" fallaban (flaky por reloj); un test que necesita otro instante viaja encima con `travelTo()` (así lo hace `FechaNegocioTest`, la batería de frontera nocturna).
- **Evitar a futuro:** (1) NUNCA `now()` directo para un "hoy" que ve el usuario u opera el negocio — siempre `FechaNegocio`; el gate greppea la familia completa (`toDateString|format('Y-m-d'|isToday|now()->translatedFormat|after_or_equal:today`), no un solo patrón; (2) los deltas del motor (`subMinutes`, backoff, colas) se quedan en UTC — un diff ahí es señal de error; (3) el render de timestamps absolutos (+4h) es P-TZ-02, capa aparte.

### [2026-07-20] `assertSee` de markup pegado (`'>X<'`) puede pasar por la razón EQUIVOCADA (verde-engañoso)
- **Síntoma:** un test del lote QA verificaba el nav-link de la bandeja con `assertSee('>Aprobaciones<', false)` y la suite estaba VERDE — pero el nav-link jamás produce esa cadena (`<x-nav-link>` renderiza el slot con saltos de línea e indentación). El Director lo diagnosticó como "falla determinista"; en realidad era peor: **verde-engañoso**.
- **Causa:** la cadena pegada `>Aprobaciones<` SÍ existía en el HTML… aportada por OTRA superficie de la misma página (el chip del zócalo del dashboard M16, `…">{{ $item['label'] }}</a>`). El assert pasaba sin verificar el elemento que su comentario decía verificar. Un verde que pasa por la razón equivocada no protege nada y esconde la regresión el día que esa otra superficie cambie.
- **Solución:** asertar por **RUTA o marcador estable**, nunca por la forma pegada del HTML: `assertSee(route('aprobaciones.index'), false)` para la bandeja + `assertSee('Historial de aprobaciones')` para el label (commit `ad750a1`; doctrina adoptada por el Director en `bab5da2`).
- **Evitar a futuro:** (1) ante todo `assertSee` de fragmento HTML preguntarse: *¿puede OTRA superficie de la página satisfacer esta cadena?* (nav, zócalo, dropdown, sr-only — el gotcha del sr-only «(0 sin leer)» del 2026-07-14 es el mismo patrón); (2) preferir rutas, `aria-label` o el texto exacto de un label único; (3) para detectar un verde-engañoso: **mutar** — quitar el elemento objetivo y confirmar que el test se pone rojo; (4) los tests de asserts NEGATIVOS (`assertDontSee` de una cadena que nunca existió) pasan trivial: son los primeros sospechosos.

### [2026-07-13] Repo PÚBLICO: un incidente de seguridad se documenta REDACTADO desde el primer commit
- **Síntoma:** al archivar el expediente I-05 (servidor HostGator comprometido con webshells) en `docs/qa/INFRA/`, el detalle sensible (rutas de los webshells + crontab + detalle de llaves) alcanzó la rama pública antes de notarlo — en repo público (D-012) eso es un mapa útil para un atacante.
- **Causa:** se documentó el incidente con TODO el detalle operativo directo en un doc del repo, sin recordar que el repo es **público** (D-012).
- **Solución:** decisión del Director — **NO reescribir historia** (rompe todos los clones de la flota y no des-publica lo ya clonado); **commit REDACTADO encima**: dejar veredicto + datación + "comprometido era-2022, remediación asignada a [sysadmin], superficie de la app limpia" y **quitar rutas/patrón/crontab/blobs**. El informe operativo completo viaja al responsable de infra por **canal privado** (fuera del repo). Ver `docs/qa/INFRA/2026-07-08--INFRA--servidor-comprometido-i05.md` (versión redactada).
- **Evitar a futuro:** en repo público, **todo incidente de seguridad se archiva REDACTADO desde el primer commit** (veredicto/impacto sí; rutas/comandos/credenciales/mapa de atacante no) y el detalle va por canal privado. Vale también para logs de GitHub Actions (públicos).

### [2026-07-13] Factory con `estado` aleatorio → un test que asertaba el estado era FLAKY (main verde/rojo entre pushes idénticos)
- **Síntoma:** la CI de main alternaba verde y rojo entre pushes de solo-docs (idénticos en código) — señal clásica de test flaky. El culpable: `ServicioTecnicoManagementTest::test_reparado_exige_diagnostico_final` (M12). Fallaba en `assertNotSame('reparado', $orden->fresh()->estado)` de forma intermitente.
- **Causa:** `OrdenServicioFactory` asigna `estado => fake()->randomElement(OrdenServicio::ESTADOS)` (ALEATORIO, 7 estados). El test creaba la orden **sin fijar `estado`**, intentaba `PUT` a `reparado` con `causa_falla=''` (la validación lo rechaza bien → no hay update), y luego asertaba que el estado NO fuera `reparado`. Cuando el estado aleatorio inicial caía en `reparado` por azar (~1/7 de las corridas), la orden YA estaba en `reparado` antes del PUT → el assert fallaba. El código de producción (la validación de `causa_falla`) estaba CORRECTO; el defecto era solo del test.
- **Solución:** fijar el `estado` inicial en el `create()` a uno ≠ del objetivo — `OrdenServicio::factory()->create(['facturacion' => 'reparacion', 'estado' => 'en_revision'])` (mismo patrón que el test hermano del descuento). Aplicado a los dos tests con el patrón (`reparado` y `sin_solucion`). Verificación anti-flaky: **20 corridas seguidas en procesos frescos → 20/20 verdes** (sin el fix, `(6/7)^20 ≈ 4.6%` de pasar las 20 → el verde 20/20 confirma el arreglo). NO se tocó la factory ni el controller (arreglar la factory para no-aleatoria es refactor de M12). Fix por autorización puntual del dueño (excepción de territorio, I-06).
- **Evitar a futuro:** una factory con un campo **aleatorio** (`fake()->randomElement`, `fake()->boolean`, etc.) obliga a que **todo test que asserte sobre ese campo lo FIJE en el `create()`** — nunca asumas el valor por defecto de la factory. Para cazar un flaky: correr el test 20+ veces en procesos frescos (cada `php artisan test` re-siembra faker), no una sola vez.

### [2026-07-13] Cambiar `manifest.json` desalinea `PwaTest` → main rojo (la CI es la red que lo atrapa)
- **Síntoma:** la CI de main (Actions "Tests") quedó en FAILURE tras el merge del fix `fix/pwa-start-url-403` (stream M12): `PwaTest::test_manifest_es_valido_e_instalable` asertaba `start_url === '/produccion/mi-reporte'` pero el manifest ya decía `/dashboard`. El deploy no se ve afectado (Tests y Deploy son workflows distintos) pero main queda rojo y cualquier otro stream que mergee arrastra el rojo.
- **Causa:** `PwaTest` fija el **contrato del `manifest.json`** con `assertSame` sobre `start_url` (parte del spike PWA, P-SPK-01). Un cambio LEGÍTIMO del manifest hecho por otro stream (mover el `start_url` a `/dashboard` porque a los no-sopladores la app instalada les abría con 403 en `/produccion/mi-reporte`) desalinea la aserción. El test hace exactamente su trabajo — avisar que el contrato cambió —, pero el stream que tocó el manifest no actualizó el test en el mismo push.
- **Solución:** alinear la aserción a `/dashboard` (hotfix `fix(pwa): alinear PwaTest…`, main). El test golden-hash de `offline.blade.php` quedó intacto (ese Blade no cambió). Sin build (test+doc).
- **Evitar a futuro:** tocar `public/manifest.json` (o `public/sw.js`) ⇒ revisar `tests/Feature/PwaTest.php` en el MISMO push — el contrato del manifest/SW vive ahí. Entre streams paralelos, quien cambia un archivo con contrato en test alinea el test o deja main rojo para el resto.

### [2026-07-07] Prompts de delegación con `tinker --execute`: `$m` dentro de comillas dobles se lo come el shell
- **Síntoma:** (cazado en revisión pre-despacho, no llegó a ejecutarse) el prompt de P-M15-10 pedía a la IA-cPanel correr `php artisan tinker --execute="Mail::raw('...', fn($m) => $m->to([...])...)"`. Ejecutado tal cual en bash, habría fallado con un syntax error de PHP incomprensible para el operador.
- **Causa:** dentro de **comillas dobles**, bash expande `$m` como variable de shell (vacía) ANTES de que PHP lo vea → el código llega como `fn() => ->to(...)`. El error aparece "dentro de tinker", lejos de la causa real (el quoting), así que el operador remoto no puede diagnosticarlo. Segundo bloqueante de la misma revisión: un paso condicional cuya condición dependía de un paso POSTERIOR (resetear la clave "si la autenticación falla", validada recién en el paso 9) — lógica circular imposible de ejecutar en orden; se volvió incondicional.
- **Solución:** revisión del Director ANTES del despacho (protocolo de flota) corrigió ambos. Regla para `--execute`: envolver el código en **comillas simples** (`--execute='...'`) o escapar cada variable PHP (`\$m`); y en prompts para operadores remotos, ninguna condición de un paso puede depender de un paso posterior.
- **Evitar a futuro:** checklist al redactar prompts de delegación con comandos: (1) todo `tinker --execute` / heredoc con `$variables` PHP se revisa contra expansión de shell (comillas simples por defecto); (2) verificar que las condiciones de los pasos solo miren pasos ANTERIORES; (3) la revisión pre-despacho del Director no es burocracia — cazó 2 bloqueantes que el autor no vio.

### [2026-07-07] Merge entre streams: build post-merge falla si main trajo una dependencia npm nueva (`qrcode`)
- **Síntoma:** en el merge de M15 (rama `feature/m15-notificaciones` ← `origin/main`), el `npm run build` obligatorio post-merge falló: `[vite]: Rollup failed to resolve import "qrcode" from ".../resources/js/app.js"`. Además, tras el `npm install` que lo arregla, `git status` mostró `package-lock.json` modificado sin haberlo tocado.
- **Causa:** (1) otro stream (M12) agregó la dependencia `qrcode` a `package.json`; el merge trae el `package.json` nuevo pero `node_modules` local queda stale → Rollup no resuelve el import dinámico. (2) `npm install` reescribe el campo `name` del `package-lock.json` con el **nombre de la carpeta local** (`DaliGo-M15` en vez de `DaliGo`) cuando `package.json` no fija `name` — ruido que NO debe viajar al repo (pisaría el lock de los demás).
- **Solución:** tras todo merge que toque `package.json`/`package-lock.json`: **`npm install` ANTES de `npm run build`**; y después revisar `git diff package-lock.json` — si el único cambio es el `name`, descartarlo (`git checkout -- package-lock.json`). Merge `00297d5`: con eso el build generó `app-CXFiiPSt.css` con las 4 clases críticas (grep literal `lg\:flex`/`lg\:hidden`/`.w-80`/`bg-white\/60`) y la suite quedó 444 verdes.
- **Evitar a futuro:** checklist de merge entre streams: si `git diff --name-only` del merge incluye `package.json` → `npm install` primero, build después, y vigilar el `name` del lockfile. Los clones de la flota usan carpetas con sufijo (`DaliGo-M15`), así que este ruido aparecerá en cada stream.

### [2026-07-07] HostGator reescribe los crons <15 min: no agendar NADA por-minuto — grilla `*/15` alineada (I-01)
- **Síntoma:** las syncs de Bsale murieron OTRA VEZ sin error visible: `sync-stock/clients/prices` sin corridas desde el 03-07 (solo `sync-catalog` seguía intentando a :00). El fix del corrector del 02-07 (`* * * * *`) había funcionado <24h.
- **Causa:** TERCERA reescritura no registrada del crontab (03-07 entre 06:41 y 06:50 CDT): `schedule:run` → `*/19` (dispara :00/:19/:38/:57 → :20/:40/:50 no coinciden jamás) y `queue:work` → `*/15`. Las 3 reescrituras observadas dejaron SIEMPRE intervalos ≥15 min → **automatización de HostGator que estrangula crons por-minuto** en plan compartido (cPanel no guarda historial de ediciones de cron, verificado). Pelear reponiendo `* * * * *` es churn.
- **Solución:** **modo compatibilidad** (dictado del Director, 07-07): (1) crontab por SSH con AMBAS líneas `*/15` (`schedule:run` y `queue:work --stop-when-empty --max-time=840`); (2) `routes/console.php` re-agenda las syncs EXACTO en la grilla :00/:15/:30/:45 (`hourly()`/`hourlyAt(15|30|45)`); (3) test `ScheduleBsaleTest::test_las_syncs_van_en_la_grilla_de_15` fija la grilla. Evidencia completa (timeline de las 3 reescrituras, before/after): `docs/qa/INFRA/2026-07-07--INFRA--i01-cierre-modo-compatibilidad.md`. Costo aceptado: latencia de cola/notificaciones ≤15 min.
- **Evitar a futuro:** (1) en este hosting **nada se agenda por-minuto ni fuera de la grilla `*/15`** — toda tarea nueva en `routes/console.php` cae en :00/:15/:30/:45 o no correrá; (2) el crontab NO es configuración estable: ante cualquier "no corrió", PRIMERO `crontab -l` textual + frescura de los logs (la señal barata: última línea de cada sync en `laravel.log`); (3) supersede el punto (1) del "Evitar a futuro" del [2026-07-02].

### [2026-07-06] Ingreso público por QR: dependencia front nueva sin romper el deploy + ruta sin-auth que escribe (patrón)
- **Síntoma:** el piloto de ingreso a ST por QR (P-M12-01) necesitaba dos cosas nuevas y riesgosas para este hosting: (1) **generar un QR** (no había librería) y (2) una **ruta pública sin login que ESCRIBE en la BD** (el cliente crea la orden desde su celular). El servidor **no tiene Node** y el `deploy.sh` del server puede estar congelado (skip-worktree, ver `.github/workflows/deploy.yml`) → no se puede confiar en que instale algo nuevo en cada deploy.
- **Causa:** una dependencia **composer** nueva depende de que `deploy.sh` corra `composer install` (que podría estar congelado) → riesgo de clase inexistente en prod. Y un endpoint público que inserta filas es superficie de abuso (spam/bots) y de fuga (enumerar folios ajenos).
- **Solución:** (1) **QR con `qrcode` (npm) + import dinámico** en `app.js` (`import('qrcode')` SOLO cuando hay `canvas[data-qr]`) → Vite lo separa en un chunk aparte; se **commitea `public/build`** (que ES lo que sirve prod, sin Node en el server) → cero dependencia nueva de servidor, cero cambio en `deploy.sh`. Interop CJS robusto: `const QRCode = mod.default ?? mod`. (2) **Ruta pública segura:** el link del QR es **firmado** (`URL::signedRoute` con la sucursal embebida; middleware `signed` → 403 si se altera), grupo con `throttle:6,1`, **honeypot** oculto en el form, el flujo **solo escribe** (nunca lista/lee otras órdenes) y la pantalla de éxito es otro `signedRoute` (no se pueden enumerar folios). La orden nace real (`fuente='qr'`) pero `confirmada_at=null` hasta que el encargado la valida (con `lockForUpdate` anti doble-correo). 391 tests verdes.
- **Evitar a futuro:** (1) capacidades front nuevas en este hosting sin Node → **npm + build commiteado** (nunca depender del `deploy.sh` del server para deps); libs pesadas por **import dinámico** para no inflar el bundle global. (2) toda ruta pública que muta datos: **link firmado + throttle + honeypot + solo-escribe + éxito por signedRoute**; endurecer el POST con token firmado queda para F3 (P-F3-01). (3) `deploy.sh` NO compila assets → si tocaste Blade/CSS/JS, `npm run build` y commitea `public/build` sí o sí.

### [2026-07-06] Registrar ingreso (Servicio Técnico) rechazaba toda «Reparación»: `Rule::in` sin `nullable` sobre un select oculto
- **Síntoma:** al enviar el formulario de ingreso con Condición = «Reparación» desde el navegador, la validación fallaba con "El campo garantia doc tipo seleccionado no es válido", aunque el bloque de garantía estaba oculto. Los tests estaban verdes.
- **Causa:** el bloque de garantía se oculta con `x-show` (Alpine), pero sus campos **igual se envían**: `garantia_doc_tipo=""` llega al servidor y `ConvertEmptyStringsToNull` lo vuelve `null`. La regla era `[Rule::requiredIf($esGarantia), Rule::in(...)]` **sin `nullable`**, y `Rule::in` sí valida los `null` → rechazo. Los tests no lo pillaban porque su payload **omite la clave por completo** (campo ausente no se valida), cosa que un navegador real nunca hace.
- **Solución:** agregar `'nullable'` a la regla de `garantia_doc_tipo` en `ServicioTecnicoController::validateData` (igual que ya tenían `garantia_doc_numero`/`garantia_doc_fecha`) + test de regresión `test_reparacion_acepta_campos_de_garantia_vacios` que envía las claves de garantía presentes pero vacías. Detectado al verificar en preview el nuevo campo `cliente_telefono`.
- **Evitar a futuro:** todo campo condicional oculto con `x-show`/`x-cloak` se envía igual; sus reglas deben llevar `nullable` junto al `requiredIf`. En los tests de formularios, simular al navegador: incluir las claves condicionales como `""`, no omitirlas.

### [2026-07-02] Cola offline de tandas (P-SPK-02): idempotencia por UUID + CSRF fresco + errores clasificados
- **Síntoma:** (spike) el soplador en planta sin señal necesita registrar tandas y que se sincronicen solas al volver la conexión, SIN duplicados. Tres trampas clásicas de una cola offline en app Blade con sesión: (1) reintento del drenado duplica la tanda; (2) el token CSRF serializado con el form queda stale tras un rato offline (419); (3) errores permanentes (máquina desactivada mientras estaba offline → 422) vs transitorios (red/419/5xx) mal manejados → borrado silencioso o loop infinito.
- **Causa/decisiones (validadas adversarialmente ANTES de codear):** (1) idempotencia = UUID generado en el cliente (`crypto.randomUUID`) + columna `cliente_uuid` con **unique compuesto `[reporte_id, cliente_uuid]`** (36 char < 191; MySQL 5.7 y SQLite permiten múltiples NULL en unique → el camino nativo sin uuid no choca) + check dentro del `lockForUpdate` (defensa en profundidad: check + constraint). (2) NO exceptuar CSRF (el endpoint muta inventario tras auth+permission+ownership): el drenado NO serializa `_token`; lee el token FRESCO del `<meta name=csrf-token>` de la página viva y lo manda en header `X-CSRF-TOKEN`. (3) `registroStore` responde JSON si `expectsJson()` (el drenado manda `Accept: json` → 422/403 llegan como status crudo, no como el redirect del handler 419); las reglas de negocio se lanzan como `ValidationException` (no `back()->withErrors`) para que en JSON den 422 real y en web el mismo redirect-con-errores de siempre. El drenado clasifica: 2xx→borrar, 422/403→permanente (marca el item, no reintenta), 419/5xx/red→transitorio (reintenta, con tope `MAX_INTENTOS`).
- **Solución:** migración `…_add_cliente_uuid_a_produccion_registros`; `resources/js/offline-queue.js` (IndexedDB `encolar/drenar/pendientes` con guard de reentrada `drenando` y reload SOLO tras drenar con éxito para reconciliar UI con la fuente de verdad del server); integración en el `x-data` del form (`agregarTanda` encola si `!$store.red.online`, feedback optimista acumulando ANTES de resetear, contador "N sin conexión"). El SW de P-SPK-01 deja pasar los POST (passthrough non-GET). 4 tests de idempotencia (incluye el 422 de máquina inválida) → 372 verdes. Verificado E2E en preview: 2 tandas offline → cola en IndexedDB (sin `_token`) → volver online → drenan → reload → 2 registros reales sin duplicar, motivos preservados.
- **Evitar a futuro:** toda cola offline usa este patrón — UUID de cliente + unique compuesto + check bajo lock; token CSRF fresco del `<meta>` (nunca serializar `_token`); `ValidationException` para respuestas que sirvan a web y a fetch por igual; clasificar transitorio/permanente y NUNCA borrar en silencio. iOS no tiene Background Sync → drenar en `online` + `load`, no con sync events.

### [2026-07-02] Spike PWA (P-SPK-01): las 5 reglas del service worker de DaliGo (footguns evitados por diseño)
- **Síntoma:** (spike, no bug) primer service worker del proyecto (`public/sw.js` + `manifest.json` + `/offline`). Los SW en una app Blade con sesión/CSRF tienen footguns que rompen el login sin dar error visible; se validó el diseño adversarialmente ANTES de implementar y estas reglas quedan como contrato para P-SPK-02 y M08.
- **Causa (los footguns):** (1) una navegación con redirect (el 302 del middleware `auth`, login/logout) llega al SW como `opaqueredirect` con `status 0, ok:false` → un fallback "por status" mostraría la página offline EN CADA LOGIN; (2) sin `"scope":"/"` explícito, el scope por defecto del manifest es el directorio del `start_url` (`/produccion/`) → login/logout quedarían "fuera de la app" instalada; (3) envolver el passthrough en `event.respondWith(fetch(req))` rompe redirects/streaming — lo no manejado se deja pasar con `return` temprano; (4) el SW se actualiza por byte-diff de `sw.js`: cambiar el Blade de `/offline` NO lo gatilla → quedaría precacheado para siempre; (5) un SW registrado en `localhost:8001` persiste POR ORIGEN y contamina cualquier proyecto futuro servido en ese puerto.
- **Solución (las 5 reglas, ya en el código):** (1) fallback offline SOLO en el `catch` del fetch, jamás por `response.ok`; (2) manifest con `"scope":"/"` + `"id":"/"`; (3) passthrough sin respondWith para non-GET/cross-origin/`/up`; assets `/build/*` cache-first en cache aparte con poda (hashes inmutables); JAMÁS cachear HTML autenticado; (4) regla operativa: **tocar `offline.blade.php` exige bump de `CACHE` en `public/sw.js`** (comentarios espejo en ambos archivos); (5) registro con guard de hostname (en localhost solo con `localStorage.daligoSW='1'`) y `updateViaCache:'none'`. Extra iOS: `apple-touch-icon` aparte (ignora los icons del manifest) y **no existe Background Sync** → la cola offline de P-SPK-02 se vacía en el evento `online`/carga de página, no con `sync` events. Verificado en preview: SW activo scope `/`, `/offline` precacheada, assets en runtime cache, indicador `<x-produccion.indicador-red>` reacciona a offline/online, login intacto. 368 tests.
- **Evitar a futuro:** todo cambio futuro al SW pasa por estas 5 reglas; el patrón de indicador de red es `Alpine.store('red')` (declarado ANTES de `Alpine.start()` o `$store.red` llega undefined a los `x-show`).

### [2026-07-02] Panel del jefe: alerta "Por aprobar" contaba reportes que la cola no mostraba (alerta sin destino)
- **Síntoma:** en staging el panel decía "1 Por aprobar" pero "Cola de reportes · hoy: 0 reportes" (lo notó el dueño). El jefe no tenía dónde encontrar ni aprobar ese reporte desde el panel.
- **Causa:** las alertas `porAprobar`/`devueltos` son **globales** (cuentan todas las fechas — diseño intencional: "señales de ahora"), pero la única lista del panel era `delDia($hoy)`. Un reporte enviado de un día anterior (aquí uno del 01/07 nunca aprobado) quedaba contado pero **invisible**: la alerta enlazaba a `#cola`, donde no estaba. El patrón correcto ya existía en la app — la pantalla del soplador lista sus "devueltos de OTROS días" aparte — pero el panel del jefe no lo replicaba.
- **Solución:** sección **"Pendientes de otros días"** en el panel (`ProduccionController::index` → `pendientesOtrosDias`: enviados/devueltos con `whereDate('fecha','!=',$hoy)`), renderizada solo si no está vacía, ARRIBA de la cola de hoy y dentro del mismo ancla `#cola`; cada fila muestra su fecha. La fila de reporte se extrajo al partial `admin/produccion/partials/_fila-reporte.blade.php` (flag `$mostrarFecha`) y la usan ambas listas (DRY). Info-tips de las alertas aclaran que cuentan todas las fechas. Test `test_panel_lista_pendientes_de_otros_dias`. Verificado en preview: enviado de ayer visible con fecha → aprobado desde ahí → "Todo al día". 362 tests verdes.
- **Evitar a futuro:** (1) toda **alerta contadora debe tener una superficie donde ver y actuar** sus ítems con el mismo filtro que el conteo — si la alerta es global y la lista es de hoy, hay ítems fantasma; (2) al depurar "no aparece X" en el DOM con `innerText`, ojo con `text-transform: uppercase` — `innerText` devuelve el texto YA transformado (buscar case-insensitive o en `innerHTML`).

### [2026-07-02] Auditoría E2E de M11: el gotcha del `whereBetween` con fecha casteada REINCIDIÓ en el historial del soplador
- **Síntoma:** auditoría de calidad pre-demo del flujo completo de producción (pedida por el dueño; el módulo se construyó en parte con un modelo de IA inferior). El historial del soplador (`sopladorHistorial`) filtrado a un rango de UN día (`?desde=hoy&hasta=hoy`) salía **vacío en local/SQLite** aunque el reporte existiera; en MySQL prod funcionaba → bug silencioso dependiente de entorno, invisible hasta filtrar el borde.
- **Causa:** `whereBetween('fecha', [$desde, $hasta])` sobre la columna `fecha` casteada `date` — la **misma causa raíz de la entrada [2026-07-01]** ("panel del jefe"): en SQLite el valor se guarda `"Y-m-d 00:00:00"` y el borde superior del between compara por string (`'2026-07-02 00:00:00' > '2026-07-02'`). El fix del panel se aplicó solo en `index()`; `sopladorHistorial()` conservó el patrón prohibido. Además la auditoría encontró 3 endpoints del jefe con **check-then-act sin lock** (`devolver`, `ajustar`, `destroyReporte`) — la regla de la entrada [2026-06-30] (lock sobre la fila ancla) se aplicó a `aprobar()` pero no al resto: una carrera aprobar∥devolver podía dejar un reporte DEVUELTO con kardex ya generado (y al re-aprobar, el guard de idempotencia saltaba la regeneración → kardex desincronizado de las tandas finales).
- **Solución:** (1) `sopladorHistorial` → `whereDate('fecha','>=',…)->whereDate('fecha','<=',…)` + test `test_historial_del_soplador_incluye_el_dia_hasta` (verificado que FALLA con el código viejo). (2) `devolver`/`ajustar`/`destroyReporte` → `DB::transaction` + `lockForUpdate` + re-check del guard, espejando el idioma de `aprobar()`. (3) `max:100000` anti-dedazo en asignadas/cantidades (asignar, ajustar, tanda del soplador) + 2 tests. (4) Hint stale de `asignar.blade.php` corregido (decía "se actualiza la cantidad", falso desde el 2026-06-30). 361 tests verdes; demo E2E local verificada (asignar→tandas→enviar→aprobar→kardex 470=450+10+10→historial). De paso se descartaron por verificación 8 falsas alarmas de los agentes exploradores (p. ej. "aprobar sin lock" —lo tiene—, "orWhere se fuga del whereHas" —Laravel 12 agrupa, verificado con `toSql()`—, "kardex duplicado en devolver→re-aprobar" —imposible por guard de estado).
- **Evitar a futuro:** (1) al corregir un gotcha, **grepear el patrón en TODO el módulo** (`grep whereBetween.*fecha`), no solo el archivo del síntoma — aquí el mismo bug sobrevivió 1 día en otro método; (2) la regla del lock aplica a TODOS los endpoints que mutan el mismo agregado, no solo al "principal" (devolver/ajustar/destroy también compiten con aprobar/registrar); (3) en auditorías con agentes/IA, **verificar cada hallazgo contra el código o empíricamente antes de corregir**: 8 de 13 sospechas eran falsas alarmas.

### [2026-07-02] El cron del scheduler NUNCA fue por-minuto: `bsale:sync-stock` (:50) jamás corrió en producción
- **Síntoma:** ninguno visible — ese es el problema. Las syncs de catálogo/clientes/precios corrían "normal", pero el espejo de stock no se actualizaba por cron y nadie lo notó (sin errores en logs: la tarea simplemente nunca era due).
- **Causa:** en cPanel había DOS crons de `schedule:run` con frecuencias `*/20` y `*/15` — **ninguno `* * * * *`**. Laravel evalúa qué tareas "tocan" en el minuto exacto en que corre `schedule:run`: `*/20` coincide con :00/:20/:40 (por eso catálogo/clientes/precios sí corrían — pura coincidencia) y `*/15` con :00/:15/:30/:45 — **el minuto :50 (stock) no coincide jamás con ninguno**. Además la doc (HANDOFF §9) describía un estado distinto del real (`*/20` + `* * * * *`): nadie había verificado el crontab de verdad.
- **Solución:** eliminar ambos crons y dejar UNO: `* * * * * /opt/cpanel/ea-php83/root/usr/bin/php /home4/impdali/daligo/artisan schedule:run >> /dev/null 2>&1` (vía IA de cPanel, 2026-07-02). Verificado: `schedule:list` muestra las 4 syncs y `bsale:sync-stock` corrió a las :50 por primera vez (16 bodegas, 28.350 stocks actualizados, 0 errores). Evidencia: `docs/qa/INFRA/2026-07-02--INFRA--cron-deploysh-infra.md`.
- **Evitar a futuro:** **[Superada 2026-07-07, I-01]** el punto (1) quedó invertido: HostGator NO tolera crons por-minuto (los reescribe) → rige la grilla `*/15` de la entrada [2026-07-07]. Siguen vigentes: (2) al agendar una tarea nueva en `routes/console.php`, confirmar el crontab textual del servidor; (3) no documentar el estado de infra "de memoria": pedir el valor textual al servidor (así se descubrió esto).

### [2026-07-01] Patrón "señalar el control" ante errores + gotcha del `animation` shorthand
- **Síntoma:** (mejora, no bug) el jefe pidió adaptar un patrón de UX visto en un video: al bloquear una acción por una precondición, en vez de un texto rojo lejano, **sacudir el control exacto** que falta y llevar la mirada ahí. DaliGo ya sacudía solo el **texto** del error (`<x-input-error>` trae `dg-shake`) y tras un round-trip; en móvil el texto solía quedar bajo el fold. Al implementarlo aparecieron dos escollos: (a) dos animaciones sobre el mismo elemento y (b) cómo cubrir tanto lo client-side como todos los forms.
- **Causa:** (a) `dg-shake` y una clase nueva de "anillo" no pueden coexistir como **dos clases separadas** sobre el mismo elemento: ambas setean la **shorthand `animation`**, así que la de mayor especificidad/orden **pisa** a la otra y solo corre una. (b) La mayoría de validaciones de DaliGo son server-side (recargan); el soplador (`mi-reporte.blade.php`) ya tenía estado Alpine y colapsables que se auto-abren ante error, pero el resto de forms no.
- **Solución:** primitivo `window.dgDestacar(el, {ring})` + magic `$destacar` en [resources/js/app.js](resources/js/app.js): `scrollIntoView` (con `behavior:'auto'` si `prefers-reduced-motion`) + reinicia la animación (`classList.remove`→`void offsetWidth`→`add`). `ring:true` usa **`.dg-destacado`**, que corre **las dos animaciones en un solo `animation`** (`dg-shake 0.38s, dg-destacar-ring 1.4s`) para evitar el pisado; `ring:false` reusa `.dg-shake` (para el texto). El anillo (`box-shadow` rojo que se desvanece) se limpia con un `setTimeout(1400)` (box-shadow no crea capa de composición como `filter`/`transform`, así que no repite el bug de `dg-enter`). Dos frentes: (1) **soplador** — los dos `x-on:submit` llaman métodos `agregarTanda`/`enviar` del `x-data` que, antes del round-trip, abren el `<x-collapsible>` que falta (`paneles.*`) y `$destacar($refs.grupoX)` (anclas `x-ref` en máquina/tipo/motivos/botón/diferencia); el servidor sigue validando como respaldo. (2) **global** — `<x-input-error>` emite `[data-error-message]` y un handler post-`Alpine.start()` (en `requestAnimationFrame`) lleva y sacude el **primer error visible** (`offsetParent !== null`, sin `focus()` para no abrir el teclado). Verificado en preview a 375: las 6 preconditions del soplador interceptan client-side (panel abierto, `dg-destacado` aplicado, `box-shadow: rgba(248,113,113,0.52)`, **sin navegar**), camino feliz procede, limpieza sin residuo; y el handler global scrollea (scrollY 321) al error de `asignadas` tras un submit fallido. 358 tests verde (sin cambios de servidor).
- **Evitar a futuro:** para **dos animaciones sobre un mismo elemento**, combínalas en una sola declaración `animation` (comas), nunca en dos clases que ambas setean la shorthand. Todo nuevo bloqueo por precondición usa `$destacar`/`dgDestacar` (client-side si hay estado Alpine; el global cubre lo server-side automáticamente por el `[data-error-message]` de `<x-input-error>`). No auto-`focus()` en móvil (abre el teclado).

### [2026-07-01] `<x-info-tip>` en móvil: los globos ⓘ se apilaban y se recortaban en los bordes
- **Síntoma:** al abrir el panel de Producción en el teléfono, tocar varios ⓘ dejaba **varios globos abiertos a la vez** (superpuestos) y los de la columna izquierda (Asignado, % de avance, Tasa 1ª) **se recortaban** por el borde izquierdo de la pantalla ("asignadas hoy a los s…ir."). En desktop se veía bien.
- **Causa:** dos cosas en `resources/views/components/info-tip.blade.php`. (1) *Apilamiento:* el botón usa `@click.stop` (necesario para no navegar cuando el ⓘ va dentro de una tarjeta-enlace), pero eso **corta la propagación** del click; los `@click.outside` de los demás tips son listeners a nivel de `document` y nunca se enteraban de que se abrió otro → no se cerraban. (2) *Recorte:* el globo era `absolute w-64` (≈ el ancho del móvil) anclado con `left-0`/`right-0` al ícono; para un chip pegado a un borde el globo se sale por ese lado. El `align` es estático y no se puede acertar por chip porque la grilla cambia de columnas por breakpoint (2 en móvil, 5 en desktop).
- **Solución:** reescribir **solo** el componente. (1) **Single-open por evento de ventana:** al abrir un tip (tap o hover) se hace `$dispatch('close-info-tips')` y cada tip escucha `@close-info-tips.window="open=false; hover=false"` → se cierra el resto sin depender de la propagación. Toda ruta de cierre resetea también `hover` (evita el `hover` pegado en devices que reportan `hover:hover` por error y sintetizan `mouseenter` al tocar). El toggle del botón evalúa el estado **mostrado** (`open || (canHover && hover)`). (2) **Presentación responsive:** en móvil (clases base) el globo es una **hoja inferior** `fixed inset-x-4 bottom-4 z-50` (anclada al viewport, jamás se recorta) + backdrop `fixed inset-0 z-40 bg-neutral-900/20 sm:hidden` que atenúa y cierra al tocar fuera; en desktop (`sm:`) se resetean los insets y vuelve el popover chico junto al ícono (`sm:absolute sm:top-full sm:mt-2 sm:w-64 sm:left-0|right-0`). Verificado en preview: móvil `position:fixed`, left=16/right=16, width=343, dentro del viewport y **un solo** globo abierto; desktop `absolute`, w-64, bajo el ícono. `view:clear` + `npm run build` (bundle `app-DD6dNPhK.css` con las clases nuevas y las `lg:flex`/`lg:hidden` del nav intactas). 358 tests verde.
- **Evitar a futuro:** cuando un disparador use `@click.stop`, **no** confiar en `@click.outside` de otros elementos para cerrarlos (el `stop` los deja sordos) → usar un evento de ventana para el "cerrar a los demás". Un popover `absolute` anclado al ícono **se recorta** en móvil angosto: para contenido de ayuda usar una hoja inferior/anclada al viewport en móvil (`fixed inset-x-4 bottom-4`) y reservar el popover junto al ícono para `sm:`+.

### [2026-07-01] Secciones desaparecían al scrollear (capa de composición fantasma por `dg-enter` con fill-mode `both`)
- **Síntoma:** en el panel de Producción, al tocar "Por aprobar" (salta a `#cola`) y subir con scroll, la sección "Requiere tu atención" (o cualquier bloque `dg-enter`) quedaba **en blanco** hasta que un **click en cualquier parte** la hacía reaparecer. Reportado en Brave (Chromium).
- **Causa:** `.dg-enter` usaba `animation: dg-enter 0.4s … both`. El `animation-fill-mode: both` **retiene el último keyframe para siempre**, dejando `filter: blur(0px)` y `transform: matrix(1,0,0,1,0,0)` **persistentes** (confirmado con `getComputedStyle`). Un elemento con `filter`/`transform` ≠ `none` se promueve a **capa de composición** propia; Chromium/Brave (según GPU) a veces **no la repinta al scrollear** → contenido en blanco hasta que un evento (click/hover/resize) fuerza el repaint.
- **Solución:** cambiar el fill-mode de `.dg-enter` de `both` a **`backwards`** en `resources/css/app.css`. `backwards` mantiene el primer keyframe *antes* de arrancar (sin flash de entrada) pero al **terminar** revierte al estado base **sin `filter`/`transform`** → no queda capa persistente. Verificado: post-animación `filter: none` y `transform: none` (antes `blur(0px)`/`matrix`). La entrada (blur + desliz) se ve igual. 358 tests verde.
- **Evitar a futuro:** en animaciones de **entrada** (que terminan en el estado natural) usar `animation-fill-mode: backwards`, **no `both`/`forwards`** — así no dejan `filter`/`transform` persistentes que creen capas de composición fantasma. Reservar `forwards`/`both` solo cuando el estado final DIFIERE del base y debe conservarse.

### [2026-07-01] Paleta de 4 colores + drill-downs de producción · clases stale del caché de vistas inflaban el bundle
- **Síntoma:** (1) se habían colado **verde/ámbar** fuera de la paleta de marca (producido/merma/estados) → el dueño pidió estricto naranjo+negro+gris+blanco (rojo solo destructivo). (2) El panel del jefe no dejaba profundizar (día puntual, máquina, tipo, ranking). (3) Al limpiar los colores, el grep del bundle **seguía mostrando** `text-emerald`/`bg-amber`/`bg-blue` aunque **ningún Blade** los usara (grep en `resources/**` daba 0), y el bundle pesaba ~55kB.
- **Causa:** (colores) los estados/números usaban `emerald`/`amber` y el `badge` tenía variantes `info/warning/success`. (bundle) `resources/css/app.css` tiene `@source '../../storage/framework/views/*.php'`: Tailwind v4 escanea las **vistas Blade compiladas** en `storage/framework/views`, que conservaban clases **viejas** (compiladas antes de la edición) hasta que se re-renderizaran o se limpiara el caché → generaba utilidades muertas y falsos positivos en el grep. Además el `@theme` solo definía `brand-50/100/500/600/700`: usar `brand-200/300` (bordes/tintes suaves) **no generaba** la clase (color no definido).
- **Solución:** (A) paleta: mapear por **relleno/peso/tono**, no por matiz — producido/foco/atención = `brand`; merma/secundario = `neutral` atenuado; reposo/final = `neutral`; badges por relleno (Enviado `brand-600` sólido, Aprobado `neutral-800` sólido); rojo solo destructivo. Se agregaron `--color-brand-200/300` al `@theme`. `badge` quedó en `brand`/`neutral`/`danger`. (B) drill-downs: rutas `produccion.dia` (`?fecha=`), `produccion.maquina/{maquina}`, `produccion.tipo/{tipoBotellon}` + ranking de sopladores y "por tipo" del periodo en el panel; helpers de agregación reutilizables en `ProduccionController` (`rango`, `construirTendencia`, `reportesPorDia`, `registrosPorDia`, `desgloseSopladores`, `desgloseRegistros`, …) y partials `admin/produccion/partials/{_totales,_tendencia,_desglose}`; el panel quedó como **hub** (día/máquina/tipo/soplador clickeables) y cross-links en `reporte`/`maquinas`/`tipos-botellon`. (C) bundle: **`php artisan view:clear` ANTES de `npm run build`** cuando se quitan/renombran clases → el bundle bajó a ~41kB y el grep de utilidades (`.text-emerald`, etc.) dio 0. 358 tests verde; verificado en preview a 375/1280 sin scroll horizontal.
- **Evitar a futuro:** (1) tras **eliminar/renombrar clases** Tailwind, correr `php artisan view:clear` antes del build y **grepear por la UTILIDAD** (`.bg-amber`), no por el nombre suelto (`amber` matchea las variables `--color-*` del `@theme`, que siempre están). (2) Todo shade nuevo de `brand-*` (ej. `brand-200`) debe existir en el `@theme` o la clase no se genera. (3) Paleta de 4: la intención se expresa con recursos no-cromáticos; el rojo es la única excepción y solo para destructivo/error.

### [2026-07-01] Panel del jefe con contexto histórico + `whereBetween('fecha', …)` no matchea el borde con fecha casteada
- **Síntoma:** (mejora + gotcha) el panel de Producción (`/admin/produccion`) solo mostraba "hoy" y arrancaba en ceros; el jefe no veía días anteriores. Al agregar el bloque "producción por periodo", el test `test_panel_filtra_por_rango` con rango `hoy..hoy` daba `producido = 0` (el reporte de hoy quedaba fuera).
- **Causa:** la columna `fecha` (cast `date`) se guarda con hora `00:00:00`; `whereBetween('fecha', [$desde, $hasta])` compara **por string** y `'2026-06-30 00:00:00' > '2026-06-30'` (borde superior) → cuando `desde == hasta` el registro del propio día queda **fuera**. (Con `hasta` de un día posterior no se nota, por eso solo falló el filtro de un día. Mismo espíritu que la entrada del 2026-06-04 sobre `updateOrCreate` y fechas casteadas.)
- **Solución:** en `ProduccionController::index`, usar `->whereDate('fecha','>=',$desde)->whereDate('fecha','<=',$hasta)` (envuelve la columna en `DATE()`, comparación por día) en vez de `whereBetween`, tanto para reportes como para asignaciones del periodo. El panel se rediseñó en 5 bloques: **Requiere tu atención** (por aprobar/devueltos/atrasados hoy), **Hoy** (asignado/producido/avance/merma%/tasa 1ª), **Producción por periodo** (filtro `desde/hasta` default últimos 7 días, totales + tabla por día con mini-barras CSS `style="width:%"`), y **Por máquina · hoy** + **Cola · hoy** (intactos). Métricas con `selectRaw + groupBy(fecha)` (sin N+1) y un helper `armarResumen()` como fuente única. Tests: `test_panel_muestra_alertas`, `test_panel_resumen_de_hoy`, `test_panel_tendencia_por_periodo_agrega_por_dia`, `test_panel_filtra_por_rango` (con `assertViewHas`). 353 verde; verificado en preview a 375/1280.
- **Evitar a futuro:** para filtrar por rango sobre columnas `date`/`datetime` casteadas, usar `whereDate(col,'>=',…)` / `whereDate(col,'<=',…)` (o normalizar a `Y-m-d`), **no** `whereBetween` con strings de fecha — el borde con hora `00:00:00` se escapa. Las mini-barras van con ancho por `style="width:%"` (Tailwind purga clases dinámicas de ancho).

### [2026-06-30] `dropUnique` antes de crear el índice de reemplazo rompió el deploy en MySQL (1553) → 500 al asignar
- **Síntoma:** tras pushear el feature "varias producciones por día" (`a2229d7`), **Tests** quedó verde pero **"Desplegar a producción" #103 rojo** (~11s). En staging `POST /admin/produccion/asignar` daba **500** y el soplador no veía producciones nuevas. (El deploy anterior `#102`, sin migración, fue verde.)
- **Causa:** la migración `2026_06_30_120000_drop_unique_de_produccion_asignaciones` hacía `dropUnique(['soplador_id','fecha','turno'])` **antes** de crear el índice de reemplazo. En MySQL/InnoDB la **FK de `soplador_id`** se apoyaba en ese índice único (es su columna líder, prefijo izquierdo); soltar el único índice que cubre una FK falla con **error 1553** ("needed in a foreign key constraint"). `migrate --force` abortó → `deploy.sh` salió con error → deploy rojo, y la migración **no quedó registrada**. Pero el `git pull` del deploy ya había actualizado el **código** (la vista lista aparecía en staging): código nuevo (`asignarStore` **siempre crea**) + **unique todavía presente** → asignar a un soplador que ya tenía asignación de ese día/turno **violaba el unique** → `QueryException` → 500. **SQLite (tests/CI) no valida el requisito de índice de la FK**, por eso la migración pasó verde local y el bug solo apareció en MySQL.
- **Solución:** reordenar la **misma** migración (estaba pendiente en prod; una nueva no servía porque la vieja seguiría corriendo): **crear primero** el índice de cobertura `[soplador_id, fecha]` (deja la FK de soplador_id cubierta por prefijo izquierdo) y **después** `dropUnique`. Idempotente con `Schema::hasIndex($table, $columnas[, 'unique'])` (Laravel 12; matchea por nombre **o** por array de columnas) por si un intento dejó estado parcial. `down()` reordenado al reverso (re-crear unique antes de soltar el índice). 349 tests verde en SQLite; la prueba real es el deploy verde en MySQL.
- **Evitar a futuro:** **toda migración que suelta un índice que cubre una FK** (columna líder de un índice único/compuesto que respalda una `foreignId`) debe **crear primero** un índice alternativo que cubra esa FK. Los tests en SQLite **no** atrapan esto (no exige índice para FKs); para DDL no trivial, razonar en términos de MySQL y/o validar contra MySQL. Si un deploy con migración falla rápido (~seg) y el anterior sin migración fue verde, sospechar del DDL.

### [2026-06-30] El soplador no veía una producción recién asignada (cupo único día/turno) → varias producciones por día
- **Síntoma:** el jefe asignó 500 preformas a un soplador para hoy/día, pero el soplador **no podía** abrir esa producción — solo veía un reporte **ya aprobado** de hoy/día en solo lectura. En el panel el reporte aprobado aparecía con `asignadas` cambiado a 500 (Δ -100).
- **Causa:** el modelo forzaba **un reporte por `(soplador, fecha, turno)`** (unique `[soplador_id, fecha, turno]`). `ProduccionController::asignarStore` hacía "buscar y actualizar": al re-asignar el mismo día/turno reusó la asignación existente y, con `$asignacion->reporte()->firstOrNew([])`, **recuperó el reporte aprobado**, le pisó `asignadas` (→ Δ -100) pero dejó `estado=aprobado` (solo ponía `borrador` si el reporte era nuevo). El `index` del soplador mostraba ese aprobado de solo lectura → "nada nuevo que llenar". De paso, se mutaba un registro histórico (aprobado).
- **Solución:** permitir **varias producciones por día** (decisión del dueño). (1) Migración `…_drop_unique_de_produccion_asignaciones` quita el unique y deja un índice no único `[soplador_id, fecha]`. (2) `asignarStore` **siempre crea** una asignación nueva + reporte en borrador (se eliminó la rama buscar-y-actualizar) → nunca toca/pisa un reporte existente (un aprobado queda intacto), lo que también cierra el bug de mutación. (3) Pantalla del soplador: `MiProduccionController::index` pasó de "el reporte del día" a **listar** las producciones de hoy (`resources/views/produccion/mis-producciones.blade.php`), cada tarjeta abre `mi.show` para reportarla; la pantalla de llenado (`mi-reporte`) gana un enlace "← Mis producciones". `registroStore/Destroy` ahora redirigen a `mi.show` (quedarse en el reporte) y `update(enviar)` a `mi.index` (volver a la lista). (4) Limpieza: `ProduccionController::destroyReporte` (+ ruta `produccion.reporte.destroy` + botón papelera en la cola) borra una producción **solo si está en borrador y sin tandas** (sin kardex, sin trabajo) para deshacer una asignación equivocada. Tests: `test_asignar_de_nuevo_crea_otra_produccion`, `test_asignar_no_muta_un_reporte_aprobado_existente`, `test_soplador_lista_varias_producciones_del_dia`, `test_jefe_elimina_produccion_vacia_en_borrador` (+ rechazos). 349 verde. Verificado en preview a 375/768/1024.
- **Evitar a futuro:** un "upsert por slot" (`firstOrNew`/`updateOrCreate`) sobre un agregado que puede estar **cerrado** (aprobado/enviado) pisa historia silenciosamente; si el flujo permite repetir, **crear nuevo** y no reutilizar. Al cambiar la cardinalidad de un modelo (de 1 a N por día), revisar **todas** las lecturas que asumían "el único" (aquí el `index` del soplador) y las **redirecciones** de las sub-acciones (varias aserciones de test esperaban volver al `index`).

### [2026-06-30] CI de tests (GitHub Actions): xdebug daba 500 falsos + test stale tras rename → 6 rojos engañosos
- **Síntoma:** al montar el primer workflow de CI que corre `php artisan test`, (a) los runs fallaban al **arrancar** (0 jobs, "workflow file issue") y (b) ya corriendo, 6 tests de `ServicioTecnicoManagementTest` fallaban con `received 500` / `DateMalformedStringException: Failed to parse time string (fecha_ingreso) … Next Error: Cannot create dynamic property DateMalformedStringException::$xdebug_message`.
- **Causa:** **tres** capas. (1) Arranque: un **bloque de comentarios** en el YAML hacía que GitHub no parseara el workflow (la versión sin comentarios arranca; el `name:` se mostraba como ruta en `gh workflow list` = no parseado). (2) Los 500: el runner trae **xdebug**; en PHP 8.3, cuando Carbon lanza `DateMalformedStringException` (que Laravel normalmente captura en `getDateTime`), xdebug intenta agregarle `$xdebug_message` → lanza un **`Error` de propiedad dinámica** (no `Exception`) que escapa el `catch (\Exception)` → 500. **Prod (ea-php83) no tiene xdebug, así que no pasa.** (3) Tras `XDEBUG_MODE=off`, los 500 se volvieron fallos de aserción: el helper `payload()` del test usaba `tipo_equipo => 'maquina'`, renombrado a `'dispensador'` por la migración `rename_tipo_equipo_maquina_a_dispensador` → `Rule::in(TIPOS)` lo rechazaba. El test quedó **stale** y nadie lo notó (sin PHP local, sin CI).
- **Solución:** `.github/workflows/tests.yml` SIN actions de terceros (clona con `git`; el runner ya trae PHP 8.3 + Composer) y con `env: XDEBUG_MODE: 'off'` para **igualar prod**. Fix del test (no de producción): `tipo_equipo` `'maquina'`→`'dispensador'` (líneas 47 y 446). Resultado: **339 verde**. Ningún bug real de ServicioTécnico — ambos eran artefactos de test/CI.
- **Evitar a futuro:** (1) la CI de tests debe correr **sin xdebug** (`XDEBUG_MODE=off`) para reflejar prod; con xdebug, PHP 8.3 convierte excepciones de fecha capturables en 500. (2) Comentarios en los YAML de workflows: mantenerlos mínimos/ASCII; un bloque problemático rompió el parseo (0 jobs). (3) Toda **migración que renombra un valor de enum/lista** (`Rule::in`) debe actualizar los tests y seeders que usan el valor viejo el mismo día. (4) Ahora que hay CI, **mirar el run de Tests** además del de deploy en cada push.

### [2026-06-30] Revisión adversarial del merge M11: carrera al aprobar() + preforma_id sin scope
- **Síntoma:** auditoría sobre el HEAD ya **mergeado** (kardex M11 + motivos por tanda de otro dev + fixes m05.3 de otra sesión de IA, todos pusheados a `main` en paralelo). 6 dimensiones × 3 verificadores con voto por mayoría → 3 hallazgos confirmados, 0 falsos. Ninguno corrompe stock (el kardex nunca toca `stocks`/`bodegas`).
- **Causa:** (M-2, medio) el guard de idempotencia de `ProduccionController::aprobar` (`movimientos()->exists()` → generar) era **check-then-act sin `lockForUpdate`** ni índice único: dos aprobaciones concurrentes (doble-tap en el celular) podían pasar ambas y duplicar el kardex. El fix de tandas (m05.3) agregó el lock a `registroStore/Destroy` pero no a `aprobar`. (M-3, bajo) `asignarStore` validaba `preforma_id` solo con `exists:productos,id`, sin exigir `activo` ni el scope del selector → un id inactivo/de otra categoría entraba al kardex. (M-1, medio) `generarParaReporte` leía el consumo de los totales denormalizados (`$reporte->total`) pero producción/merma de los `registros`; como `ajustar()` edita los totales sin recalcular registros y sin guard de estado, `enviado→ajustar→aprobar` dejaba el kardex con `consumo ≠ producción+merma`. **Corregido el 2026-06-30** (a pedido del dueño): `generarParaReporte` ahora deriva el consumo de la **suma de las tandas** (misma fuente que producción/merma) → kardex internamente consistente; el ajuste del jefe queda como capa de reporte (marcado con `motivo_ajuste`, "marcar no remodelar"). Test `ProduccionKardexTest::test_kardex_usa_las_tandas_no_los_totales_ajustados`.
- **Solución:** M-2 → `lockForUpdate` sobre el reporte al tope de la transacción de `aprobar` (re-chequea `esPendienteDeRevision()` con la fila bloqueada; la 2ª request sale sin regenerar) — mismo idioma que `MiProduccionController`. M-3 → `Rule::exists('productos','id')->where('activo', true)` (requiere `use Illuminate\Validation\Rule;`) + test `ProduccionKardexTest::test_asignar_rechaza_preforma_inactiva_y_acepta_activa`. Sin cambios de Blade/CSS (no hubo rebuild). Verificado estáticamente (esta PC no tiene PHP): edits válidos, test assertea por las razones correctas.
- **Evitar a futuro:** todo guard de idempotencia "leer-luego-crear" en endpoints que mutan inventario va con `lockForUpdate` sobre la fila ancla; todo `<select>`/id que alimente el kardex se valida contra el **mismo scope** que ofrece su selector (no solo `exists`). Y `git fetch` antes de empezar: el repo tiene varios autores (humano + otra sesión de IA) pusheando directo a `main`.

### [2026-06-30] El deploy sembraba SOLO roles: `deploy.sh` del servidor "congelado" (seeders nuevos nunca corrían en prod)
- **Síntoma:** tras desplegar M11, la IA de pruebas (Chrome) no encontraba en staging las preformas/botellones `TEST-` que siembra `ProduccionTesteoSeeder` (el selector "Preforma" mostraba 19 reales, 0 de testeo), pese a deploy **verde**. El log del deploy mostraba `INFO Seeding database.` e **inmediatamente** el siguiente paso — **cero líneas `Database\Seeders\... DONE`** (Laravel siempre las imprime, como sí pasó con `migrate`). El echo del paso de seed decía `==> Seed de roles/permisos (idempotente)`, texto que **no existe** en el `deploy.sh` de `origin/main` (dice `==> Seed base ... + sucursales` con `db:seed --force`).
- **Causa:** el `deploy.sh` **del servidor** quedó congelado en una versión vieja cuyo seed era `db:seed --class=RolesAndPermissionsSeeder --force` (solo roles; al correr un seeder raíz con `--class` no se imprime sub-línea). `git reset --hard origin/main` (del workflow y del propio script) **no lo pisa** — casi seguro tiene `git update-index --skip-worktree deploy.sh` puesto en el server. El **resto** del código sí se actualiza (la migración nueva `130001` corrió), así que `DatabaseSeeder` y los seeders en el server estaban al día, pero **nunca se ejecutaban** vía deploy. Esto explica también por qué en [2026-06-05] hubo que correr `SucursalSeeder` a mano: misma causa, no era "self-update lag" puntual sino un `deploy.sh` permanentemente stale.
- **Solución:** correr el seed **a nivel workflow**, no dentro de `deploy.sh`: en `.github/workflows/deploy.yml` el comando SSH termina con `&& /opt/cpanel/ea-php83/root/usr/bin/php artisan db:seed --force` (después de `bash deploy.sh`, sobre el código ya reseteado a `origin/main`). Garantiza el `DatabaseSeeder` completo independiente del `deploy.sh` stale; idempotente. **Verificado en el log** del deploy `28449808479`: corren los 6 seeders (`RolesAndPermissions`, `Sucursal`, `Configuracion`, `TipoBotellon`, `Maquina`, `ProduccionTesteo`) con `RUNNING/DONE`.
- **Evitar a futuro:** (1) limpieza pendiente en el server: `git update-index --no-skip-worktree deploy.sh` (y verificar `git status` lo deje rastreado) para que `deploy.sh` vuelva a actualizarse solo; ahí el seed del workflow queda redundante pero inofensivo. (2) **Leer el log del seed** (no solo el verde del deploy): si ves `INFO Seeding database.` sin las líneas `Database\Seeders\... DONE`, no se sembró nada. (3) Para diagnosticar `deploy.sh` stale: comparar el **texto de los `echo`** del log contra el `deploy.sh` del repo — si difieren, el server corre otra versión.
### [2026-06-30] Soplador escribe lo menos posible: todo el reporte por opciones tocables
- **Síntoma:** (mejora pedida por el jefe, no un error) en la pantalla del operario (`resources/views/produccion/mi-reporte.blade.php`) aún quedaban 3 puntos de tecleo: el **motivo de la diferencia** (texto libre + `datalist` con 5 sugerencias hardcodeadas en la vista), las **observaciones** (textarea libre) y los **motivos de defecto** de 2ª/malas (un `<select>` nativo, sin tecleo pero no táctil como el resto). Además los números de tanda grandes (>500) se tecleaban porque el `stepper-input` solo tenía pasos +1/+10.
- **Causa:** la pantalla ya era muy táctil (máquina/tipo con `chip-radio`, cantidades con `stepper-input`), pero esos campos quedaron como texto libre / dropdown y las sugerencias del motivo de diferencia nunca se centralizaron (vivían en el Blade).
- **Solución:** componente nuevo reutilizable **`<x-reason-chips>`** (sobre `chip-radio`) usado 3 veces: motivos de 2ª y malas (`MOTIVOS_DEFECTO`, sin "Otro") y motivo de la diferencia (`MOTIVOS_DIFERENCIA`, **con chip "Otro"** que revela un texto libre como salida de escape). Constantes centralizadas en `ProduccionReporte`: `MOTIVO_OTRO` (centinela `__otro__`), `MOTIVOS_DIFERENCIA` (antes hardcodeadas) y `NOTAS_COMUNES`. Observaciones: chips de notas frecuentes que **insertan** su frase en el textarea (`x-model="obs"`, dedup por `includes`), dejando el texto libre como respaldo. Steppers de 1ª/2ª/malos con `:steps="[10, 100]"` (dañada queda en `[1,10]`); el input central sigue como atajo para el número exacto (no se vuelve solo-tocable: tipear 540 a golpes de +100 sería peor). En `MiProduccionController::update` se resuelve el "Otro" **antes** de validar: si `motivo === '__otro__'` se toma `motivo_otro` (trim) y se normaliza `blank → null`; `motivo` sigue `nullable|string|max:255` (texto libre permitido, **sin** `Rule::in` para no romper la salida de escape). Verificado en preview a 375/768/1024 sin scroll horizontal (la fila `−/0/+10/+100` cabe en móvil) y nav horizontal `lg:flex` intacto en el bundle. Tests nuevos en `ProduccionTest` (motivo de lista, "Otro" libre, "Otro" sin texto, render de chips/notas) → 46/46 verdes.
- **Evitar a futuro:** todo nuevo selector de motivo/razón del operario usa `<x-reason-chips>` (no recrear chips ni volver a `<select>`/texto libre); las listas de opciones van como **constantes** (única fuente para vista y validación), nunca hardcodeadas en el Blade. Un select/radio no obligatorio sigue normalizando `''`→`null` antes de `Rule::in` (ver entrada del 2026-06-26).

### [2026-06-26] M11 (soplador) no tenía conexión con el stock + drift documentación↔código
- **Síntoma:** auditoría del flujo del soplador antes de la presentación. El módulo era un sistema autocontenido de conteo + aprobación: `aprobar()` solo cambiaba `estado`, `tipos_botellon` no enlazaba con `productos`, y no existía kardex ni descuento de preforma. Además `docs/PLAN-M11-FASE2.md` describía el diseño **opuesto** al implementado (preformas/botellones como `productos` + `produccion_movimientos`, descartar `maquinas`/`tipos_botellon`) — pero el código mantuvo `maquinas`/`tipos_botellon` y nunca construyó el kardex. `HANDOFF.md` no mencionaba M11.
- **Causa:** la Fase 2 documentada nunca se implementó como estaba escrita; se siguió un diseño alternativo (máquinas + tipos de botellón + tandas) sin reconciliar los docs. La "conexión con stock" quedó pendiente y sin registrar.
- **Solución:** **kardex local puente** (sin push a Bsale): migración aditiva `…_create_produccion_movimientos_y_enlaces_catalogo` agrega `tipos_botellon.producto_id` y `produccion_asignaciones.preforma_id` (ambos nullable, degradan con gracia) y crea `produccion_movimientos`. `ProduccionMovimiento::generarParaReporte()` se llama en `ProduccionController::aprobar` dentro de `DB::transaction` con guard de idempotencia (`movimientos()->exists()`): consumo_preforma = total, producción 1ª/2ª contra el producto del tipo, merma = malo+danada. **Nunca toca `stocks`/`bodegas`** (espejo Bsale). Vistas: kardex (`admin/produccion/movimientos`), preview "al aprobar se registrará" y selector de preforma. Seeders `ProduccionTesteoSeeder` (productos `TEST-`) + `MaquinaSeeder`. Fixes de paso: `ProduccionReporte`/`Maquina`/`TipoBotellon` agregados a `AuditController::MODELOS` (se auditaban pero no se veían); métrica "producido" separada en `producido` (1ª+2ª) vs `merma` (no inflar productividad con desperdicio); `asignarStore` envuelto en transacción. Docs reconciliados (este archivo, `HANDOFF.md` §8d, nota de superado en `PLAN-M11-FASE2.md`).
- **Evitar a futuro:** cuando un plan documentado no se implemente tal cual, **reconciliar el doc el mismo día** (el plan describía un diseño que no era el del código → un colaborador nuevo se confunde). Toda fase nueva de M11 escribe su estado real en `HANDOFF.md`.

### [2026-06-26] `<select>` estricto manda `''` y rompe `Rule::in` al validar
- **Síntoma:** un select cerrado con opción placeholder (`<option value="">Elige…</option>`) que no es obligatorio siempre fallaba la validación cuando el usuario no elegía: `Rule::in([...])` rechaza el string vacío `''` aunque la regla lleve `nullable` (nullable solo salta para `null`, no para `''`).
- **Causa:** un `<select>` no seleccionado envía `''` (string vacío), no `null`. `nullable` no lo trata como ausente, así que pasa a `Rule::in` y no matchea ningún valor de la lista.
- **Solución:** normalizar a `null` **antes** de validar: `$request->merge(['motivo_x' => $request->filled('motivo_x') ? $request->input('motivo_x') : null])`. Luego `['nullable', Rule::in(...)]` deja pasar el null y valida solo los valores reales. La obligatoriedad condicional (ej. motivo requerido si la cantidad > 0) se chequea aparte tras `validate()`, como ya se hace con el motivo de la diferencia. Ver `MiProduccionController::registroStore` (motivo de defecto por tanda). Lista cerrada centralizada en `ProduccionRegistro::MOTIVOS_DEFECTO` (fuente única para validación y el `<x-select>`).
- **Evitar a futuro:** todo select no obligatorio con `Rule::in` → normaliza `''`→`null` antes de validar; no confíes en que `nullable` cubra el string vacío.

### [2026-06-15] `syntax error, unexpected token "endif"` al compilar Blade con condicionales inline pegados
- **Síntoma:** una vista no compila (`syntax error, unexpected token "endif" (View: ...index.blade.php)`) y el test que la renderiza falla, aunque las llaves parecen balanceadas.
- **Causa:** condicionales `@if(...) ... @endif@if(...) ... @endif` **encadenados en una sola línea** (un `@endif` pegado al siguiente `@if`, con interpolaciones `{{ }}` en medio) confunden al compilador de Blade y el `@endif` queda sin su `@if`.
- **Solución:** construir el texto en un bloque `@php` (p. ej. `collect([...])->filter()->implode(' · ')`) y renderizar el resultado con un solo `{{ $detalle }}`. Ver `resources/views/admin/servicio-tecnico/index.blade.php` (subtítulo de la fila: tipo · marca modelo · N° serie).
- **Evitar a futuro:** no encadenar `@endif@if` en línea ni mezclar varios condicionales inline con interpolaciones; si la lógica arma una cadena, resolverla en `@php` y dejar el Blade declarativo.

### [2026-06-15] Rebuild de Tailwind dropeó `lg:flex`/`lg:hidden` del nav → hamburguesa en escritorio
- **Síntoma:** tras desplegar M05.2 (`eff4995`), en escritorio (≥1024px) staging mostraba la **hamburguesa** en vez del nav horizontal agrupado; el menú móvil se desplegaba en pantalla ancha. `navigation.blade.php` estaba intacto (el nav horizontal es `lg:flex`, la hamburguesa `lg:hidden`).
- **Causa:** el `npm run build` de M05.2 generó un bundle (`app-CARLsbwS.css`) **sin** las utilidades `lg:flex` ni `lg:hidden` (verificado con grep: 0 y 0), pese a que el Blade las usa. El CSS del commit anterior (`3374acc`/`app-CohybUlu.css`) sí las tenía. Sin `lg:flex` el nav horizontal queda `hidden` siempre; sin `lg:hidden` la hamburguesa (base `flex`) no se oculta nunca. El build escaneó un estado donde esas clases no aparecieron y el push reemplazó el CSS bueno por el incompleto. No hubo cambio de código fuente.
- **Solución:** `npm run build` de nuevo (generó `app-Bo5nCYa1.css`) y **verificar el bundle antes de commitear** que contenga `lg:flex`/`lg:hidden` (regresión corregida) y las clases nuevas del cambio (`peer-checked`/`min-h-12` del `chip-radio`) → superset. Commit `4e7… (hotfix nav)`. Verificado en preview a 375 (hamburguesa) y 1280 (nav horizontal).
- **Evitar a futuro:** tras recompilar Tailwind, **grepear el bundle** por las clases responsivas críticas (hoy: las `lg:`/`max-lg:` de `components/layout/sidebar.blade.php` y `topbar.blade.php`) antes de commitear `public/build/`. Un escaneo incompleto puede dropear clases presentes en el Blade y romper el layout **solo en producción** (local levanta con `npm run dev`, que no purga).

### [2026-06-12] `whereNotIn('col', [])` compila como `1=1` → riesgo de delete masivo en syncs espejo
- **Síntoma:** (riesgo latente detectado en auditoría, no llegó a dispararse en prod) `PriceListSync::syncDetalles` borraba los precios obsoletos con `whereNotIn('producto_id', $vistos)->delete()`; si `$vistos` quedaba vacío —la lista trae details pero **ninguno** matchea el catálogo local (p. ej. productos que perdieron su `bsale_variant_id`)— el `whereNotIn([])` no filtra nada y el delete barre **todos** los precios de la lista. Corriendo cada hora por scheduler, sin supervisión.
- **Causa:** en SQL, `NOT IN (lista vacía)` es verdadero para toda fila (los query builders lo compilan como `1=1` o equivalente). Un "espejo fiel" implementado como delete-lo-no-visto se vuelve delete-todo cuando el recorrido no reconoce nada.
- **Solución:** guard en `app/Services/Bsale/PriceListSync.php::syncDetalles`: contar los details recorridos; si `details > 0 && $vistos === []` → **saltar el delete** y registrar el caso en `errores` (es síntoma de catálogo desincronizado, no un espejo válido). Lista con 0 details sí espeja el borrado (caso legítimo). Además se cerró el vector que podía des-sincronizar el enlace: `bsale_variant_id`/`bsale_product_id` ya no se aceptan en `ProductoController::validateData` (la sync es la única dueña del enlace; el form no los expone y el import CSV ya los ignoraba). Tests: `BsalePreciosSyncTest::test_zero_matches_with_details_skips_delete`, `::test_api_failure_on_details_does_not_delete`, `ProductoManagementTest::test_form_ignores_bsale_link_fields`.
- **Evitar a futuro:** en todo sync espejo con fase de borrado, **nunca** ejecutar el delete si el conjunto "visto" quedó vacío habiendo recorrido elementos; tratarlo como error. Ojo con `whereNotIn`/`NOT IN` con arrays potencialmente vacíos en general.

### [2026-06-08] `cURL error 60: unable to get local issuer certificate` al llamar APIs HTTPS desde PHP en Windows local
- **Síntoma:** cualquier llamada HTTPS saliente de PHP local (ej. `bsale:explore` contra `https://api.bsale.io`) falla con `cURL error 60: SSL certificate ... unable to get local issuer certificate (20)`. En el servidor (Linux/HostGator) NO ocurre. (Composer sí funciona porque trae su propio CA bundle.)
- **Causa:** el PHP de Windows (`C:\Users\mauri\php83`) no trae bundle de certificados CA y `php.ini` tenía `curl.cainfo` y `openssl.cafile` **vacíos** → cURL no puede verificar el certificado del servidor. La env var `CURL_CA_BUNDLE` **no** la respeta Guzzle en este build.
- **Solución:** descargar el bundle oficial `https://curl.se/ca/cacert.pem` (ej. a `C:\Users\mauri\php83\cacert.pem`) y configurarlo en `php.ini`: `curl.cainfo = "C:/Users/mauri/php83/cacert.pem"` y `openssl.cafile = "C:/Users/mauri/php83/cacert.pem"` (usar `/`). Verificar con `php -r "echo ini_get('curl.cainfo');"`. Arregla todo el HTTPS de PHP, no solo Bsale.
- **Evitar a futuro:** es solo de entorno local Windows; **no** desactivar la verificación TLS (`verify=false`) — es inseguro. En producción el CA del sistema ya está.

### [2026-06-05] owen-it/laravel-auditing no registra audits en consola (tests sin audits)
- **Síntoma:** con la auditoría recién integrada, todo `assertDatabaseHas('audits', …)` en los tests fallaba aunque el feature funcionara; en la app web (request HTTP real) sí se registraban.
- **Causa:** owen-it **desactiva la auditoría cuando `runningInConsole()` es true** salvo que `config('audit.console') === true`. **PHPUnit corre como CLI** y el controlador se ejecuta dentro del mismo proceso (no hay request HTTP separado), así que con `console=false` no se registra ningún audit en los tests. (En prod ese mismo comportamiento es deseable: el `db:seed --force` del deploy no debe generar audits.)
- **Solución:** hacer `audit.console` dependiente de env → `config/audit.php`: `'console' => env('AUDITING_CONSOLE', false)`; y en `phpunit.xml` agregar `AUDITING_CONSOLE=true` (+ `AUDITING_ENABLED=true`). Prod nunca define la var → queda `false`. Commit `1328de8`+ (Incremento 4). Además se quitó el guard `api` de `audit.user.guards` (no existe en este proyecto; el `UserResolver` lanzaría "Auth guard [api] is not defined").
- **Evitar a futuro:** los **cambios de rol** (pivote spatie `model_has_roles`) **no** se auditan solos; se registran a mano con un custom audit (`OwenIt\Auditing\Events\AuditCustom`, evento `roleChanged`) en `UserController`. Al auditar un modelo nuevo, recordar excluir secretos con `$auditExclude` (owen-it **no** respeta `$hidden`).

### [2026-06-05] Un cambio en `deploy.sh` no surte efecto en el MISMO deploy (self-update lag) → las sucursales no se sembraron
- **Síntoma:** el deploy del Incremento 1 salió `success` en Actions y las migraciones de `sucursales` quedaron `Ran`, pero la tabla `sucursales` quedó **vacía (0 filas)** en producción. Los roles y el permiso `manage sucursales` **sí** se crearon.
- **Causa:** `deploy.sh` se actualiza a sí mismo con `git pull` *dentro* del propio script, pero la shell que lo ejecuta ya tenía cargada la versión **anterior**. Ese deploy corrió el seed viejo (`db:seed --class=RolesAndPermissionsSeeder --force`, solo roles) en lugar del nuevo (`db:seed --force` = `DatabaseSeeder` completo, que incluye `SucursalSeeder`). Por eso los roles sí y las sucursales no.
- **Solución:** correr a mano **una vez** por SSH el seed nuevo: `php artisan db:seed --class=SucursalSeeder --force` (idempotente). El `deploy.sh` ya quedó actualizado en el server, así que los próximos deploys sí siembran. Verificar siempre en prod (`Sucursal::count()`) tras un deploy que cambia `deploy.sh`.
- **Evitar a futuro:** cuando un commit modifique `deploy.sh`, asumir que **ese** deploy usa la versión vieja del script; verificar prod y re-correr a mano los pasos nuevos. (Hardening posible: en el workflow hacer `git pull` ANTES de invocar `deploy.sh`, o que el script se re-ejecute tras el pull.)

### [2026-06-05] Deploy de GitHub Actions: "Too many authentication failures" (llave equivocada en el secret)
- **Síntoma:** los deploys #9–#13 fallan en segundos sin actualizar el server (HEAD se queda atrás, árbol limpio). Log del job: `Received disconnect from <host> port 2222:2: Too many authentication failures` → exit 255. El handshake SSH y el host key SÍ funcionan (`Permanently added ... ED25519`); falla la **autenticación**.
- **Causa raíz:** el secret `SSH_PRIVATE_KEY` de GitHub contenía una llave **RSA** (`SHA256:2CW02O...`) que **no está** en el `authorized_keys` del servidor. El server solo autoriza la llave **ed25519 `daligo-deploy`** (`SHA256:m2QhGn...`, la misma que `~/.ssh/daligo_hostgator` local). El server rechaza la RSA → ssh cae a keyboard-interactive/password → "Too many authentication failures". Funcionó hasta el #8 porque el `authorized_keys` del server se cambió/reseteó después (quedó solo la ed25519), pero el secret siguió con la RSA vieja. **No era KEX ni el ssh-agent** (las 3 primeras teorías, descartadas al leer el `ssh -v` del job).
- **Solución:** actualizar el secret `SSH_PRIVATE_KEY` con la llave **ed25519 autorizada** (`daligo_hostgator`). En el workflow se deja `-o IdentitiesOnly=yes -o IdentityAgent=none` (ofrecer solo la llave de deploy) y `-o KexAlgorithms=+diffie-hellman-group-exchange-sha256` (KEX legacy de HostGator). Para desbloquear producción al toque: `deploy.sh` a mano por SSH con la llave buena.
- **Evitar a futuro:** (1) el secret debe ser **la misma** llave que está en el `authorized_keys` del server — verificar con `ssh-keygen -lf` en ambos lados; preferir ed25519. (2) Si un deploy falla en segundos, **leer el log del job** (`GET /repos/{o}/{r}/actions/jobs/{id}/logs`, con `ssh -v`) ANTES de teorizar.

### [2026-06-04] `updateOrCreate` con columna `date` casteada no actualiza
- **Síntoma:** al reasignar producción (mismo soplador/fecha/turno con otra cantidad), la fila no se duplicaba pero la cantidad no se actualizaba (quedaba la primera).
- **Causa:** `updateOrCreate(['fecha' => $fecha, ...], [...])` compara `fecha` (cast a `date`) por igualdad exacta de string; el valor almacenado incluye hora (`Y-m-d 00:00:00`) y no coincide con `Y-m-d`, dejando el match inconsistente.
- **Solución:** buscar con `whereDate('fecha', $fecha)->where(...)->first()` y luego `update()` o `create()` explícito. Ver `app/Http/Controllers/Admin/ProduccionController.php::asignarStore` (módulo Producción).
- **Evitar a futuro:** para columnas `date`/`datetime` casteadas, usar `whereDate()` al buscar coincidencias; no confiar en la igualdad directa de `updateOrCreate`.

### [2026-06-04] Estilos nuevos de Tailwind no aparecen en producción
- **Síntoma:** un cambio de clases Tailwind se ve en local pero en staging/producción sigue igual.
- **Causa:** `public/build` está versionado y el servidor no tiene Node; no se recompiló ni commiteó el bundle, y Tailwind purga las clases no usadas (las nuevas no existían en el CSS desplegado).
- **Solución:** correr `npm run build` y commitear `public/build/` (manifest + assets `app-*.css/js`) **junto** con los Blade modificados. Ej.: commit `0c3f4fa`.
- **Evitar a futuro:** todo cambio de Blade/CSS/JS termina con `npm run build` + commit del build.

### [2026-06-03] El deploy por GitHub Actions no conecta a HostGator (KEX)
- **Síntoma:** el job de deploy falla con `handshake failed: no common algorithm for key exchange`.
- **Causa:** el SSH de HostGator solo ofrece el key-exchange `diffie-hellman-group-exchange-sha256`, que la librería SSH en Go de `appleboy/ssh-action` no negocia.
- **Solución:** usar el **cliente OpenSSH nativo del runner** (escribir la llave a un archivo y `ssh -i ...`). Ver `.github/workflows/deploy.yml` (commit `ad02f82`).
- **Evitar a futuro:** **no** volver a `appleboy/ssh-action` para este servidor.

### [2026-06-03] `git pull --ff-only` falla en el servidor por `public/.htaccess`
- **Síntoma:** `deploy.sh` aborta en el `git pull` porque el árbol de trabajo está sucio.
- **Causa:** cPanel reinyecta un bloque de handler PHP (solo comentarios) en `public/.htaccess`.
- **Solución:** `git checkout -- public/.htaccess` **antes** del pull (ya está en `deploy.sh`, commit `3b292c5`). cPanel lo vuelve a agregar después sin afectar el funcionamiento.

### [2026-06-03] Los `git push` no disparaban el deploy en el servidor
- **Síntoma:** se pusheaba a `main` pero el servidor nunca actualizaba.
- **Causa:** el `origin` del repo en el servidor apuntaba al repo viejo (`Mauricio-Alvarez-T/DaliGo`).
- **Solución:** `git remote set-url origin https://github.com/DaliGo-2013/DaliGo.git` en el servidor.

### [2026-06-02] 404 al fijar la versión de PHP con `AddHandler` en `.htaccess`
- **Síntoma:** la app responde 404 tras agregar un `AddHandler ...` en `.htaccess` para forzar PHP.
- **Causa:** en este hosting la versión de PHP se controla por **MultiPHP Manager (vhost)**, no por `.htaccess`.
- **Solución:** fijar la versión en **MultiPHP Manager**; no usar `AddHandler`.

### [2026-06-02] Estilos rotos tras instalar Laravel Breeze (Tailwind v3 vs v4)
- **Síntoma:** la UI queda sin estilos / rota después de instalar Breeze.
- **Causa:** Breeze trae **Tailwind v3** (`tailwind.config.js` + `postcss.config.js`), pero el proyecto usa **Tailwind v4**.
- **Solución:** borrar `tailwind.config.js` y `postcss.config.js`, volver `resources/css/app.css` a `@import 'tailwindcss'` y `vite.config.js` a `@tailwindcss/vite` (conservando `alpinejs`). Recompilar con `npm run build`.

---

## Nota de mantención

Si la Bitácora supera ~40 entradas y empieza a pesar, mover **solo esa sección** a `docs/PLAYBOOK.md`
y dejar aquí la regla + un enlace. Por ahora, un único archivo es lo más simple.
