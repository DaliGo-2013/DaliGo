# Parte de cierre — Max-1 (Forjador A, stream 1) · P-M14-06

> Emitido 2026-07-13. Modelo de la sesión: Fable 5 (decisión del dueño hasta el 19-07).

**TAREA:** P-M14-06 · Historial de aprobaciones (`/admin/aprobaciones`, permiso `view aprobaciones`).
**ESTADO:** HECHA (en rama; el `[x]` de RUTA se pone al merge de P-M14-07).

**EVIDENCIA:** commit `8c8d3e2` en `feature/m14-aprobaciones` (8 archivos, +382/−5):
- `AprobacionController::historial()` — filtros estado/tipo/solicitante/aprobador/rango de
  fechas con **`whereDate` sobre `created_at` (NUNCA `whereBetween`** — bitácora; test de
  regresión del borde `hasta=hoy`), paginación 25 `withQueryString`. Resumen que respeta los
  filtros: conteo por estado + agrupación por solicitante y por aprobador **sin N+1** (nombres
  en un solo `pluck`).
- Vista `admin/aprobaciones/index.blade.php` responsive (`x-page-header`/`x-select`/
  `x-list-card`/`x-list-row`/`x-badge`/`x-aprobaciones.estado-badge`); enlace en el dropdown
  **Administración** desktop+móvil con `@can('view aprobaciones')`. «Mis solicitudes» del
  solicitante ya existía (P-M14-03).
- Ruta admin `admin.aprobaciones.index` (permiso propio; la bandeja del aprobador sigue en
  `/aprobaciones` con `aprobar solicitudes` — superficies distintas).

**TESTS:** 6 nuevos (permiso, filtro por estado, borde `whereDate` del rango, filtro
solicitante/aprobador, resumen por estado, paginación preserva filtros). **Suite 485 verdes.**

**BUILD:** `app-BdqXoT9t.css` commiteado; grep del bundle OK (`lg:flex`/`lg:hidden` +
`sm:grid-cols-2`/`lg:grid-cols-3`/`tabular-nums`/`sm:w-44`).

**VERIFICADO EN PREVIEW** a 375/768/1280: sin scroll horizontal en los 3; filtros en 1/2/3
columnas; resumen en fila en desktop; filtro `estado=rechazada` aplica (1 fila + "Limpiar");
nav Administración con hamburguesa oculta en desktop.

**ESTADO E2·M14:** 6/7. Falta solo **P-M14-07** (re-sellado del plan — anotar el guard
`class_exists` omitido y los eventos registrados ya en 02 — + gate `/pre-merge` R-31 + merge
coordinado con doble llave Director+Mauricio + QA staging desde celular real). **No arranco 07
sin dictado nuevo** (así lo pide el dictado vigente).

**/usage:** lo corre Mauricio; lo anoto si me lo pasa.
