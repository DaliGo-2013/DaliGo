# Parte — Max-1 (Forjador A, stream 1) · P-M14-07 FASE A: merge de main EN LA RAMA

> Emitido 2026-07-13. **Main NO fue tocado** — el merge vive solo en `feature/m14-aprobaciones`.
> Fase B (push a main = deploy) espera la DOBLE LLAVE (Director + Mauricio).

**HASH DEL MERGE EN LA RAMA:** `db627d9` (Merge origin/main → feature/m14-aprobaciones).
Base de main plegada: `fcb9466` (118 commits — M12 completo: lote conductor, folio código,
informes ST, fotos, descuentos, mano de obra por horas + los 2 hotfixes de CI).

**CONFLICTOS (3, resueltos según doctrina):**
- `database/seeders/RolesAndPermissionsSeeder.php` → **unión de ambos lados**: jefe_bodega
  conserva `aprobar solicitudes` (M14) Y conductor/técnico ganan `crear lote servicio` (M12).
  El array de permisos auto-mergeó con TODO (verificado por grep).
- `tests/Feature/Admin/RoleMatrixSeedTest.php` → unión: matriz con ambos permisos nuevos por
  rol (admin con los 3: `crear lote servicio` + `aprobar solicitudes` + `view aprobaciones`).
- `public/build/manifest.json` → **jamás a mano**: se tomó main y se REGENERÓ con el build.

**BUILD (regenerado en el árbol mergeado):** `npm install` primero (gotcha qrcode de la
bitácora — mordió de nuevo y se resolvió en 1 min) → `view:clear` → `npm run build` →
`app-B1OCUURN.css` (47 kB) + `app-CKzqjbCg.js` commiteados. Lockfile sin ruido.

**GREP DEL BUNDLE (8/8):**
```
lg\:flex          -> 1     (nav crítico)
lg\:hidden        -> 1     (nav crítico)
min-w-\[1\.5rem\] -> 1     (M14 bandeja)
sm\:grid-cols-2   -> 1     (M14 historial)
.w-80             -> 1     (campanita M15/main)
bg-white\/60      -> 1     (PWA main)
.h-12             -> 1     (botones táctiles)
tabular-nums      -> 1     (métricas)
```

**SUITE COMPLETA DEL ÁRBOL MERGEADO: 560 verdes (1723 aserciones)** — 507 de main + 53 de
M14. Cero cruce entre streams, cero test roto por el merge.

**HALLAZGO LATERAL (deuda M12, NO la toqué):** el permiso `crear lote servicio` NO tiene
etiqueta en `config/permissions.php` — falta también en main (la UI de roles cae al nombre
técnico, no rompe). Para el dictado de Marcos o quien corresponda.

**LISTO PARA FASE B (a la espera de la doble llave):**
1. OK del Director sobre este parte + OK de Mauricio → push de la rama a main = DEPLOY.
2. QA staging desde el celular de Mauricio: ajuste grande de producción (Σ|Δ| ≥ 50) →
   campanita+correo al admin → aprobar desde el teléfono → ajuste aplicado al reporte.
3. P-M14-07 [x] en RUTA-MAESTRA (mismo push del merge o el siguiente docs) → **E2·M14 CERRADA**.

**/usage:** lo corre Mauricio; lo anoto si me lo pasa.

---

## ADDENDUM (mismo día, refresh): la rama quedó al día con main otra vez

Main avanzó 5 commits tras el primer merge (`e87e242` ingreso por CANTIDAD en el QR + GO
despachos + docs fleet). **Refresh merge en la rama: `f4f4484`** — 1 solo conflicto
(`manifest.json`, regenerado por build como manda la doctrina), `routes/web.php` auto-mergeó
(rutas de lote M12 + aprobaciones M14 conviven). Bundle `app-BzrcTguX.css` con **grep 6/6**.
**Suite del árbol refrescado: 570 verdes (1.779 aserciones).**

**Fase B sigue a la espera de la doble llave** (endoso del Director a este parte + visto bueno
de Mauricio). La rama está fast-forward-limpia respecto de main: el push será trivial.
