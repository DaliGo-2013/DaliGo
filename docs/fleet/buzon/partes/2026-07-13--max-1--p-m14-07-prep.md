# Parte — Max-1 (Forjador A, stream 1) · P-M14-07 PREP (re-sello + gate /pre-merge)

> Emitido 2026-07-13. Adelanto en rama de lo que NO requiere main verde (dictado v2).
> El MERGE en sí sigue en espera: main rojo (test de M12 ajeno) + doble llave.

## 1. Re-sellado de PLAN-M14 — HECHO
Commit `2ad7bf4` en `feature/m14-aprobaciones`: sello → `2026-07-13 (rama @ 8c8d3e2)` + nota de
las 2 desviaciones benignas del plan tal como se construyó:
- **Guard `class_exists` OMITIDO**: la rama nació post-merge M15 → dispatcher disponible desde
  P-M14-02; el servicio notifica directo, sin el puente degradador a `Log`.
- **Eventos en P-M14-02, no en 07**: los 3 (`aprobacion.solicitada/escalada/resuelta`) se
  registraron en `Notificacion::EVENTOS` al construir el servicio; notificaciones reales desde
  entonces.

## 2. Gate /pre-merge (R-31 adversarial) — dry-run sobre `feature/m14-aprobaciones` vs `origin/main`

| Gate | Resultado | Evidencia |
|---|---|---|
| Tests verdes | **OK** | `php artisan test` en la rama = **485 verdes** (el re-sello posterior es docs-only) |
| `public/build` recompilado | **OK** | P-M14-06 recompiló y commiteó `app-BdqXoT9t.css`; nada de Blade/CSS/JS desde entonces |
| MySQL 5.7 | **OK** | strings indexados ≤191 (`tipo_accion`/`rol_aprobador` 64, `estado` 32); `motivo`/`descripcion`/`resultado_motivo` 255 son NO indexados (ok); **0 `whereBetween` reales** (los 3 del diff son comentarios del gotcha) → historial usa `whereDate` con test de borde; **0 `dropIndex`/`dropUnique`** (migración solo crea) |
| `lockForUpdate` en guards | **OK** | `Aprobaciones::aprobar/rechazar/escalarVencidas` = transacción + `lockForUpdate` + re-check; handler `AjusteReporteProduccion` lockea el reporte |
| Permisos por ruta | **OK** | `/aprobaciones` → `aprobar solicitudes`; `admin/aprobaciones` → `view aprobaciones`; `/aprobaciones/mias` → `auth` |
| Reuso de x-componentes | **OK** | vistas 100% `<x-*>` (list-card/row, badge, select, reason-chips, estado-badge, page-header…); cero markup reinventado |
| Responsive 375/768/1024 | **OK** | verificado en preview: bandeja (P-M14-03) e historial (P-M14-06) a 375/768/1280, sin scroll horizontal |
| Bitácora (no reincidir) | **OK** | `whereBetween` evitado (con test de regresión), gotcha docblock `*/15` evitado, locks presentes |
| **Suite POST-merge verde** | **NO VERIFICABLE (bloqueado)** | **main está ROJO** por `ServicioTecnicoManagementTest:687` (M12/Marcos, `2d8fd73`) — ajeno a M14. El merge no puede aterrizar verde hasta que main esté limpio |

**Superficie de merge (conflictos esperados, aditivos):** solo 2 archivos de código —
`routes/web.php` y `resources/views/layouts/navigation.blade.php` (el stream ST también los
tocó; resolución = unión de ambos lados) + 2 artefactos de build (`manifest.json`, bundle CSS)
que se **regeneran** post-merge (`npm install` → `build` → grep), nunca a mano. Seeders /
`config/permissions.php` / `RoleMatrixSeedTest` NO chocan (main no los tocó desde el merge-base).

## VEREDICTO: APROBADO CON OBSERVACIONES
El código de M14 pasa **todos** los gates propios — está merge-ready. El merge NO puede
ejecutarse aún por **dos gates externos**, ninguno de código M14:
1. **main rojo** por el test de M12 (Marcos) — esperando su fix o autorización de Mauricio.
2. **doble llave** (Director + Mauricio) + QA staging desde el celular de Mauricio.

**Cuando main esté verde y se abra la ventana:** rebase de la rama sobre main → resolver los 2
conflictos aditivos → `npm install`+build+grep del bundle → suite completa verde → parte con el
hash del merge EN LA RAMA + conteo → push SOLO con doble llave → deploy → QA del flujo real.

**/usage:** lo corre Mauricio; lo anoto si me lo pasa.
