# Parte — Max-1 · PLAN-M16-V0 sellado (primer entregable de M16-v0)

> De: Max-1 (Forjador A) · Para: Director / Mauricio
> Dictado ejecutado: v5 (14-07) — «primer entregable NO es código: mini-plan con sello,
> confírmamelo por el buzón ANTES de construir vistas».

## Entregable

**`docs/planes/PLAN-M16-V0.md`** en la rama **`feature/m16-v0-dashboard`** (creada desde main
fresco `c86e341`, con M14 ya dentro) — commit **`dd20ee8`**, rama pusheada.

## Cómo se selló (método)

1. **Exploración paralela** de las 7 áreas (Inicio actual, M11, M12, M14, M15, stock, matriz
   permisos×roles): cada hecho del plan tiene archivo:línea verificado contra el árbol real.
2. **Verificación adversarial** posterior: 3 refutadores independientes contrastaron el plan ya
   redactado contra el código (citas de §0, las 15 cards con sus rutas/queries, y la derivación
   completa rol→cards). Resultado: **cards/rutas/matriz sin hallazgos**; 2 imprecisiones de
   conteo cazadas y corregidas antes del commit (DashboardTest son 9 tests, no 8; el seeder
   define 19 permisos, no 18).

## Lo esencial del alcance propuesto (detalle en el plan)

- **No hay página nueva:** `/dashboard` (el Inicio, y `start_url` de la PWA) se convierte en el
  tablero, **reagrupado por módulo** con las cards existentes INTACTAS (semilla de Marcos sin
  tocar: mismos queries, labels y permisos) + **5 cards nuevas**:
  Producido hoy / Avance % / Merma % (`manage production`), Recepciones por confirmar
  (`confirmar servicio tecnico`, scope `porConfirmar()` ya existente), Aprobaciones pendientes
  (`aprobar solicitudes`, espejo exacto de la bandeja) y Notificaciones fallidas
  (`view notificaciones`, filtro `?estado=fallida` ya validado en el controller).
- **Cero permisos nuevos, cero migraciones, cero componentes nuevos** (`<x-stat-card>` tal cual).
- **Stock crítico OMITIDO con evidencia:** no existe señal de mínimo en el espejo Bsale (sin
  columna/scope/config — verificado en migraciones, modelos y seeders). Anotado como fase
  posterior, tal como pedía el dictado.
- 3 pasos: P-M16-01 (controller+tests de conteo) → 02 (vista+responsive+build) → 03
  (visibilidad por rol + suite + RUTA + parte).

## 2 preguntas abiertas para el visto bueno (§5 del plan)

1. ¿Ampliar las 5 cards ST de `manage servicio tecnico` a incluir `view servicio tecnico`?
   (default propuesto: NO — semilla intacta, cero cambio de comportamiento).
2. Card «Aprobaciones pendientes» del admin cuenta TODAS las pendientes (espejo de su bandeja)
   — ¿ok? (default propuesto: sí, consistente con lo que ve al hacer click).

## Además, ejecutado antes en el mismo turno

Verificación post-deploy de M14 (parte separado `2026-07-14--max-1--verificacion-post-deploy-m14.md`,
ya en main `c86e341`): Actions 4/4 verdes, bundle `app-BzrcTguX.css` vivo en staging, rutas M14
en 302 — más el guion del QA de celular con su detalle clave (el ajuste lo pide un NO-admin).

**Quedo a la espera del visto bueno al plan para arrancar P-M16-01.** No construyo vistas sin él.

— Max-1
