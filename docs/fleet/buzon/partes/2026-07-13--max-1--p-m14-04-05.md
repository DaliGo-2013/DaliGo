# Parte de cierre — Max-1 (Forjador A, stream 1) · P-M14-04 + P-M14-05

> Parte formal pendiente que el Director pidió al abrir (nunca llegó por el bus viejo).
> Emitido 2026-07-13.

**TAREA:** P-M14-04 (escalamiento por scheduler) + P-M14-05 (cablear `ProduccionController::ajustar`)
**ESTADO:** HECHAS — verificadas [x] por el Director (consta en el dictado de max-1).

**EVIDENCIA:** commit `7a01da2` en `feature/m14-aprobaciones` (6 archivos, +443):
- **P-M14-04:** `Aprobaciones::escalarVencidas()` — pendientes nivel 0 más viejas que
  `aprobacion_escala_minutos` con regla activa que tenga `rol_escalamiento`: lock + re-check
  por fila (idioma de `notificaciones:reintentar`), re-notificación `aprobacion.escalada` al
  rol NUEVO. Comando `aprobaciones:escalar` agendado `everyFifteenMinutes()` +
  `withoutOverlapping(15)` con test que fija la expresión `*/15` (doctrina I-01). "Escalada"
  NO es estado (nivel + `escalada_at` + rol reescrito); badge ya estaba en la bandeja.
- **P-M14-05:** `ajustar()` pasa por el motor; validación INTACTA, la transacción migró al
  handler. Magnitud = Σ|Δ| de las 5 cantidades → bajo umbral se auto-aprueba y aplica en el
  mismo request (UX de siempre + fila histórica); sobre umbral queda pendiente para admin con
  el reporte INTACTO; el propio admin fluye sin fricción. Los tests históricos de `ajustar`
  siguieron verdes SIN cambios (no siembran la regla → auto-aprueban → cubren la semántica
  original por construcción).

**TESTS:** 10 nuevos (5 escalamiento + 5 cableado, incluye conflicto real: solicitud vieja vs
ajuste chico interino → rechazo automático y el estado interino sobrevive). Suite en su
momento: 479 verdes.

**GOTCHA (anotado en el commit y BITACORA):** un `*/15` dentro de un docblock `/** */` cierra
el comentario y rompe la sintaxis PHP — en docblocks se escribe "grilla de 15 min".

**/usage:** n/d (lo corre Mauricio; no me lo pasó en su momento).
