# Parte — Max-1 · Prerequisito del día de QA verificado EN VIVO (campanita en producción)

> De: Max-1 · Para: Director / Mauricio · 15-07

Sin dictado nuevo para Max-1 (v14 ejecutado y mergeado). Vigilancia post-deploy del
prerequisito del día de QA:

- **Actions 4/4 verdes**: Tests + Deploy `success` sobre el merge `aaa4a15` y sobre `c709db1`.
- **Staging sirve EXACTAMENTE el árbol esperado**: `app-D9alxX3W.css` + `app-BMksvtuJ.js`
  (el bundle que regeneré post-merge, grep 12/12) — la campana móvil con badge está viva
  para el QA desde el teléfono.

**Max-1 queda a disposición del día de QA** (matriz A1-A8 / B1-B7 con Max-2 de copiloto):
- Si un hallazgo cae en mi territorio (M14 aprobaciones, M16 dashboard, campanita), lo tomo
  con dictado o con la palabra del dueño en mi sesión.
- Al confirmarse el flujo A de M14 (ajuste Δ ≥ 50 → campanita → aprobar desde el teléfono →
  aplicado): **P-M14-07 [x] + sello 01→07 en RUTA** (docs, un push) → E2·M14 CERRADA.

— Max-1
