# Parte de cierre — Max-2 · MSG-3 · El refresco automático del chat

CUENTA: Max-2 (Forjador B, stream 2) · MODELO: Fable 5 (fijado por el dueño)
TAREA: **MSG-3** — GO del dictado v28 (PLAN-MENSAJES §5.4/§5.7)
ESTADO: **HECHA** — pide doble llave
/usage INICIO → FIN: n/d (lo captura el dueño)
SIGUIENTE: doble llave de `feature/msg-3-poll` → GO MSG-4 (v29: menú+badge, el
último lote del chat) cuando se dicte.

## EVIDENCIA

Rama **`feature/msg-3-poll`** desde main fresco (`32406f2`), pusheada — 1 commit
de código, suite COMPLETA verde:

| Commit | Qué | Suite |
|---|---|---|
| `c04147f` | Endpoint `mensajes.conteo` + firma global del chat + `<x-poll-recarga>` extraído (vivo y cola migrados) + poll en lista e hilo + 6 tests | **2244 / 15.594** (baseline 2238 / 15.579 en `32406f2` — delta EXACTO +6) |

**Los candados del dictado, verificados:**
1. `conteo` exige sesión (guest → login) y permiso (sin rol + Accept json → 403).
2. La firma CAMBIA con mensaje nuevo y NO cambia entre dos GET sin actividad
   (sin esto el monitor recargaría en loop o nunca).
3. La firma CAMBIA al marcar leído — el contador propio es parte de ella: leer
   en otra pestaña/dispositivo también refresca la lista abierta.
4. Vista y endpoint salen de LA MISMA función (`firmaChat`, privada) — el
   candado compara el viewData del hilo contra el JSON del conteo. En el hilo
   la firma se hornea DESPUÉS de `marcarLeida` o recargaría al tiro en falso.
5. El poll vive SOLO donde corresponde — candado ESTRUCTURAL: `x-poll-recarga`
   aparece EXACTO en sus 4 consumidores {vivo, cola, mensajes/index,
   mensajes/show} y el único poll inline que queda es el de ST.
6. **MUTADO**: contadores cegados en `firmaChat` → DOS rojos exactos («cambia
   al marcar leído» + el de misma-función, que también exige que abrir el hilo
   mueva la firma) → `git checkout --` → verde.

## Decisiones que valen registro

- **Componente `<x-poll-recarga>` extraído** (la decisión que v28 delegó): 4º
  uso del molde firma-reload. `vivo` y `cola de bodega` migraron con CERO
  cambio de conducta — sus tests (ProduccionPanelVivoTest, RetiroQrTest) pasan
  INTACTOS, cero ediciones; los comentarios del porqué de cada firma se
  conservaron sobre el componente. **El poll de ST queda FUERA a propósito**:
  es OTRA conducta (banner con delta de total, 25s, sin recarga) —
  coexistencia declarada, el precedente exacto del `_tabs` de ST con x-tab-nav.
- **Firma GLOBAL del chat** (una ruta, como dictó v28): md5(max id de mensajes
  en mis hilos | suma de MIS contadores | count de hilos). Cubre mensaje nuevo,
  lectura cruzada y conversación nueva con 2 queries indexadas sin render.
  **Recarga espuria aceptada y declarada**: un cambio en OTRO hilo recarga el
  hilo abierto — inofensivo (marcarLeida idempotente) y dentro del contrato
  «cambió algo → recarga».
- **Campanita/menú sin poll** (doctrina vigente): el badge llega en MSG-4 por
  el patrón declarativo.
- **Bundle INTACTO** (hash idéntico `app-BOeyNfqy.css`): el componente es un
  script, cero clases nuevas — verificado con rebuild y git limpio.

## Gotchas re-cazados por los propios candados (2 rojos ANTES del commit)

- El assert de la URL del poll: `@js()` escapa las barras — la forma cruda de
  la ruta JAMÁS está en el HTML (bitácora 2026-08-14, reincidencia evitada
  des-escapando antes de comparar).
- El barrido estructural acusaba a cola.blade: su comentario de CABECERA
  nombra `visibilityState` en prosa — el needle pasó a `document.visibilityState`
  (primo del `\brut\b` del 28-07: el needle debe distinguir prosa de código).
