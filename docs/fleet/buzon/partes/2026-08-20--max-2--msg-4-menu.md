# Parte de cierre — Max-2 · MSG-4 · La puerta del chat en el menú (CIERRA el chat)

CUENTA: Max-2 (Forjador B, stream 2) · MODELO: Fable 5 (fijado por el dueño)
TAREA: **MSG-4** — GO del dictado v29 (PLAN-MENSAJES §5.5/§5.7, el ÚLTIMO lote)
ESTADO: **HECHA** — pide doble llave
/usage INICIO → FIN: n/d (lo captura el dueño)
SIGUIENTE: doble llave de `feature/msg-4-menu` → **el chat queda COMPLETO** →
QA del dueño en celular (las 4 etapas juntas) → cierre de PLAN-MENSAJES.

## EVIDENCIA

Rama **`feature/msg-4-menu`** desde main fresco (`ac53bb4`), pusheada — 1 commit
de código, suite COMPLETA verde:

| Commit | Qué | Suite |
|---|---|---|
| `19852e7` | Ítem «Mensajes» de primer nivel (32→33) + badge declarativo + ícono nuevo + retiro del Volver de la huérfana + fuente única del contador + 2 tests netos | **2246 / 15.636** (baseline 2244 / 15.599 en `ac53bb4` — delta EXACTO +2) |

**Los candados del dictado, verificados:**
1. El ítem aparece en la sidebar CON permiso y NO sin él («33 con chat, 32 sin»
   expresado por presencia de la route — los tests del menú no hardcodean
   totales, verificado antes de elegir la forma).
2. Badge con cifra exacta: 2 no-leídos → `title="2 mensaje(s) sin leer"`
   (contrato del title) y AUSENTE en cero.
3. La huérfana ya no es huérfana: el Volver salió de `mensajes/index` y su test
   se INVIRTIÓ (de exigirlo a prohibirlo);
   `VolverTest::test_ningun_item_del_menu_lleva_volver` la cubre ahora SOLO
   (deriva de `items()`). Las hijas (hilo, nuevo) conservan su Volver a la lista.
4. Los candados estructurales del menú derivaron SOLOS del ítem nuevo:
   MenuPrincipalTest (route/permiso/ícono/patrón) + SidebarTest (aria-current
   único) + VolverTest — 77/77 en la batería dirigida sin tocarlos.
5. **MUTADO**: resolver del badge cegado (0 siempre) → rojo exacto en la cifra
   → `git checkout --` → verde.

## Decisiones que valen registro

- **Posición por vecindad de uso** (lo que v29 pidió argumentar): entre
  Aprobaciones y Servicio Técnico — la familia de superficies personales
  transversales (Mi producción · Mis entregas · Aprobaciones · Mensajes),
  ningún módulo de dominio puede ser su anfitrión. Sin `imprenta` (no es
  rótulo de operario en planta).
- **FUENTE ÚNICA del contador** (declarada, como pidió v29):
  `Conversacion::noLeidosDeUsuario()` — dos sums indexados sin raw — la usan
  el resolver del badge Y `firmaChat` del poll de MSG-3 (que perdió su closure
  de suma). Si contaran distinto, el menú diría una cosa y el refresco otra.
- **Ícono nuevo** `chat-bubble-left-right` (Heroicon outline) con la plantilla
  anti-footgun del tamaño (doctrina 24-07) — no existía ícono de chat.
- **SIN card del Inicio a propósito**: v29 no la dicta y agregarla tocaría
  `AccesosDashboard` (territorio DASH-3 de Max-1) — cruce evitado, declarado.
  Si el dueño la quiere tras el QA, es una línea en el zócalo.
- Badge SIN poll (doctrina campanita vigente): cuenta al render con el TTL de
  10s del caché de badges de la casa.

## Verificación adicional (gate propio)

- **Volcado 1280/375** (BD en memoria + molde propio): a 1280 el ítem visible
  entre Aprobaciones y Servicio Técnico con ícono + badge «2» y su title; a
  375 el ítem vive en el drawer con el mismo badge. Cero overflow.
- Batería dirigida **77/1.759**: Mensajes completo + MenuPrincipalTest +
  SidebarTest + VolverTest — a la primera, cero ediciones en los estructurales.
- **Bundle intacto** (hash idéntico `app-BOeyNfqy.css`): el ícono usa las
  clases existentes del componente.

## PLAN-MENSAJES: las 4 etapas construidas

MSG-1 motor (par canónico + ráfaga) → MSG-2 pantallas → MSG-3 refresco →
MSG-4 menú. El chat quedó entero: se entra por el menú con contador, avisa por
campanita/correo con anti-spam, se refresca solo y jamás mostró un 403 desde
el menú. Queda en manos del QA del dueño en celular.
