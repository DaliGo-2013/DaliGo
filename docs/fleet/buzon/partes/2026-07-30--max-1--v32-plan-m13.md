# Parte — Max-1: dictado v32 · `PLAN-M13.md` sellado, espera VISTO BUENO del dueño · 2026-07-30

> De Max-1 (Forjador A) al Director. Rama `feature/m13-devoluciones` desde main fresco
> (`b57c672`). **Cero código de producción: solo el plan**, como ordena el dictado.
> El gate no es doble llave todavía — es el **visto bueno de Mauricio antes de la primera
> migración**, misma regla que M14.

## Lo que cambió respecto del encuadre del dictado (3 cosas, verificadas)

Ninguna invalida el GO; las tres afectan qué se puede prometer, y el dictado pidió
explícitamente que el dueño apruebe «con los ojos abiertos».

**1. El bloqueo del stock no es D-003, es D-005 — y D-005 dice que M13 NO depende de ella.**
Textual (`docs/DECISIONES.md:144`): *«Mientras tanto: M05-F1, **M13**, diseño de M07 no
dependen; el espejo read-only ya funciona.»* D-003 (bodegas virtuales) sí importa, pero por otra
razón: define **a qué bodega** reingresa lo devuelto — hay una `BODEGA MERMAS` y una
`CONTENEDORES` sin clasificar. Eso sale por configuración y no bloquea el lote.

**2. El «Hecho cuando» de E6 exige justo lo que el recorte quitaba.** Textual
(`RUTA-MAESTRA.md:224`): *«…desde el link público del cliente **hasta el reingreso a stock**;
límites de upload verificados por IA-cPanel»*. Con el recorte, E6 no podía cerrarse nunca.

**3. Existe un camino intermedio que el dictado no contempló, y ya está sancionado en casa.**
M11 escribe `produccion_movimientos`, un **kardex local que «NUNCA toca `stocks`/`bodegas`»**
(`HANDOFF.md:376`). Ese es exactamente el problema de M13, ya resuelto una vez.

## Tres decisiones de alcance, consultadas y selladas con el dueño

Se las planteé antes de escribir el plan, para no entregarte un documento lleno de preguntas
abiertas. Las tres quedaron **con su recomendación**:

| Punto | Decisión | Efecto |
|---|---|---|
| Reingreso a stock | **Kardex local propio** (patrón M11) | E6 puede cerrarse; el dato queda listo para empujar cuando exista M04 |
| Fotos obligatorias | **Los dos momentos**: cliente **y** bodega | Es lo que pide la biblia («al ser recibido en bodega») y lo único que sirve para un reclamo al transportista |
| Transportista | **Dentro, mínimo**: transportista + N° de seguimiento | Barato ahora; carísimo de retro-llenar después |

Las dos últimas salen de la biblia (`PROYECTO_DALIGO.md`, en la **raíz**, no en `docs/`), que
lista funcionalidades que el dictado no nombraba.

## El plan

`docs/planes/PLAN-M13.md`, familia «plan fino» (M14 / M15 / DESPACHOS-V1), 199 líneas.

- **Sello honesto:** no `VIGENTE` sino **`PROPUESTA (sin código)`**, la variante de PLAN-M16-V1.
  Decir «verificado contra el código» cuando el módulo tiene cero código sería falso.
- **§0 es una tabla de evidencia de 18 filas**, todas con `archivo:línea`. **Las verifiqué una
  por una antes de commitear** y una estaba mal: afirmaba «cero hits de `devoluc`» y hay 5. No
  son de M13 —son el `devolver` de M11, la glosa de la guía de despacho y las notas de crédito
  de Bsale— así que la fila ahora dice eso y de paso **documenta la colisión de vocabulario con
  evidencia**, que vale más que la afirmación original.
- **4 tablas**: `devoluciones`, `devolucion_items` (los dos nombres los fija la biblia §5),
  `devolucion_fotos` (con **`origen`** = `cliente|bodega`, que es lo que hace posible la decisión
  de los dos momentos) y `devolucion_movimientos` (el kardex local).
- **Cada decisión nombra su alternativa descartada**, que es la regla de estilo de la casa.

## Lo que NO se reinventa (todo verificado en el código)

- **M14**: 3 puntos de registro + handler con **su propio `lockForUpdate`** y chequeo de
  `objetivo_updated_at`. Dos trampas que el plan deja escritas: `solicitar()` **aplica el efecto
  inline** cuando no supera el umbral (el llamador nunca aplica), y **`monto` nunca puede ir
  `null`** o cae a pendiente siempre. El umbral no crea clave: `umbral_aprobacion_clp` ya está
  sembrado y **PLAN-M14 lo reservó textualmente para las reglas monetarias de M13**.
- **M15**: 3 eventos. **Las claves nuevas NO necesitan migración one-shot** — el `firstOrCreate`
  del seeder las crea en el deploy; la one-shot solo hace falta al EDITAR un texto ya entregado.
- **Ruta pública**: se copia la variante **endurecida** (cotización/confirmación, GET **y** POST
  firmados), no la del QR de M12 que dejó el POST sin firma — `P-F3-01` ya arrastra «revisión de
  seguridad de rutas públicas (M12/**M13**)» como deuda y este módulo nace sin agregarle.
- **Fotos**: nunca la regla `image` (revienta con HEIC de iPhone), y la compresión en el
  navegador no es un lujo: una foto de 12MP consume ~104 MB en GD y produce un 500.

## Dos cosas que necesito de ti

**1. Enmendar el «Hecho cuando» de E6.** Con el kardex local se cumple en términos **locales**,
pero no hay escritura real en Bsale y no la habrá hasta M04/D-005. Propuesta de redacción:
*«…hasta el cierre de la devolución (reembolso aprobado o movimiento de reingreso registrado en
el kardex local)»*. Si preferís dejarlo como está, E6 queda abierta a propósito y hay que decirlo.

**2. El dato de cPanel.** El «Hecho cuando» exige límites de upload verificados. `public/.user.ini`
está commiteado con `post_max_size=30M` y `upload_max_filesize=12M`, pero **nadie verificó que
LiteSpeed los honre** y `max_file_uploads` no está fijado (default 20). Importa antes de fijar el
validador: si el techo real es menor, un envío con varias fotos **se pierde en silencio** —PHP
descarta el body entero, incluido el `_token`— y el cliente lee «Tu sesión expiró», que es mentira.
La delegación está redactada en §5 del plan, lista para despachar.

## Riesgos que el plan declara

El más concreto: **`throttle:6,1` es compartido por las 15 rutas públicas** y la firma del
limitador no incluye la ruta, así que un GET→POST→GET ya gasta 3 de 6. Con fotos y un reintento
el cliente queda fuera con un 429. Se propone throttle propio para el grupo de M13.

Los otros: M09 no existe (la fuente principal de devoluciones según la biblia no tiene
integración; el canal se captura a mano), M05 no emite (M13 registra la **decisión** de reembolso,
**no emite nota de crédito** — y eso es territorio activo de Marcos), y una trampa de numeración
que dejé anotada: en la matriz de D-002 **«M13» significa caja/tesorería**, no Devoluciones.

## De paso

`docs/planes/README.md` §5 llevaba 3 días afirmando que `PLAN-DESPACHOS-V1` «no existe en el
repo» — entró el 27-07 en `2d0ec92`. Es el mismo defecto que esa sección denuncia, ocurriendo en
su propio texto. Corregido, con inventario de la carpeta y la regla de actualizarlo en el mismo
push que agrega un plan.

## Verificación

- Plan conforme al `README` de la carpeta: sello en línea 2, §0 con evidencia real, pasos **1:1**
  con RUTA-MAESTRA, **sin marcas `[x]`** (regla de estado único).
- Las 18 citas `archivo:línea` verificadas una por una; 1 corregida.
- Sin Blade → sin build. Cero código de producción.
- **Suite completa: 1199 verdes / 7.843 aserciones, cero rojos** — **idéntica a la baseline**, que
  es lo que corresponde: este lote no toca una línea de código de producción. La corrí igual
  porque el dictado lo pide antes de cualquier push, y porque un lote «solo docs» que mueve el
  número sería justamente la señal de que no era solo docs.

## Consumo

Talla **M** (3 sweeps de exploración en paralelo + lectura de la biblia y las decisiones + plan
sellado + verificación cita por cita). `/usage`: Mauricio, cuando puedas.
