# Anexo — Paquete de re-aplicación del lote NOTIF-1 sobre main del 27-07

> Producido por el Director el 2026-07-27 con workflow adversarial (4 lentes de análisis +
> refutadores independientes por archivo) sobre `origin/main` @ `2479353` y la rama
> `feature/notif-especificas` @ `e6fbcc6` (base común `61da8ea`, 22-07).
> **Los refutadores corrigieron al análisis inicial** (indentaciones falsas, cifras infladas,
> un riesgo estrella equivocado) — lo que sigue es la versión ya refutada.
> Verificaciones propias del Director, aparte del workflow:
> - `urlDestinoPara()` es wrapper PURO (`return $puede ? $url : null`): el ancla
>   `#aprobacion-{id}` sobrevive intacta al gate de permisos. El conflicto de
>   `Notificacion.php` es TEXTUAL, no semántico.
> - La migración one-shot del lote SIGUE VÁLIDA: las plantillas de aprobación del seeder no
>   cambiaron desde el 22-07 (el único toque de main fue agregar
>   `notif_plantilla_taller_ingresado`), y la one-shot de Marcos (`2026_07_22_180000`) opera
>   sobre claves 100 % disjuntas (cotización/terreno).
> - Baseline de main verificada en worktree limpio: **920 verdes / 4.418 aserciones**.

## Los dos modos de fallar (leer antes de tocar nada)

1. **Resolver «a favor de main»** (`-X ours`, `checkout --ours`, o «main va 122 commits
   adelante») → archivo limpio, compila, **pasa TODOS los tests de main**… con el ancla
   DESAPARECIDA. Falla silenciosa; el único test que la caza es `NotificacionEspecificaTest`.
2. **Resolver «a favor de la rama»** → revierte `urlDestinoPara()` → `urlDestino()` y
   **reabre el 403 del vendedor** que main arregló (4 asserts en `AprobacionAccionableTest`
   lo cubren), y devuelve el N+1 al perder `->with('notificable')`.

La resolución correcta NO es una dirección: es tomar main y re-injertar quirúrgicamente.

---

######################################################################
## resources/views/notificaciones/index.blade.php -> RE-APLICABLE_QUIRURGICO

* [APLICAR] Resolver el conflicto REEMPLAZANDO EL ARCHIVO COMPLETO por la version de main (65 lineas, verificada). **⚠️ CORRECCION DEL DIRECTOR 27-07 (post-mortem, la cazo Max-1 antes de commitear): la forma que este anexo recomendaba originalmente — `git show origin/main:<archivo> > <archivo>` — ES PELIGROSA EN ESTA MAQUINA.** El `>` de PowerShell 5.1 es `Out-File`: inyecta **BOM + CRLF**. En un Blade el BOM se emite antes del HTML; en `manifest.json` hace fallar el `json_decode` de Vite y **la app queda sin CSS/JS en runtime**. Y `git status` solo delata el CRLF, no el BOM. **Forma correcta: `git checkout origin/main -- <archivo>`** (lo escribe git, no el shell), o `git checkout --ours <archivo>` cuando HEAD ya es main. Comprobar con `file -b <archivo>` antes de commitear. Ojo con el matiz de direccion: `--ours` es main solo si HEAD es main (merge de la rama SOBRE main); si alguien mergea main DENTRO de la rama, `--ours` es la RAMA y produce el desastre del riesgo inverso — por eso la forma con `origin/main --` explicito es la mas segura. Solo usar 'git checkout --ours' si HEAD es main (merge de la rama sobre main, o rebase de la rama sobre main donde --ours = upstream = main): si alguien mergea main DENTRO de feature/notif-especificas, --ours es la RAMA y produce exactamente el desastre del riesgo inverso. Confirmado en main linea 1: <x-app-layout ancho="formulario"> (la rama y la base usan <x-app-layout> pelado) y confirmado que el wrapper <div class="mx-auto max-w-xl px-4 sm:px-6 lg:px-8"> existe en base:5 y rama:5 y NO existe en main: no reintroducirlo.

* [APLICAR] Re-aplicar el UNICO cambio del lote en este archivo: agregar whitespace-pre-line al span del CUERPO. donde: main linea 39, con 32 ESPACIOS de indentacion (NO 36: 36 es la indentacion de la rama, ver refutacion 1). Es el segundo span dentro de span.min-w-0.flex-1, entre el span del titulo (main:33-38) y el del created_at (main:40). ANTES (32 espacios): <span class="mt-0.5 block text-sm text-neutral-500">{{ $n->cuerpo }}</span> DESPUES (32 espacios): <span class="mt-0.5 block whitespace-pre-line text-sm text-neutral-500">{{ $n->cuerpo }}</span>. Token entre 'block' y 'text-sm', igual que la rama y que la convencion de main en admin/servicio-tecnico/show.blade.php:101. Verificado que es la unica edicion: git diff 61da8ea origin/feature/notif-especificas -- <ruta> devuelve exactamente 1 linea cambiada. Nota: no hay ordenador de clases Tailwind en package.json (solo tailwindcss ^4 + @tailwindcss/vite), asi que el orden es cosmetico, no lo exige ningun linter.

* [APLICAR] Comprobacion de obsolescencia CONFIRMADA: main NO agrego whitespace-pre-line en este archivo por su cuenta. git grep -n whitespace-pre origin/main -- resources/views devuelve exactamente 4 hits y ninguno es este archivo: admin/notificaciones/index.blade.php:86 (pre-wrap, ultimo_error) y admin/servicio-tecnico/show.blade.php:101, 106, 141. El cambio del lote SIGUE SIENDO NECESARIO.

* [APLICAR — NUEVO, la brecha que el analisis no vio] El cambio del paso 2 NO TIENE COBERTURA DE TESTS EN NINGUNA DE LAS DOS RAMAS: git grep -n whitespace-pre origin/main -- tests y ... origin/feature/notif-especificas -- tests devuelven 0 hits. El test del lote para la campanita (tests/Feature/Notificaciones/NotificacionEspecificaTest.php:55 test_la_campanita_muestra_el_cuerpo_de_la_notificacion) asserta el TEXTO del cuerpo, no la clase. Conclusion: si el token se pierde al resolver, la suite queda VERDE y nadie se entera. Verificar a ojo el render post-merge, o agregar un assertSee('whitespace-pre-line', false) sobre route('notificaciones.index').

* [NO-OP / NO TOCAR — reetiquetado, ver refutacion 3] El enlace de la fila se queda tal cual esta en main: main:27 $destino = $n->urlDestinoPara(auth()->user()); con el comentario de 24-26 sobre el 403 del vendedor. NO revertir a $n->urlDestino() (la rama usa eso en su linea 25). Verificado que el ancla del lote fluye intacta por transitividad: app/Models/Notificacion.php:116 hace $url = $this->urlDestino(); y :134 hace return $puede ? $url : null; — devuelve el MISMO string, ancla incluida. Cero ediciones en el blade por este punto. Efecto secundario aceptado y correcto: sin el permiso del evento, urlDestinoPara devuelve null, la fila deja de ser link y desaparece el chevron (main:42-44) — es el fix de main contra el 403, no una regresion del lote.

* [APLICAR — archivo distinto, condiciona esta vista] El ancla va DENTRO de urlDestino(), nunca en la vista ni en urlDestinoPara(). Injertar en el urlDestino() de main (app/Models/Notificacion.php:159-176) la linea de la rama (Notificacion.php:128) $ancla = $this->notificable_id ? '#aprobacion-'.$this->notificable_id : ''; y concatenarla en los brazos 'aprobacion.solicitada','aprobacion.escalada' => route('aprobaciones.index').$ancla y 'aprobacion.resuelta' => route('aprobaciones.mias').$ancla. PRESERVAR el brazo 'taller.ingresado' => route('admin.servicio-tecnico.index') que main agrego en :166 y que la rama NO tiene (verificado: el urlDestino de la rama, lineas 123-142, carece de ese caso). VERIFICADO TEST-SAFE: main AprobacionAccionableTest:53-55 y :150 hacen assertSame(route('aprobaciones.index'), ...->urlDestino()) — no se rompen porque el helper notifDe() (main:38-45) NO setea notificable_type/notificable_id, asi que $ancla queda '' ; la rama conserva esas mismas lineas 53-56 sin tocarlas, lo que lo corrobora. VERIFICADO QUE EL ANCLA SI SE PUEBLA EN PRODUCCION: Aprobaciones.php:279-288 (notificarRol) y :290-304 (notificarSolicitante) pasan $aprobacion como $origen, y NotificacionDispatcher.php:59-60 escribe notificable_type/notificable_id desde el ; por lo tanto notificable_id ES el id de la Aprobacion, que es justo lo que emiten los id="aprobacion-{{ $aprobacion->id }}".

* [APLICAR — dependencia externa del ancla] Resolver a favor de la RAMA los dos emisores de anclas, o el ancla no aterriza: resources/views/aprobaciones/index.blade.php:28 <div id="aprobacion-{{ $aprobacion->id }}" ... class="... scroll-mt-6 ... target:ring-2 target:ring-brand-300"> y resources/views/aprobaciones/mias.blade.php:22 <li id="aprobacion-{{ $solicitud->id }}" class="... scroll-mt-6 ... target:ring-2 target:ring-inset target:ring-brand-300">. Traer tambien scroll-mt-6 y las clases target:ring-* (son el efecto de aterrizaje, no decoracion). Verificado: main tiene CERO ocurrencias de id="aprobacion- en todo resources/views. Ojo: main tambien reindento/movio esos dos archivos (sus filas equivalentes caen ~7 lineas mas abajo), asi que ahi tampoco sirve resolver linea por linea.

* [APLICAR — campanita, resources/views/layouts/partials/campanita.blade.php] Re-agregar TRES cosas de la rama, no solo el span (ver refutacion 5): (a) rama:32 <input type="hidden" name="ir" value="1"> dentro del form de la fila — es el CABLEADO de la navegacion; sin el, la feature 'campanita navegable' queda muerta aunque el cuerpo se vea; (b) rama:35-39 el bloque @if (filled($dgN->cuerpo)) con <span class="mt-0.5 line-clamp-2 whitespace-pre-line text-xs text-neutral-500">{{ $dgN->cuerpo }}</span> — conservar line-clamp-2 y NO agregarle 'block' (el comentario de la rama en :36-37 lo explica: line-clamp-2 define su propio display:-webkit-box y un display:block posterior lo anularia); (c) rama:40 el mt-0.5 que se le suma al span del created_at. main solo tiene el titulo en :32 y el created_at en :33.

* [APLICAR — controlador, app/Http/Controllers/NotificacionUsuarioController.php] Conservar el ->with('notificable') de main:28 (con su comentario 25-27): la rama no lo tiene (su :25 es el orderByRaw) y sin el la bandeja vuelve al N+1 porque urlDestinoPara consulta $this->notificable->esVisiblePara($user) por fila. Y al traer el redirect ?ir=1 de la rama (:44 if ($request->boolean('ir') && ($url = $notificacion->urlDestino())) { return redirect($url); }) cambiarlo a urlDestinoPara($request->user()) con caida a back() si es null, o la campanita navega SIN el gate que la bandeja si respeta. Aviso al integrar: los tests del lote NotificacionEspecificaTest:71, :82 y :98-100 assertan ese redirect, asi que sus fixtures pueden necesitar el permiso del evento para seguir verdes con urlDestinoPara.

RIESGO: CONFIRMADO (con cifra corregida a 45 lineas, no 109): el conflicto es ~90% reindentacion por el wrapper eliminado. De las 21 lineas de cada lado, la unica diferencia semantica es $destino = urlDestinoPara(auth()->user()) vs urlDestino() mas 3 lineas de comentario; el resto es +4/-4 espacios. Resolver a mano linea por linea reintroduce la indentacion vieja o el div mx-auto max-w-xl y rompe el ancho unificado de main.

RIESGO: CONFIRMADO (riesgo inverso, el mas grave): resolver a favor de la rama revierte urlDestinoPara -> urlDestino y reabre el 403. Verificados verbatim los 4 asserts de main en tests/Feature/Aprobaciones/AprobacionAccionableTest.php:106 (assertSame de la cotizacion de su cartera), :123 (assertNull otra cartera), :139 (assertNull sin permiso servicio tecnico), :149 (assertNull sin 'aprobar solicitudes'). Ademas :125 y :150 ejercitan urlDestino() en paralelo, y :156 assertDontSee('active:scale-[0.98]').

RIESGO: CONFIRMADO Y AMPLIADO: aceptar el hunk ENTRANTE por UI (no solo checkout --theirs) duplica el cuerpo y el timestamp de cada fila, porque el lado 'theirs' del conflicto termina a mitad de estructura y la cola con los spans de main se auto-mergea. Falla silenciosa: compila y la suite queda verde.

RIESGO: CONFIRMADO verbatim: main agrego ->with('notificable') en NotificacionUsuarioController.php:28 (con comentario en 25-27) precisamente porque urlDestinoPara consulta $this->notificable->esVisiblePara($user) por fila. La rama no lo tiene (su :25 es el orderByRaw). Tomar el lado de la rama devuelve el N+1 de ~20 queries por pagina.

RIESGO: CONFIRMADO verbatim: la rama agrega en NotificacionUsuarioController.php:44 el redirect if ($request->boolean('ir') && ($url = $notificacion->urlDestino())) { return redirect($url); }. Sobre main eso navega SIN el gate de permisos: la campanita puede caer en el 403 que la bandeja evita. Incoherencia real entre superficies. Aviso adicional propio: los tests del lote NotificacionEspecificaTest:71, :82, :98-100 assertan ese redirect y pueden requerir ajuste de fixtures al pasar a urlDestinoPara.

RIESGO: CONFIRMADO en substancia (numeros de linea corregidos a 28 y 22): main tiene CERO id="aprobacion- en resources/views, asi que si aprobaciones/index.blade.php y mias.blade.php se resuelven a favor de main el ancla no matchea nada — navega igual, sin scroll a la tarjeta. Degradacion silenciosa. Ademas main reindento/movio esas dos vistas, asi que tampoco sirve resolverlas linea por linea.

RIESGO: CONFIRMADO: main renderiza solo el titulo en campanita.blade.php:32; el cuerpo con line-clamp-2 whitespace-pre-line es de la rama (:38) y hay que re-agregarlo entero. El line-clamp-2 es obligatorio y el comentario de la rama (:36-37) documenta ademas que NO lleva 'block' a proposito (line-clamp-2 define display:-webkit-box y un block posterior lo anularia).

RIESGO: CONFIRMADO: en la bandeja el span del cuerpo no tiene truncate ni line-clamp (main:39 = 'mt-0.5 block text-sm text-neutral-500'; el truncate de la fila esta en el span del TITULO, main:33). Con whitespace-pre-line las filas de aprobacion crecen a 3-4 lineas y cambia la densidad mobile-first. Es el efecto buscado, pero merece revision visual post-merge.

RIESGO: NUEVO CONFIRMADO: cero cobertura de tests para el token whitespace-pre-line en ambas ramas (git grep whitespace-pre -- tests = 0 hits en main y en la rama). Si la unica edicion de este archivo se pierde al resolver, nada la detecta.
######################################################################
## resources/views/aprobaciones/index.blade.php + resources/views/aprobaciones/mias.blade.php -> RE-APLICABLE_QUIRURGICO

* [CONTEXTO VERIFICADO — el conflicto real] Reproduje el merge 3-vias con `git merge-file -p <main> <61da8ea> <rama>` sobre los tres blobs reales. CADA archivo tiene EXACTAMENTE UN hunk de conflicto: index.blade.php lo abre en `@else` (lineas 25-55 del output mergeado) y mias.blade.php en `@else` (lineas 19-84). Todo lo demas (cabecera, wrapper, y en index TODO el cuerpo de la tarjeta desde `</div>` en adelante) se auto-resuelve a main SIN marcadores. Lado main del hunk de index = `@else` + comentario `{{-- Una seccion por categoria... --}}` + `@foreach ($pendientes->groupBy('tipo_accion')->sortKeys() as $tipoAccion => $delTipo)` + `<section>` + div de cabecera + `<h2>` + `<x-badge>`. Lado notif = `@endif` + `@foreach ($pendientes as $aprobacion)` + la tarjeta anclada. RESOLUCION: tomar el lado main COMPLETO, borrar el lado notif COMPLETO, y despues aplicar quirurgicamente las instrucciones 1 y 2 sobre el markup sobreviviente de main.

* [APLICAR · 1 de 2] Ancla de la tarjeta de la bandeja. Archivo: resources/views/aprobaciones/index.blade.php. Objetivo: el `<div>` con `x-data` del panel de rechazo que abre justo bajo `@foreach ($delTipo as $aprobacion)`. VERIFICADO en origin/main: lineas 34/35/36; indentacion real 24 espacios en `<div`, 29 en la continuacion `class=`. Base 61da8ea lo tenia en 27-28 tras el `@endif` de la 24 (confirmado), asi que el movimiento al brazo `@else` es de main. Lista de clases IDENTICA en base y main (`dg-enter rounded-2xl border border-neutral-200 bg-white shadow-sm`) -> la re-aplicacion es textual, sin adaptacion. Reemplazar:

                        <div x-data="{ paneles: { rechazo: false } }"
                             class="dg-enter rounded-2xl border border-neutral-200 bg-white shadow-sm">

por:

                        {{-- id: ancla de aterrizaje de la campanita (urlDestino puntual, lote NOTIF-1). --}}
                        <div id="aprobacion-{{ $aprobacion->id }}" x-data="{ paneles: { rechazo: false } }"
                             class="dg-enter scroll-mt-6 rounded-2xl border border-neutral-200 bg-white shadow-sm target:ring-2 target:ring-brand-300">

El `id` va ANTES de `x-data` (igual que en la rama). La continuacion sigue a 29 espacios: `<div ` son 5 chars sobre 24 de sangria, asi que 29 alinea con `id=` igual que hoy alinea con `x-data=`. CORRECCION al analisis: NO buscar 'linea 35-36' en el archivo con conflicto resuelto — ahi cae en 59-60 del output mergeado. Anclar por el `@foreach ($delTipo as $aprobacion)` que lo precede, no por numero de linea.

* [APLICAR · 2 de 2] Ancla de la fila de «Mis solicitudes». Archivo: resources/views/aprobaciones/mias.blade.php. Objetivo: el `<li>` de cada solicitud bajo `@foreach ($delTipo as $solicitud)`. VERIFICADO en origin/main: `<div class="dg-enter rounded-2xl ...">` en 27, `<ul class="divide-y divide-neutral-100">` en 28, `@foreach` en 29, `<li>` en 30 con 32 espacios de sangria. Reemplazar:

                                <li class="space-y-1 px-4 py-4 sm:px-6">

por:

                                {{-- id: ancla de aterrizaje de la campanita (urlDestino puntual, lote NOTIF-1). --}}
                                <li id="aprobacion-{{ $solicitud->id }}" class="space-y-1 scroll-mt-6 px-4 py-4 sm:px-6 target:ring-2 target:ring-inset target:ring-brand-300">

Mantener `target:ring-inset` (y NO el ring normal de index): el `<li>` sigue dentro de un contenedor `rounded-2xl` con `divide-y`, donde un ring no-inset se sale del borde redondeado. VERIFICADO que `ring-inset` NO es un utility muerto en v4: existe en el dist instalado como `t("ring-inset",[k,["--tw-ring-inset","inset"]])` y main ya lo usa 7 veces en resources/views (una de ellas en index.blade.php:49). Variable del loop en main sigue siendo `$solicitud` -> interpolado textual. Post-resolucion la linea cae en 31 del output mergeado.

* [NO-OP · sustituye a la instruccion 3 del analisis] Wrapper de pagina y comentario de cabecera: NO HAY NADA QUE HACER Y NO HAY CONFLICTO. `git merge-file` demuestra que index.blade.php:1-24 y mias.blade.php:1-18 se auto-resuelven a main sin marcadores (base == rama en esa region, asi que el 3-way toma main solo). Queda `<x-app-layout ancho="formulario">` + `<div class="space-y-4 py-6">` en ambos, y el comentario extendido de main (index 5-6 y mias 3-4, los que añaden «SEPARADAS POR CATEGORIA») que el analisis ni menciona. DATO A NO 'ARREGLAR': `ancho="formulario"` resuelve a `max-w-3xl` (resources/views/layouts/app.blade.php:45), no al `max-w-2xl` de la rama — la pagina queda MAS ANCHA que en el lote. Es decision deliberada de main (el centrado unico vive en layouts/app.blade.php:67 con `$maxW`); no restaurar el `mx-auto max-w-2xl` de la rama.

* [VERIFICAR · reemplaza y amplia la instruccion 4] Correr: `php artisan test --filter='NotificacionEspecificaTest|AprobacionCategoriaTest|AprobacionBandejaTest|AprobacionHistorialTest|AprobacionAccionableTest|CampanitaTest|NotificacionPanelTest'`. Los cuatro originales estan verificados: tests/Feature/Notificaciones/NotificacionEspecificaTest.php:123-137 es `test_la_bandeja_y_mis_solicitudes_emiten_el_ancla` y exige literal `id="aprobacion-{id}"` con `assertSee(..., false)` en AMBAS rutas (es el UNICO test que fija el markup); AprobacionCategoriaTest.php es nuevo en main (no existe en 61da8ea ni en la rama) y solo asserta labels + `assertSeeInOrder` de descripciones, no markup exacto, asi que el id y el comentario no lo rompen; BandejaTest e HistorialTest solo assertan textos y hrefs. AÑADIDOS OBLIGATORIOS que el analisis omitio: (a) AprobacionAccionableTest — es el test que fija el STRING EXACTO de `urlDestino()` con `assertSame(route('aprobaciones.index'), ...)` en :53-56, justo donde la rama concatena `$ancla`; pasa solo porque su helper `notifDe()` deja `notificable_id` nulo (verificado identico en main y rama), pero cualquier retoque ahi lo revienta. (b) CampanitaTest + NotificacionPanelTest — porque main reescribio la superficie que consume el ancla.

* [VERIFICAR · gate de CSS, ausente del analisis] `target:` no tiene NI UNA ocurrencia previa en todo `resources/` de main (`git grep -n 'target:' origin/main -- resources` = 0). Estas dos anclas son las PRIMERAS usuarias del variant, y ningun test de PHPUnit puede detectar una clase que no compilo. Añadir al gate: `npm run build` y despues grep de `:target` en el CSS generado en public/build. Verificado que DEBE compilar: tailwindcss 4.3.0 instalado registra el variant como `i("target",["&:target"])` en node_modules/tailwindcss/dist/lib.js, y `--color-brand-300: #fdba74` existe en resources/css/app.css:20. Las clases tienen que quedar en los .blade y nunca migrarse a app/: app.css:5 declara `@source '../**/*.blade.php'`, que enraiza el scan en resources/.

RIESGO: [RIESGO #1 · NUEVO, el que hay que encabezar — PERDIDA SILENCIOSA DEL ANCLA] index y mias tienen UN solo hunk de conflicto cada uno, y en AMBOS la totalidad del aporte de la rama (la linea con `id` + `scroll-mt-6` + `target:ring`) vive dentro del lado notif. Resolver con «me quedo con main, que va 122 commits adelante» (o `-X ours`, o `git checkout --ours`) produce un archivo limpio, que compila, sin residuo de marcadores, y que pasa TODOS los tests de main — con el ancla desaparecida. El unico test que lo caza es NotificacionEspecificaTest. Esto es mucho mas probable que la duplicidad de ids que reportaba el analisis, y es silencioso en vez de ruidoso.

RIESGO: [RIESGO #2 · NUEVO — la mitad de la feature vive fuera de estos dos archivos] app/Models/Notificacion.php TAMBIEN conflictua y sin ella las anclas son markup muerto que nadie enlaza. La rama inserta `$ancla = $this->notificable_id ? '#aprobacion-'.$this->notificable_id : ''` en Notificacion.php:128 y lo concatena en los brazos `aprobacion.solicitada|escalada|resuelta`. main, en el MISMO `match`, añadio el brazo `taller.ingresado` (main :162-164) y ademas un metodo NUEVO `urlDestinoPara(?User $user)` en :114 que filtra por permisos, y resources/views/notificaciones/index.blade.php:27 ahora llama `urlDestinoPara(auth()->user())` en vez de `urlDestino()`. Encima main enlaza directo con `href="{{ $destino }}"` (notificaciones/index.blade.php:30) mientras la rama pasaba por `?ir=1` en NotificacionUsuarioController.php:44 — otro conflicto aparte. Re-aplicar solo las dos blades deja anclas huerfanas.

RIESGO: [CONFIRMADO] Variable del loop: en main la variable EXTERNA es `$delTipo` (la coleccion del grupo). El id debe salir de `$aprobacion->id` (index:34) y `$solicitud->id` (mias:29), que son los nombres que main conservo. Escribir `$delTipo->id` o dejar `$pendientes`/`$solicitudes` en el interpolado revienta o emite ids vacios.

RIESGO: [CONFIRMADO] Reindentacion: la tarjeta de index bajo de 12 a 24 espacios y el `<li>` de mias de 24 a 32 al entrar en el `<section>` de main. Un copy-paste de la version de la rama mete la sangria vieja; queda valido para Blade pero rompe el estilo del archivo y ensucia el diff.

RIESGO: [CONFIRMADO] Aterrizaje mudo cuando la aprobacion ya no esta en la lista: la bandeja solo pinta `$pendientes` (AprobacionController::index:34, `Aprobacion::bandejaDe($user)`), asi que si la notificacion se abre despues de que otro aprobo/rechazo, `#aprobacion-{id}` no existe y el navegador queda arriba sin feedback. Notificacion.php:128 solo cubre el caso `notificable_id` nulo. Agravado en main: con las secciones por categoria la pagina es mas larga, y la campanita ahora navega por `href` directo. El fallback sugerido es viable — verifique que `route('admin.aprobaciones.index')` SI existe en main (lo usa AprobacionCategoriaTest:99).

RIESGO: [CONFIRMADO] Tailwind: tailwindcss 4.3.0 instalado (package.json `^4.0.0`), `--color-brand-300: #fdba74` en resources/css/app.css:20, y AMBOS utilities/variants verificados en node_modules/tailwindcss/dist/lib.js — variant `i("target",["&:target"])` y utility `t("ring-inset",[k,["--tw-ring-inset","inset"]])`. `app.css:5` declara `@source '../**/*.blade.php'`, que enraiza el escaneo en resources/: estas clases deben quedar en los .blade y nunca migrarse a app/ (mismo aviso que main dejo en layouts/app.blade.php:42-44).

---

# PARTE 2 — Los 8 archivos SIN conflicto (2ª ronda del workflow, cerrada 27-07)

> El primer paquete (arriba) cubre los archivos CONFLICTIVOS. Esta segunda ronda audita los
> que automergean y **encontró cosas que cambian el trabajo**. Léela completa: hay un defecto
> real del lote, dos candados de main que lo rompen, y un cambio de alcance que afecta lo que
> el dueño va a ver en su teléfono.

## Correcciones a la PARTE 1 (resueltas por el Director con la rama como fuente de verdad)

- **Discrepancia de clases entre agentes, resuelta:** un agente propuso `target:ring-inset`
  en AMBAS vistas. FALSO. La rama de Max-1 usa `target:ring-2 target:ring-brand-300` en
  `aprobaciones/index` (tarjeta suelta) y **`target:ring-inset` SOLO en `mias`** (el `<li>`
  vive dentro de un contenedor `rounded-2xl` con `divide-y`, donde un ring no-inset se sale
  del borde). Vale la versión de la rama, que es la que está en la PARTE 1.
- **El merge tiene 5 conflictos, no 4:** `public/build/manifest.json` también conflictúa.
- **`campanita.blade.php` y `ConfiguracionSeeder.php`: NO editar a mano.** Automergean y el
  resultado ya es el correcto (el hidden `name="ir"`, el bloque `@if (filled($dgN->cuerpo))`
  con `line-clamp-2 whitespace-pre-line` sin `block`, y el `mt-0.5` del created_at aterrizan
  solos). Aplicar la receta de la PARTE 1 encima **duplica** el input y el span. Solo VERIFICAR.
- **Gate de CSS, matizado:** el bundle ya comiteado de main YA contiene
  `.target\:ring-2:target`, `.target\:ring-brand-300:target`, `.target\:ring-inset:target`,
  `line-clamp-2`, `whitespace-pre-line` y `scroll-mt-6` — pero **por accidente**: se filtraron
  vía el `@source '../../storage/framework/views/*.php'` de `resources/css/app.css:4`, es decir
  desde un caché de blades compilados de un árbol que tenía el lote aplicado. El riesgo de
  clase faltante es NULO hoy, pero **un grep verde NO prueba que compilaron desde tus
  `.blade`** — corre `npm run build` igual para que se regeneren legítimamente.

## Los 3 hallazgos nuevos que hay que atacar

1. **🔴 DEFECTO REAL del lote (el único), agravado:** el `ir=1` llama `urlDestino()` crudo, así
   que reabre el 403 que main cerró — **y `marcarLeida()` ya corrió antes del redirect**: el
   usuario pierde la notificación de la campanita Y aterriza en un 403. Ningún test de main lo
   caza porque `ir=1` es nuevo. Fix: `urlDestinoPara($request->user())` con caída a `back()`.
2. **🔴 CANDADO DE MAIN que pone la suite ROJA:** `tests/Feature/AnchoDePaginaTest.php:80-107`
   (`test_ninguna_vista_declara_su_propio_ancho_de_pagina`) tiene el regex
   `/class="mx-auto max-w-[0-9a-z]+ px-/`, y la versión del LOTE de
   `notificaciones/index.blade.php:5` lo matchea → **rojo determinista** si se resuelve a favor
   del lote. Límite verificado: las otras dos vistas del lote NO lo trippean (su `space-y-4`
   intercalado rompe la adyacencia). Otra razón para tomar main completo ahí y re-aplicar solo
   la palabra `whitespace-pre-line`.
3. **🔴 `manifest.json` conflictúa → Vite revienta en runtime** si queda un marcador de merge
   dentro del JSON, y quedan dos `app-*.css` (el `DLrdfNJe` del lote y el de main). Resolución:
   manifest de MAIN, borrar el CSS huérfano del lote, `npm run build` al final.

## Corrección al ajuste de tests (la PARTE 1 lo tenía a medias)
El caso espejo del usuario sin permiso **no puede reusar la notificación del jefe**: `leer()`
abre con `abort_unless($notificacion->user_id === $request->user()->id, 403)`, así que un
soplador posteando sobre una notificación ajena recibe 403, nunca `back()`. Debe crear SU
propia notificación. Y para el caso feliz: `tap(User::factory()->create())->assignRole('jefe_bodega')`
(ese rol sí tiene `aprobar solicitudes`).

## Refutación fatal — NO hacer
Usar `Configuracion::cacheKey($clave)` en la migración **revienta el deploy**: el método es
`private static`. El `'config.'.$clave` hardcodeado es la única opción (y es lo que hace la
one-shot de Marcos).

## Dos cosas para el DUEÑO (no son defectos, son expectativas)
- **El «leer y llegar en un solo tap» quedó desktop-only.** main dejó el dropdown de la
  campanita en `hidden lg:flex` (`sidebar.blade.php:34`) y en móvil la campana es un `<a>`
  directo a la bandeja (`topbar.blade.php:20`). En el teléfono el aporte real del lote es el
  cuerpo legible con saltos (`whitespace-pre-line`).
- **Incoherencia de destino que se va a notar:** la campanita aterriza en la tarjeta puntual
  (`#aprobacion-{id}`) pero el botón del correo apunta a la lista pelada, porque
  `payload['url']` (`Aprobaciones.php:303`) no lleva ancla. Igualarlo obliga a tocar los
  `assertSame` de `payload['url']` en `AprobacionAccionableTest`.

## Fragilidad estructural anotada (no bloquea, merece ficha)
La one-shot está verificada byte a byte contra el seeder de main **hoy**, pero es frágil por
diseño: cualquier commit futuro que toque esos 6 strings sin actualizar el `$viejo` la
convierte en **no-op silencioso**. Vale un candado que exija que el `$nuevo` de la migración
sea igual al valor que siembra el seeder. Aplica también a la one-shot de Marcos.

## Filtro de verificación definitivo (ampliado: main tocó 5 archivos de test más)
```
--filter='NotificacionEspecificaTest|AprobacionAccionableTest|AprobacionCategoriaTest|AprobacionBandejaTest|AprobacionHistorialTest|CampanitaTest|NotificacionPanelTest|NotificacionConfigSeedTest|NotificacionDispatchTest|AnchoDePaginaTest'
```
Y OJO: no «arreglar» el arnés `notifDe()` para que setee `notificable_id` — 4 asserts de main
(`assertSame(route(...), urlDestino())`) pasan HOY solo porque el morph queda vacío.

---

## Detalle íntegro de la 2ª ronda (12 instrucciones verificadas verbatim + riesgos)

## Lote notif-especificas — 8 archivos sin conflicto + 2 de test (re-verificacion adversarial contra origin/main 6f4c8c0, rama e6fbcc6, base 61da8ea) -> RE-APLICABLE_QUIRURGICO

* [APLICAR - CONFIRMADO VERBATIM] Instr.1 (seeder): SIN ACCION MANUAL. Verifique byte a byte: los 3 pares del $viejo de la migracion (lineas 22/23, 32/33, 42/43 del one-shot) son IDENTICOS a database/seeders/ConfiguracionSeeder.php de main lineas 72/73, 82/83, 92/93. `git log 61da8ea..origin/main -- database/seeders/ConfiguracionSeeder.php` devuelve UN commit: b42d645, que solo inserta notif_plantilla_taller_ingresado en 99-111 (aguas ABAJO del bloque de aprobacion). El comentario a reemplazar es 66-68. Confirmado con git merge-tree que este archivo AUTO-MERGEA sin conflicto: el lote reescribe solas las 6 lineas y el bloque taller_ingresado sobrevive intacto. Ademas verifique que NINGUN test de main asserta el texto literal de esas plantillas (`git grep 'Resuelvela|Ver tus solicitudes|Escalo a tu rol|Tu solicitud quedo' origin/main -- tests/ app/` = 0 hits; AprobacionAccionableTest:210 solo cuenta claves), asi que cambiar los 6 strings es test-safe.

* [APLICAR - CONFIRMADO, con la parte 'opcional' REFUTADA] Instr.2 (migracion one-shot): copiar database/migrations/2026_07_22_100000_actualiza_plantillas_aprobacion_notif1.php tal cual desde la rama. Verificado: el archivo NO existe en main; el one-shot de Marcos (2026_07_22_180000_enriquecer_plantillas_notificaciones_internas.php) esta identico en 61da8ea y en main (no es churn de estos 5 dias) y sus claves son 100% disjuntas (terreno_solicitada/terreno_rechazada/cotizacion_{enviada,respondida,autorizada} en sus lineas 74-90 vs aprobacion_* del lote). main agrego 7 migraciones (2026_07_22_120000 dashboard_colores, 07_23_120000 tiempos_reparacion, 07_23_130000 procedencia, 07_24_120000 limitar_tecnico_industrial, 07_24_130000 disponibilidad, 07_24_190000 index_por_confirmar, 07_24_200000 jefe_id) y NINGUNA toca plantillas ni rompe fixtures del lote (verifique la de procedencia: columna nullable con guard hasColumn). La invalidacion de cache sigue valida: Configuracion::get() usa self::cacheKey($clave) que devuelve 'config.'.$clave (app/Models/Configuracion.php:42-45). NO aplicar la sugerencia 'opcional' de usar Configuracion::cacheKey($clave) - ver refutadas.

* [APLICAR - CONFIRMADO VERBATIM, es el unico defecto real del lote] Instr.3 (ir=1 con gate): main app/Http/Controllers/NotificacionUsuarioController.php tiene marcarLeida() en 43 y `return back();` en 45 - EXACTO. urlDestinoPara(?User) esta en app/Models/Notificacion.php:114-135 - EXACTO, y es wrapper PURO ($url = $this->urlDestino() en 116; return $puede ? $url : null en 134), asi que el ancla del lote fluye intacta por transitividad. routes/web.php:71-72 tiene el middleware 'permission:aprobar solicitudes' - EXACTO. Insertar entre 43 y 45 el bloque del lote con urlDestinoPara($request->user()). Verifique ademas que la cobertura de eventos de urlDestinoPara es EXACTAMENTE la misma que la de urlDestino (aprobacion.*, taller.ingresado, cotizacion.*, terreno.*), asi que el cambio no introduce ningun degradado nuevo a back(): solo aplica el gate. El ->with('notificable') de main en index() (linea 28) se conserva SOLO por auto-merge (el lote no toca index()) - verificado presente en el arbol mergeado; no hay que hacer nada, pero tampoco borrarlo.

* [APLICAR - CORREGIDO] Instr.4 (test del lote): tests/Feature/Notificaciones/NotificacionEspecificaTest.php:71-80 es test_la_fila_de_la_campanita_marca_leida_y_navega_al_destino y su linea 73 es `$user = User::factory()->create();` - EXACTO. Cambiarlo a `$user = tap(User::factory()->create())->assignRole('jefe_bodega');` es CORRECTO: verifique que jefe_bodega tiene 'aprobar solicitudes' (RolesAndPermissionsSeeder:94-95) y que RolesAndPermissionsSeeder ya esta en el setUp (linea 37). test_la_fila_sin_destino_cae_al_comportamiento_actual (linea 93) sigue valido tal cual (evento sistema.prueba -> urlDestino null -> urlDestinoPara null -> back()). CORRECCION al caso espejo: NO puede reusar la notificacion del jefe. leer() abre con abort_unless($notificacion->user_id === $request->user()->id, 403) (controlador linea 38), asi que un soplador posteando sobre la notificacion ajena recibe 403, jamas back(). El espejo debe crear SU PROPIA notificacion: $soplador = tap(User::factory()->create())->assignRole('soplador'); $n = $this->notifCon($soplador, ['notificable_id' => 77]); y postear con ir=1 esperando ->assertRedirect(route('notificaciones.index')) desde ->from(...). Verificado que soplador solo tiene 'report production' (seeder:114-115), o sea no puede llegar a la bandeja.

* [APLICAR - CONFIRMADO VERBATIM] Instr.5 (cambio de alcance): resources/views/components/layout/sidebar.blade.php:34-35 es `<div class="hidden lg:flex" data-menu-campanita>` + el @include del partial - EXACTO. resources/views/components/layout/topbar.blade.php:20 es `<a href="{{ route('notificaciones.index') }}"` dentro de un <header ... lg:hidden> - EXACTO. Verificado que el partial se incluye en UN solo lugar en todo main (`git grep layouts.partials.campanita origin/main` = 1 hit, sidebar:35). El 'un solo tap' del lote queda desktop-only; para movil el aporte real es el whitespace-pre-line de la bandeja. No tocar sidebar ni topbar. Nota: el HTML del dropdown SI se renderiza en movil (hidden lg:flex es solo CSS), por eso el assertSee('name="ir"', false) del test del lote sobre route('dashboard') sigue verde - lo verifique contra el patron ya probado de CampanitaTest:82-111, que assertea el titulo del dropdown en el dashboard con un usuario SIN roles.

* [REFORMULADO - la version original induce duplicacion] Instr.6 (campanita): main resources/views/layouts/partials/campanita.blade.php tiene @forelse en 28, <form> 29, @csrf 30, <button> 31, span titulo 32, span created_at 33 - EXACTO. PERO el archivo AUTO-MERGEA LIMPIO y el resultado ya es el correcto: verifique el blob mergeado real y trae el hidden name="ir" en 33, el bloque @if (filled($dgN->cuerpo)) con el span 'mt-0.5 line-clamp-2 whitespace-pre-line text-xs text-neutral-500' (SIN 'block') en 36-40, y el created_at con mt-0.5 en 41 - todo despues del hub 'Funciones' y la cabecera nueva de main. INSTRUCCION DEFINITIVA: NO insertar nada a mano; VERIFICAR el resultado del auto-merge contra esa forma. Aplicar la receta literal de la instruccion original DESPUES del merge duplica el <input hidden> y el span del cuerpo. Confirmado que $dgNoLeidas trae modelos completos (MenuPrincipal::campanita() -> Notificacion::campanitaDe(...)->latest('id')->take(5)->get(), app/Support/MenuPrincipal.php:287, sin select parcial).

* [APLICAR - CONFIRMADO VERBATIM] Instr.7 (payload rico): main app/Services/Aprobaciones/Aprobaciones.php tiene 'magnitud' => ... en 320, cierre del array en 321 y cierre de datosNotificacion() en 322 - EXACTO. Bloque use: Configuracion en 6, ReglaAprobacion en 7 - insertar `use App\Models\ProduccionReporte;` entre ambas es alfabeticamente correcto. Verificado que main NO toco este archivo (git diff --stat 61da8ea origin/main -- ese path = vacio) y que TODO lo que el lote consume existe: Aprobacion::resueltoPor() en app/Models/Aprobacion.php:104, Aprobacion::regla() en :109, ReglaAprobacion tiene rol_aprobador en su $fillable:30, Aprobacion::aprobable() en :94, ProduccionReporte::soplador() en :105 con 'fecha' casteado a date (:87) y 'turno' fillable (:68), BORRADOR en :22, y la macro Carbon enChile() en app/Providers/AppServiceProvider.php:30. La forma de datos['nuevo']/['anterior'] esta intacta en app/Http/Controllers/Admin/ProduccionController.php:700-704 y las reglas de validate() de ajustar() (675-684) siguen siendo las mismas 5 cantidades + motivo_ajuste, asi que {cambio} no se ensucia con campos nuevos. Unico consumidor de solicitar() en main sigue siendo ProduccionController:695 (grep unico), o sea la rama instanceof cubre el 100% del trafico. Copiar tal cual, incluida la comparacion como STRING en describirCambio().

* [APLICAR - CONFIRMADO VERBATIM] Instr.8 (fallback nunca-mudo): main app/Services/Notificaciones/NotificacionDispatcher.php:119 es `$cuerpo = is_array($plantilla) ? (string) ($plantilla['cuerpo'] ?? '') : '';` - EXACTO, y main no toco el archivo. Insertar el bloque `if (! is_array($plantilla)) { ... }` justo despues, antes del $reemplazos de 121. Verificado que NO rompe test_sin_plantilla_usa_la_etiqueta_del_catalogo (tests/Feature/Notificaciones/NotificacionDispatchTest.php:126-133): despachar() declara `array $datos = []` (linea 35), asi que con el despacho sin $datos el collect vacio implode a '' y el cuerpo degradado sigue siendo ''. El ->except('url') es intencional.

* [APLICAR - CONFIRMADO VERBATIM] Instr.9 (boton del correo): main app/Mail/NotificacionMail.php tiene content() en 31, `return new Content(` en 33 y `'cuerpo' => ...` en 37 - EXACTO. main resources/views/emails/notificacion.blade.php:22 es el <p> del cuerpo y la 23 es el </td> - EXACTO. main no toco ninguno de los dos. Aplicar tal cual: el $url con su comentario entre 32 y 33, el `'url' => is_string($url) && str_starts_with($url, 'http') ? $url : null,` despues de 37, y el bloque @if (filled($url ?? null)) con la <table> del boton despues de la linea 22.

* [APLICAR - AMPLIADO, la enumeracion original estaba incompleta] Instr.10 (tests de main): la CONCLUSION es correcta - ningun test de main se rompe - pero la lista omitia 5 archivos. Verifique sobre el arbol mergeado real: (a) AprobacionAccionableTest AUTO-MERGEA sin conflicto; los 4 asserts assertSame(route(...), urlDestino()) sobreviven en mergeado 53-55 y 150 y pasan porque notifDe() (mergeado 40-44) crea la notificacion SIN notificable_id -> $ancla = ''. (b) Los 3 asserts de titulo exacto que main tenia en sus lineas 183/194 los AFLOJA el hunk del lote y el merge los ubica bien: mergeado 182/197/203 quedan como assertStringContainsString, y las lineas que exigian la url en el CUERPO (main 183 y 194) desaparecen correctamente, sustituidas por assertSame(...payload['url']) en mergeado 185/198. (c) CampanitaTest no cambio, usa sistema.prueba sin ir -> back(), verde. (d) NotificacionConfigSeedTest: su const CLAVES_M15 (17-32) NO lista ninguna clave notif_plantilla_aprobacion_*, solo cuenta idempotencia. AGREGADO OBLIGATORIO: main tambien modifico AprobacionBandejaTest, AprobacionHistorialTest, NotificacionPanelTest, PreferenciasCanalTest y CREO tests/Feature/Aprobaciones/AprobacionCategoriaTest.php (nuevo, no existe en la rama). Los verifique uno a uno: CategoriaTest solo usa labels + assertSeeInOrder de descripciones (no markup exacto), Bandeja/Historial solo textos y hrefs (el unico raw es assertSee(route(...), false) en Bandeja:188-189 y assertDontSee('>Aprobaciones<', false) en :198), Panel/Preferencias no tocan plantillas de aprobacion. Ninguno se rompe con el id= ni con el comentario Blade. El filtro de verificacion debe incluirlos: --filter='NotificacionEspecificaTest|AprobacionAccionableTest|AprobacionCategoriaTest|AprobacionBandejaTest|AprobacionHistorialTest|CampanitaTest|NotificacionPanelTest|NotificacionConfigSeedTest|NotificacionDispatchTest|AnchoDePaginaTest'. Y OJO con no 'arreglar' notifDe(): si algun dia setea notificable_id, esos 4 asserts pasan a exigir el ancla.

* [APLICAR - CONFIRMADO VERBATIM y verificado que el alcance NO es mayor] Instr.11 (bandeja): tests/Feature/AnchoDePaginaTest.php:80-107 es test_ninguna_vista_declara_su_propio_ancho_de_pagina con el regex /class="mx-auto max-w-[0-9a-z]+ px-/ en la linea 99 - EXACTO. La version del lote de resources/views/notificaciones/index.blade.php:5 declara class="mx-auto max-w-xl px-4 sm:px-6 lg:px-8" y SI matchea el regex - la rompe. Resolver ese conflicto tomando la version de MAIN COMPLETA (65 lineas: <x-app-layout ancho="formulario"> en 1, sin contenedor propio, $destino = $n->urlDestinoPara(auth()->user()) en 27) y re-aplicar UNA sola cosa: agregar whitespace-pre-line a la linea 39 -> <span class="mt-0.5 block whitespace-pre-line text-sm text-neutral-500">{{ $n->cuerpo }}</span>. VERIFICACION EXTRA QUE AMPLIA LA INSTRUCCION: comprobe que el mismo candado NO afecta a las otras dos vistas del lote - la version de la rama de aprobaciones/index.blade.php:6 y mias.blade.php:4 declaran `class="mx-auto max-w-2xl space-y-4 px-4 py-6 sm:px-6"`, y el `space-y-4` intercalado impide que el regex (que exige max-w-XXX seguido inmediatamente de px-) matchee. O sea el candado global solo caza notificaciones/index. No tocar el assert de active:scale-[0.98] de AprobacionAccionableTest (mergeado 156).

* [APLICAR - CONFIRMADO VERBATIM] Instr.12 (anclas): `git grep 'id="aprobacion-' origin/main -- resources/` = 0 hits, el ancla del lote sigue siendo necesaria y no esta duplicada. main resources/views/aprobaciones/index.blade.php:35-36 es el <div x-data="{ paneles: { rechazo: false } }" + su class= de continuacion, ya dentro del @foreach ($delTipo as $aprobacion) de la 34 bajo la <section> del groupBy('tipo_accion')->sortKeys() de la 27 - EXACTO. En mias.blade.php el equivalente es el <li class="space-y-1 px-4 py-4 sm:px-6"> de main linea 30, bajo @foreach ($delTipo as $solicitud) de la 29. Aplicar el id + scroll-mt-6 + target:ring-* sobre ESAS lineas de main (indentacion 24 y 32 espacios respectivamente), no sobre las que indica el diff del lote. Verificado que las variables de vista siguen siendo $pendientes (AprobacionController::index:34-39, ahora via Aprobacion::bandejaDe($user), scope nuevo en Aprobacion.php:130-137 que da TODAS las pendientes al admin) y $solicitudes (AprobacionController::mias:84-92, ->get(), Collection, groupBy funciona), asi que test_la_bandeja_y_mis_solicitudes_emiten_el_ancla (rama, 123-137) sigue valido con su $aprobador con rol admin.

* [NUEVO - dependencia critica que las 12 instrucciones dan por hecha] Resolver el conflicto de app/Models/Notificacion.php (CONFLICTA, no estaba en el alcance asignado pero sin el las anclas son markup muerto y la instr.3 no compila su semantica). El conflicto es TEXTUAL y trivial: en el arbol mergeado la linea `$ancla = $this->notificable_id ? '#aprobacion-'.$this->notificable_id : '';` ya aterrizo limpia en 164 (con su comentario 161-163) y el unico hunk en pugna es el interior del match (mergeado 167-176). Resolucion: CONSERVAR el brazo 'taller.ingresado' => route('admin.servicio-tecnico.index') que main agrego y que la rama no tiene, Y concatenar $ancla en los brazos 'aprobacion.solicitada','aprobacion.escalada' => route('aprobaciones.index').$ancla y 'aprobacion.resuelta' => route('aprobaciones.mias').$ancla. No tocar urlDestinoPara() (114-135): el ancla fluye sola.

* [NUEVO - ausente por completo del analisis] Artefactos de build: public/build/manifest.json CONFLICTA en el merge (verificado con git merge-tree) y el lote arrastra public/build/assets/app-DLrdfNJe.css mientras main lleva app-C7eHxgkz.css; un merge ingenuo deja LOS DOS css y el manifest con marcadores (JSON invalido -> Vite revienta en runtime). Resolucion: tomar el manifest de MAIN, borrar el css huerfano del lote, y correr `npm run build` al final. Buena noticia verificada: el bundle ya comiteado de main YA contiene todas las clases que el lote necesita (.target\:ring-2:target, .target\:ring-brand-300:target, .target\:ring-inset:target, line-clamp-2, whitespace-pre-line, scroll-mt-6), asi que no hay riesgo de clase faltante ni siquiera sin rebuild. Contexto util: main NO usa target:ring en ninguna parte de resources/ (grep vacio) - esas clases entraron a su bundle por el @source '../../storage/framework/views/*.php' de resources/css/app.css:4, es decir por un cache de blades compilados de un arbol que tenia el lote aplicado. Es accidental; al re-aplicar el lote el rebuild las regenera legitimamente desde los .blade.

* [NUEVO - mapa exacto del merge, para no adivinar] El merge real (git merge-tree origin/main origin/feature/notif-especificas) da EXACTAMENTE 5 conflictos: app/Models/Notificacion.php, public/build/manifest.json, resources/views/aprobaciones/index.blade.php, resources/views/aprobaciones/mias.blade.php, resources/views/notificaciones/index.blade.php. TODO lo demas auto-mergea, INCLUIDOS resources/views/layouts/partials/campanita.blade.php, database/seeders/ConfiguracionSeeder.php, app/Http/Controllers/NotificacionUsuarioController.php y tests/Feature/Aprobaciones/AprobacionAccionableTest.php. De los 8 archivos 'sin conflicto' del alcance, los unicos que requieren edicion MANUAL post-merge son: NotificacionUsuarioController.php (urlDestinoPara en vez de urlDestino), Aprobaciones.php, NotificacionDispatcher.php, NotificacionMail.php, emails/notificacion.blade.php y NotificacionEspecificaTest.php. Seeder y campanita: solo verificar.

* [NUEVO - aviso de deriva] origin/main se movio DOS veces durante esta verificacion: 2479353 (el que uso el analisis) -> e3f25d3 -> 6f4c8c0. Los tres son CODE-IDENTICOS para este lote (los deltas son solo docs/fleet/*). El commit 6f4c8c0 agrego docs/fleet/buzon/anexo-reaplicacion-notif1.md, un paquete de re-aplicacion previo que cubre los 5 archivos CONFLICTIVOS y coincide de forma independiente con lo que verifique aqui (urlDestinoPara como wrapper puro, one-shot valida, claves de Marcos disjuntas, baseline 920 verdes). Conviene leerlo antes de resolver: es complementario, no redundante, porque su alcance son los conflictivos y el de estas 12 instrucciones son los no-conflictivos.

RIESGO: CONFIRMADO, GRAVE Y SILENCIOSO (el unico defecto real del lote contra main): el ir=1 del lote usa urlDestino() (rama, controlador linea 44) en vez de urlDestinoPara(). Reintroduce el 403 que main arreglo con urlDestinoPara (Notificacion.php:114-135) y 4 asserts; agravado porque marcarLeida() ya corrio (linea 43) antes del redirect, asi que el usuario pierde la notificacion de la campanita Y aterriza en 403. Ningun test de main lo caza porque ir=1 es nuevo. Superficies verificadas hoy en main: cotizacion.* fuera de la cartera del vendedor (urlDestinoPara:127-129 exige esVisiblePara), taller.ingresado (evento nuevo de main, ruta admin.servicio-tecnico.index) y aprobacion.solicitada/escalada sin el permiso 'aprobar solicitudes' (routes/web.php:71).

RIESGO: CONFIRMADO Y NUEVO (ausente del analisis): public/build/manifest.json CONFLICTA. Un merge resuelto sin cuidado deja marcadores dentro de un JSON -> Vite revienta en runtime, y quedan dos app-*.css (el app-DLrdfNJe.css del lote y el app-C7eHxgkz.css de main). Hay que tomar el manifest de main, borrar el css huerfano y correr npm run build. Riesgo de clase faltante: NULO - verifique que el bundle ya comiteado de main contiene .target\:ring-2:target, .target\:ring-brand-300:target, .target\:ring-inset:target, line-clamp-2, whitespace-pre-line y scroll-mt-6 (aunque main no usa target:ring en resources/: se filtraron via el @source de storage/framework/views de app.css:4).

RIESGO: CONFIRMADO Y NUEVO (ausente del analisis): el token whitespace-pre-line que la instr.11 manda re-aplicar NO TIENE COBERTURA DE TESTS EN NINGUNA DE LAS DOS RAMAS (git grep whitespace-pre -- tests = 0 hits en main y en la rama). Es la UNICA edicion del lote que sobrevive en resources/views/notificaciones/index.blade.php, y si se pierde al resolver el conflicto la suite queda VERDE y nadie se entera. Vale un assertSee('whitespace-pre-line', false) sobre route('notificaciones.index') o revision visual obligatoria.

RIESGO: CONFIRMADO: si el conflicto de resources/views/notificaciones/index.blade.php se resuelve a favor del lote se rompe tests/Feature/AnchoDePaginaTest.php:80-107 (el regex de la linea 99 matchea el mx-auto max-w-xl px-4 de la rama linea 5) Y se pierde el urlDestinoPara() de main (403 de vuelta) y el ->with('notificable'). Del lote ahi solo hace falta 1 palabra. Verificado tambien el limite del riesgo: las versiones de la rama de aprobaciones/index y mias NO trippean ese regex (el space-y-4 intercalado rompe la adyacencia max-w-XXX px-), asi que el candado global solo caza esa vista.

RIESGO: CONFIRMADO: si aprobaciones/index.blade.php y mias.blade.php se resuelven a favor de main, el ancla desaparece en silencio - archivo limpio, compila, pasa TODOS los tests de main. main tiene CERO ocurrencias de id="aprobacion- en resources/. El unico test que lo caza es NotificacionEspecificaTest:123-137. Ademas main reindento y anido esas dos vistas (tarjeta de 12 a 24 espacios, <li> de 24 a 32) al meterlas en la <section> del groupBy, asi que resolver linea por linea con la version de la rama importa la sangria vieja.

RIESGO: CONFIRMADO: la migracion one-shot NO esta rota (verificada byte a byte contra ConfiguracionSeeder.php:72/73, 82/83, 92/93 de main), pero es fragil por diseno: cualquier commit futuro que toque esos 6 strings sin actualizar el $viejo la convierte en no-op silencioso. Vale un candado que exija que el $nuevo de la migracion sea IGUAL al valor sembrado por el seeder para esas 3 claves. Hoy no existe, ni para la migracion de Marcos.

RIESGO: CONFIRMADO Y AMPLIADO: el one-shot de Marcos (2026_07_22_180000) ya vive en la base comun 61da8ea, esta identico en ambas ramas, y sus claves (terreno_*, cotizacion_*) son disjuntas de las del lote (aprobacion_*). Riesgo residual nulo. Amplio la verificacion: las 7 migraciones que main agrego tampoco colisionan ni rompen los fixtures del lote - revise la de procedencia (2026_07_23_130000), que agrega columna nullable con guard hasColumn, asi que el ProduccionAsignacion::create() del arnes pendienteReal() sigue valido.

RIESGO: CONFIRMADO: si al re-aplicar alguien 'arregla' el arnes notifDe() para que setee notificable_id, los 4 asserts assertSame(route(...), urlDestino()) de main (mergeado 53-55 y 150) pasan a exigir el ancla y se ponen rojos. Hoy pasan SOLO porque el helper deja el morph vacio (verificado en el blob mergeado 40-44).

RIESGO: CONFIRMADO (incoherencia de destino que el dueno va a notar, no rompe nada): la campanita navega al ancla #aprobacion-{id} mientras el boton del correo apunta a la LISTA pelada, porque el payload['url'] de Aprobaciones.php:303 sigue siendo route('aprobaciones.mias')/route('aprobaciones.index') sin ancla. Cambiarlo obliga a actualizar los assertSame de payload['url'] en AprobacionAccionableTest mergeado 185 y 198.

RIESGO: CONFIRMADO (cambio de alcance a comunicar): main dejo el dropdown de la campanita solo en escritorio (sidebar.blade.php:34, hidden lg:flex) y en movil la campana es un <a> directo a la bandeja (topbar.blade.php:20). El 'leer y llegar en un solo tap' del lote queda desktop-only; para el celular el aporte real es el whitespace-pre-line de la bandeja. El HTML del dropdown si se renderiza en movil (es CSS), por eso el assertSee('name="ir"') del test del lote sigue verde.

RIESGO: CONFIRMADO (aterrizaje mudo, no cubierto por el lote): la bandeja solo pinta $pendientes (Aprobacion::bandejaDe($user)), asi que si la notificacion se abre despues de que otro resolvio la solicitud, #aprobacion-{id} no existe y el navegador queda arriba sin feedback. Notificacion.php solo cubre el caso notificable_id nulo. Agravado en main: con las secciones por categoria la pagina es mas larga.

RIESGO: NO VERIFIQUE EJECUTANDO LA SUITE (mandato de solo lectura, no modifique ningun archivo). Todo lo anterior sale de leer los blobs de origin/main y de la rama, del arbol de merge real (git merge-tree --write-tree, arbol 3159986 con main=6f4c8c0) y de los asserts literales - no de una corrida de PHPUnit. Aviso adicional: origin/main se movio dos veces durante la verificacion (2479353 -> e3f25d3 -> 6f4c8c0), los tres code-identicos para este lote.

REFUTADA: REFUTADA (fatal en runtime) - Instr.2, la sugerencia 'Opcional (coherencia, no defecto): usar Configuracion::cacheKey($clave) en vez del string pegado'. Es IMPOSIBLE: app/Models/Configuracion.php:42 declara `private static function cacheKey(string $clave): string`. Llamar Configuracion::cacheKey($clave) desde la migracion lanza `Error: Call to private method App\Models\Configuracion::cacheKey() from scope ...` DENTRO de up(), o sea revienta el deploy. El 'config.'.$clave hardcodeado es la unica opcion posible y es exactamente lo que hace la migracion de Marcos (2026_07_22_180000, su linea 61). No aplicar.

REFUTADA: REFUTADA (produce un test imposible) - Instr.4, el caso espejo redactado como 'un soplador con la misma notificacion + ir=1 debe recibir back(), no el redirect'. app/Http/Controllers/NotificacionUsuarioController.php:38 abre con abort_unless($notificacion->user_id === $request->user()->id, 403): un soplador posteando sobre la notificacion del jefe recibe 403, nunca back(). El espejo tiene que crear su PROPIA notificacion (notifCon($soplador, ['notificable_id' => 77])). Con esa correccion la idea es valida y necesaria (verifique que el rol soplador solo tiene 'report production', RolesAndPermissionsSeeder:114-115, o sea urlDestinoPara devuelve null).

REFUTADA: REFUTADA en la forma (riesgo de duplicacion) - Instr.6, el 'como: Tras el @csrf (linea 30) insertar el hidden... Entre la linea 32 y la 33 insertar el bloque @if...'. La instruccion se contradice: reconoce que auto-mergea y aun asi prescribe insercion manual por numero de linea. Verifique el blob del arbol mergeado real: git YA coloco correctamente los tres deltas (hidden en 33, bloque @if (filled($dgN->cuerpo)) en 36-40, mt-0.5 del created_at en 41), respetando el hub 'Funciones' y la cabecera nueva de main. Ejecutar la receta al pie de la letra DESPUES del merge duplica el <input type="hidden" name="ir"> y el span del cuerpo. La instruccion correcta es 'verificar', no 'insertar'.

REFUTADA: REFUTADA como cita (no como conclusion) - Instr.10, 'lo VERIFIQUE sobre el arbol mergeado real (git merge-tree, arbol d80bebe)'. Ese hash no es reproducible en ningun orden ni contra ningun main: obtengo 3909953 (main=2479353, main primero), 51b6496 (invertido), 3159986 (main=6f4c8c0). La provenance no se sostiene. Las 4 afirmaciones sustantivas (a)-(d) SI las verifique de forma independiente y son correctas, asi que la instruccion se mantiene como APLICAR, pero el arbol citado no debe usarse como evidencia.

REFUTADA: REFUTADA por incompleta - Instr.10, 'NINGUN test de main se rompe por los archivos asignados' enumerando solo AprobacionAccionableTest, CampanitaTest y NotificacionConfigSeedTest. main tambien modifico tests/Feature/Aprobaciones/AprobacionBandejaTest.php, tests/Feature/Aprobaciones/AprobacionHistorialTest.php, tests/Feature/Notificaciones/NotificacionPanelTest.php y tests/Feature/Notificaciones/PreferenciasCanalTest.php, y CREO tests/Feature/Aprobaciones/AprobacionCategoriaTest.php (no existe ni en 61da8ea ni en la rama) - justamente el test que ejercita el markup reindentado donde va el ancla. Los revise todos y la CONCLUSION aguanta (ninguno fija markup exacto de la tarjeta), pero la lista de verificacion del parte estaba incompleta y omitia el unico test nuevo de main en la superficie tocada.

REFUTADA: REFUTADA por imprecision menor (no afecta la accion) - Instr.2 cita 'Configuracion::cacheKey() en main (app/Models/Configuracion.php:44)'. La linea 44 es el `return 'config.'.$clave;`; la firma del metodo esta en la 42. Irrelevante para la accion, pero la cita no apunta a la declaracion.

