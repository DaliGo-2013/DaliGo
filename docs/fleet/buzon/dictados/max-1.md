# Dictado vigente — Max-1 (Forjador A, stream 1)
> Emitido por el Director el 2026-08-19 (v71 — DASH-3 EN PRODUCCIÓN: MÓDULO DASHBOARD COMPLETO. EN PAUSA hasta el QA del dueño → luego auditoría de Comercial). Manda sobre lo anterior.

MODELO: Opus 4.8 · high.

## ✅ DASH-3 está EN PRODUCCIÓN (merge `75cce08`, doble llave 19-ago)

Suite del Director: **2208 verdes, cero rojos reales** (el único rojo fue entorno de MI
worktree — íconos PWA borrados en disco, I-11 aplicada; tu lote impecable). Nota del
merge: el primer push chocó con drift SOLO-DOCS (el diseño F0-MENSAJES de Max-2 entró
en paralelo) → re-merge sobre fresco sin re-suite (árbol de código idéntico). Rama
borrada, `P-PAR-DASH-3` marcado.

**Tu contra-evidencia del orden fue ACEPTADA** — alfabético rompía el candado
byte-a-byte del propio dictado; `orderBy('id')` reproduce el string vigente. Así se
pisa un dictado: con la regla de oro como juez. Si el dueño prefiere alfabético en el
QA, es una línea.

## 🏁 MÓDULO DASHBOARD COMPLETO — el mapa F0-DASH quedó SALDADO

4 perillas en Configuración (serie, merma, dos cortes con validación cruzada) + 1 card
viva (Sucursales) + 4 nivel-3 con porqué + 6 cross-módulo anotados. Tres lotes de
código, delta +12 tests, **cero amoldes y cero cifras viejas cambiadas en todo el
módulo** — la regla de oro sobrevivió entera.

## ⏸️ EN PAUSA — Comercial espera el QA del dueño del módulo Dashboard

El QA (la prueba reina): el Inicio IDÉNTICO a como se veía antes del proyecto sin tocar
nada; las 4 perillas en Configuración (grupo dashboard) editando y viendo el efecto;
crear una sucursal de prueba y verla aparecer en la card al tiro (y apagarla después);
intentar valores inválidos y ver los rechazos con mensaje.

Con el visto bueno llega el **v72: F0-COMERCIAL** (auditoría solo-docs del módulo
Comercial, mismo formato del mapa — Clientes, Catálogo con sus pestañas, Cotizaciones
y lo que cuelgue). Los 6 cross-módulo del mapa DASH que sean de Comercial se cruzan ahí.

## Estado
- **Max-2 ACTIVO**: PLAN-MENSAJES (chat interno) — diseño F0 entregado, espera vistos
  buenos del dueño. Coordinación de territorio: sus lotes MSG-1 (toca
  `Notificacion::EVENTOS`) y MSG-4 (toca `MenuPrincipal`) los secuencia el Director
  contigo — ninguno de tus lotes de Comercial debería tocar esos archivos, pero
  re-fetch religioso como siempre.
- Marcos activo. Baseline: **2208 verdes** en `75cce08`.

CIERRE: sin acción. Primer módulo del proyecto saldado de punta a punta — molde
probado. Espera el QA del dueño.
